---
phase: 15-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-compiler/src/ast/expr.rs
  - writ-compiler/src/ast/decl.rs
  - writ-compiler/tests/lowering_tests.rs
autonomous: true
requirements: ["N/A (bookkeeping)"]

must_haves:
  truths:
    - "AstExpr::StructLit dead variant is removed from ast/expr.rs"
    - "AstComponentDecl doc comment no longer references pre-Phase-13 entity lowering model"
    - "Test comments in lowering_tests.rs no longer reference IndexMut or pre-Phase-13 model ($Health fields, ComponentAccess impls, StructLit initializers)"
    - "Codebase compiles cleanly after all changes"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "writ-compiler/src/ast/expr.rs"
      provides: "Clean AstExpr enum with StructLit variant removed"
    - path: "writ-compiler/src/ast/decl.rs"
      provides: "Corrected AstComponentDecl doc comment"
    - path: "writ-compiler/tests/lowering_tests.rs"
      provides: "Updated test comments reflecting current entity model"
  key_links: []
---

<objective>
Remove dead code and fix stale comments identified by the v1.1 milestone audit.

Purpose: Clean up tech debt items that reference pre-Phase-13 entity lowering behavior (before the entity model rewrite). The dead `AstExpr::StructLit` variant, stale `AstComponentDecl` doc comment, and outdated test comments all reference the old model where entities lowered to Struct + Impl chains with $ComponentName fields and ComponentAccess impls. Phase 13 replaced this with AstDecl::Entity + AstComponentSlot descriptors.
Output: Clean codebase with no dead code or stale references to the old entity model.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Audit identifying the tech debt items
@.planning/v1.1-MILESTONE-AUDIT.md

# Source files to modify
@writ-compiler/src/ast/expr.rs
@writ-compiler/src/ast/decl.rs
@writ-compiler/tests/lowering_tests.rs

<interfaces>
<!-- Current AstExpr::StructLit variant (lines 135-144 of expr.rs) — to be REMOVED: -->
```rust
// --- Struct literal ---

/// Struct literal: `Health { current: 80, max: 80 }`
/// Used for component field initializers in entity lowering.
StructLit {
    name: String,
    name_span: SimpleSpan,
    fields: Vec<(String, SimpleSpan, AstExpr)>,  // (field_name, name_span, value)
    span: SimpleSpan,
},
```

<!-- Current AstComponentDecl doc comment (lines 358-361 of decl.rs) — line 360 is stale: -->
```rust
/// Component declaration: `[attrs] [vis] component Name { members }`
///
/// Note: Entity declarations are lowered to Struct + Impl + lifecycle registrations
/// before reaching the AST. Components survive as-is.
```

<!-- Stale test comments in lowering_tests.rs — lines to update: -->
```
Line 216: /// operator []= → impl IndexMut<int, string> for MyList { fn index_set(idx: int, val: string) { } }
Line 570: /// use Health { current: 80, max: 80 } → $Health field with StructLit initializer + ComponentAccess<Health> impl
Line 577: /// Empty use clause STILL produces $Speaker field with empty StructLit and ComponentAccess<Speaker> impl
Line 620: /// struct, inherent impl, ComponentAccess impl, OnCreate impl
Line 629: /// Two distinct $Health and $Sprite fields, two separate ComponentAccess impls in source order
Line 639: // use Component { field: val } → $Component field with StructLit initializer
Line 643: /// StructLit contains only `current: 50`, NOT `max` (not overridden → absent; type checker fills default)
Line 650: /// Empty use clause — StructLit has empty fields vec (same behavior as entity_empty_use_clause)
Line 699: // MISC-01: IndexSet contract name (not IndexMut)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove AstExpr::StructLit and fix AstComponentDecl doc comment</name>
  <files>writ-compiler/src/ast/expr.rs, writ-compiler/src/ast/decl.rs</files>
  <action>
**1. Remove `AstExpr::StructLit` dead variant from `writ-compiler/src/ast/expr.rs`:**

Delete lines 135-144 (the `// --- Struct literal ---` comment, the doc comment, and the `StructLit { ... }` variant). This variant is never constructed anywhere in the codebase (confirmed by grep: zero occurrences of `AstExpr::StructLit` in `writ-compiler/src/`). It was added in Phase 5 (v1.0) for the old entity lowering model and became dead code when Phase 13 replaced entity lowering with `AstDecl::Entity` + `AstComponentSlot` descriptors.

**2. Fix stale doc comment on `AstComponentDecl` in `writ-compiler/src/ast/decl.rs`:**

Replace the stale comment at lines 358-361:
```rust
/// Component declaration: `[attrs] [vis] component Name { members }`
///
/// Note: Entity declarations are lowered to Struct + Impl + lifecycle registrations
/// before reaching the AST. Components survive as-is.
```

With the corrected comment:
```rust
/// Component declaration: `[attrs] [vis] component Name { members }`
///
/// Components are extern-only, host-managed data containers (no script-defined components).
/// Entity `use` clauses lower to `AstComponentSlot` descriptors, not component decls.
```

**3. Verify compilation:**
Run `cargo build --workspace` to confirm clean compilation after the StructLit removal. The variant is never constructed or matched, so removal should be clean.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo build --workspace 2>&1 | tail -3</automated>
  </verify>
  <done>AstExpr::StructLit variant removed, AstComponentDecl doc comment corrected, codebase compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Update stale test comments in lowering_tests.rs</name>
  <files>writ-compiler/tests/lowering_tests.rs</files>
  <action>
Update all stale test comments in `writ-compiler/tests/lowering_tests.rs` that reference the pre-Phase-13 entity model (IndexMut, $Health fields, ComponentAccess impls, StructLit initializers). The test code itself is correct — only the DOC COMMENTS above test functions need updating.

**Replacements (comments only, do NOT change test code or function names):**

1. **Line 216** — Update IndexMut reference:
   - Old: `/// operator []= → impl IndexMut<int, string> for MyList { fn index_set(idx: int, val: string) { } }`
   - New: `/// operator []= → impl IndexSet<int, string> for MyList { fn index_set(idx: int, val: string) { } }`

2. **Line 570** — Update pre-Phase-13 entity model reference:
   - Old: `/// use Health { current: 80, max: 80 } → $Health field with StructLit initializer + ComponentAccess<Health> impl`
   - New: `/// use Health { current: 80, max: 80 } → AstComponentSlot with overrides (host-managed descriptor)`

3. **Line 577** — Update pre-Phase-13 entity model reference:
   - Old: `/// Empty use clause STILL produces $Speaker field with empty StructLit and ComponentAccess<Speaker> impl`
   - New: `/// Empty use clause produces AstComponentSlot with empty overrides vec`

4. **Line 620** — Update emission order comment:
   - Old: `/// struct, inherent impl, ComponentAccess impl, OnCreate impl`
   - New: `/// AstEntityDecl with properties, component_slots, inherent_impl, hooks`

5. **Line 629** — Update multiple use clause comment:
   - Old: `/// Two distinct $Health and $Sprite fields, two separate ComponentAccess impls in source order`
   - New: `/// Two distinct AstComponentSlot entries in source order`

6. **Lines 639-640** — Update R13 section header comment:
   - Old: `// use Component { field: val } → $Component field with StructLit initializer`
   - New: `// use Component { field: val } → AstComponentSlot descriptor with overrides`

7. **Line 643** — Update partial override comment:
   - Old: `/// StructLit contains only \`current: 50\`, NOT \`max\` (not overridden → absent; type checker fills default)`
   - New: `/// Overrides contain only \`current: 50\`, NOT \`max\` (not overridden → absent; type checker fills default)`

8. **Line 650** — Update empty use clause comment:
   - Old: `/// Empty use clause — StructLit has empty fields vec (same behavior as entity_empty_use_clause)`
   - New: `/// Empty use clause — overrides vec is empty (same behavior as entity_empty_use_clause)`

9. **Line 699** — Already correct format but contains "(not IndexMut)" parenthetical. Leave as-is — it correctly documents MISC-01 as the fix from IndexMut to IndexSet.

After all comment changes, run `cargo test --workspace` to verify all tests still pass (comments don't affect test behavior, but this confirms nothing was accidentally broken).
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test --workspace 2>&1 | grep "test result:" | grep -v "0 passed"</automated>
  </verify>
  <done>All 8 stale comments updated to reflect Phase 13 entity model (AstComponentSlot, AstEntityDecl). All 440 tests pass. No references to $Health fields, ComponentAccess impls, or StructLit initializers remain in test comments.</done>
</task>

</tasks>

<verification>
1. `grep "StructLit" writ-compiler/src/ast/expr.rs` returns zero matches — dead variant removed
2. `grep "Struct + Impl" writ-compiler/src/ast/decl.rs` returns zero matches — stale doc comment fixed
3. `grep "IndexMut" writ-compiler/tests/lowering_tests.rs` returns zero matches at line 216 — stale reference updated
4. `grep "\$Health" writ-compiler/tests/lowering_tests.rs` returns zero matches — pre-Phase-13 references removed
5. `grep "ComponentAccess" writ-compiler/tests/lowering_tests.rs` returns zero matches — pre-Phase-13 references removed
6. `cargo build --workspace` succeeds with no errors
7. `cargo test --workspace` passes all tests with zero regressions
</verification>

<success_criteria>
- AstExpr::StructLit variant no longer exists in writ-compiler/src/ast/expr.rs
- AstComponentDecl doc comment references AstComponentSlot (not Struct + Impl)
- No test comments reference IndexMut (line 216), $Health fields, ComponentAccess impls, or StructLit initializers
- `cargo build --workspace` compiles cleanly
- `cargo test --workspace` passes all 440 tests
</success_criteria>

<output>
After completion, create `.planning/phases/15-tech-debt-cleanup/15-02-SUMMARY.md`
</output>

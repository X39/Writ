---
phase: 16-module-format-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - writ-module/Cargo.toml
  - writ-module/src/lib.rs
  - writ-module/src/error.rs
  - writ-module/src/token.rs
  - writ-module/src/tables.rs
  - writ-module/src/instruction.rs
  - writ-module/src/module.rs
  - writ-module/src/heap.rs
  - writ-module/tests/token_tests.rs
  - writ-module/tests/instruction_tests.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05

must_haves:
  truths:
    - "MetadataToken(0) is null; all token lookups return None for index 0 and Some(row) for index >= 1"
    - "All 91 opcodes have enum variants with correct operand fields matching the spec shapes"
    - "Every instruction encodes to bytes and decodes back to the same instruction (round-trip identity)"
    - "All 21 metadata table row structs exist with fields matching spec section 2.16.5"
  artifacts:
    - path: "writ-module/src/token.rs"
      provides: "MetadataToken newtype with NULL, new(), table_id(), row_index(), is_null()"
      contains: "MetadataToken"
    - path: "writ-module/src/instruction.rs"
      provides: "Instruction enum with all 91 opcode variants plus encode/decode functions"
      contains: "pub enum Instruction"
    - path: "writ-module/src/tables.rs"
      provides: "21 row structs for all metadata tables"
      contains: "TypeDefRow"
    - path: "writ-module/src/module.rs"
      provides: "Module struct holding header, heaps, 21 table Vecs, and method bodies"
      contains: "pub struct Module"
    - path: "writ-module/src/error.rs"
      provides: "ModuleError enum with decode/encode variants"
      contains: "pub enum ModuleError"
    - path: "writ-module/tests/token_tests.rs"
      provides: "MetadataToken null/1-based tests"
    - path: "writ-module/tests/instruction_tests.rs"
      provides: "All 91 opcodes encode/decode round-trip test"
  key_links:
    - from: "writ-module/src/instruction.rs"
      to: "writ-module/src/token.rs"
      via: "MetadataToken used in instruction operands (method_idx, type_idx, etc. are u32 metadata tokens)"
      pattern: "MetadataToken"
    - from: "writ-module/src/module.rs"
      to: "writ-module/src/tables.rs"
      via: "Module struct holds Vec<Row> for each table"
      pattern: "Vec<.*Row>"
---

<objective>
Create the `writ-module` crate with all data types: MetadataToken newtype, 21 metadata table row structs, the 91-opcode Instruction enum with encode/decode, the Module struct, error types, and heap utilities. This is the pure-data foundation that the binary reader/writer (Plan 02) and ModuleBuilder (Plan 03) build upon.

Purpose: Establish the complete type system for the IL module format so that subsequent plans can implement serialization and construction without defining any types.
Output: A compiling `writ-module` crate with all public types, instruction encode/decode, and tests for MetadataToken and instruction round-trip.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-module-format-foundation/16-CONTEXT.md
@.planning/phases/16-module-format-foundation/16-RESEARCH.md

Spec references (read these for precise field layouts):
@language-spec/spec/45_2_16_il_module_format.md — Header, table directory, 21 tables, method body layout, metadata tokens
@language-spec/spec/44_2_15_il_type_system.md — TypeRef encoding, primitive type tags
@language-spec/spec/34_2_5_instruction_encoding.md — Opcode numbering scheme, instruction shapes
@language-spec/spec/67_4_2_opcode_assignment_table.md — Full opcode assignment (91 opcodes with hex values and shapes)
@language-spec/spec/66_4_1_instruction_shape_reference.md — Shape byte layouts
@language-spec/spec/54_3_6_control_flow.md — SWITCH var layout
@language-spec/spec/55_3_7_calls.md — CALL_VIRT, NEW_DELEGATE, CALL_INDIRECT, TAIL_CALL var layouts
@language-spec/spec/56_3_8_object_model.md — GET_FIELD, SET_FIELD, GET_COMPONENT var layouts
@language-spec/spec/57_3_9_arrays.md — ARRAY_INIT, ARRAY_SLICE var layouts
@language-spec/spec/58_3_10_type_operations.md — NEW_ENUM, EXTRACT_FIELD var layouts
@language-spec/spec/60_3_12_globals_atomics.md — STORE_GLOBAL var layout
@language-spec/spec/61_3_13_conversion.md — CONVERT var layout
@language-spec/spec/62_3_14_strings.md — STR_BUILD var layout
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create writ-module crate with data types, MetadataToken, tables, Module struct, and heap utilities</name>
  <files>
    Cargo.toml,
    writ-module/Cargo.toml,
    writ-module/src/lib.rs,
    writ-module/src/error.rs,
    writ-module/src/token.rs,
    writ-module/src/tables.rs,
    writ-module/src/module.rs,
    writ-module/src/heap.rs,
    writ-module/tests/token_tests.rs
  </files>
  <behavior>
    - MetadataToken::NULL.is_null() == true
    - MetadataToken::NULL.row_index() == None
    - MetadataToken::new(2, 5).table_id() == 2
    - MetadataToken::new(2, 5).row_index() == Some(5)
    - MetadataToken::new(2, 5).0 == 0x02_000005
    - MetadataToken(0).is_null() == true
    - String heap: offset 0 reads as empty string; intern_string returns offset >= 4; read_string round-trips correctly
    - Blob heap: same pattern as string heap (offset 0 = empty blob)
  </behavior>
  <action>
    1. Add `"writ-module"` to workspace members in root `Cargo.toml`.

    2. Create `writ-module/Cargo.toml` with:
       - `[package]` name = "writ-module", edition = "2024"
       - `[dependencies]` byteorder = "1.5", thiserror = "2.0"
       - `[dev-dependencies]` insta = { version = "1", features = ["ron"] }

    3. Create `writ-module/src/error.rs`:
       - `pub enum ModuleError` with variants: `Decode(DecodeError)`, `Encode(EncodeError)`
       - `pub enum DecodeError` with variants: `Io(std::io::Error)`, `BadMagic([u8; 4])`, `UnsupportedVersion(u16)`, `BadUtf8(std::str::Utf8Error)`, `InvalidOpcode(u16)`, `InvalidTableId(u8)`, `UnexpectedEof`, `TypeRefTooDeep`, `InvalidTypeRefKind(u8)`, `BufferTooSmall`
       - `pub enum EncodeError` with variants: `Io(std::io::Error)`, `StringTooLarge(usize)`, `TooManyRows(usize)`
       - Derive `thiserror::Error` and `Debug` on all three

    4. Create `writ-module/src/token.rs`:
       - `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)] pub struct MetadataToken(pub u32);`
       - `const NULL: MetadataToken = MetadataToken(0);`
       - `fn new(table_id: u8, row_index: u32) -> Self` — panics if row_index > 0x00FF_FFFF
       - `fn table_id(self) -> u8` — bits 31-24
       - `fn row_index(self) -> Option<u32>` — bits 23-0, returns None if 0
       - `fn is_null(self) -> bool` — self.0 == 0

    5. Create `writ-module/src/tables.rs` — all 21 row structs exactly matching spec §2.16.5:
       - `ModuleDefRow { name: u32, version: u32, flags: u32 }`
       - `ModuleRefRow { name: u32, min_version: u32 }`
       - `TypeDefRow { name: u32, namespace: u32, kind: u8, flags: u16, field_list: u32, method_list: u32 }`
       - `TypeRefRow { scope: MetadataToken, name: u32, namespace: u32 }`
       - `TypeSpecRow { signature: u32 }` (blob offset)
       - `FieldDefRow { name: u32, type_sig: u32, flags: u16 }`
       - `FieldRefRow { parent: MetadataToken, name: u32, type_sig: u32 }`
       - `MethodDefRow { name: u32, signature: u32, flags: u16, body_offset: u32, body_size: u32, reg_count: u16 }`
       - `MethodRefRow { parent: MetadataToken, name: u32, signature: u32 }`
       - `ParamDefRow { name: u32, type_sig: u32, sequence: u16 }`
       - `ContractDefRow { name: u32, namespace: u32, method_list: u32, generic_param_list: u32 }`
       - `ContractMethodRow { name: u32, signature: u32, slot: u16 }`
       - `ImplDefRow { type_token: MetadataToken, contract: MetadataToken, method_list: u32 }`
       - `GenericParamRow { owner: MetadataToken, owner_kind: u8, ordinal: u16, name: u32 }`
       - `GenericConstraintRow { param: u32, constraint: MetadataToken }`
       - `GlobalDefRow { name: u32, type_sig: u32, flags: u16, init_value: u32 }`
       - `ExternDefRow { name: u32, signature: u32, import_name: u32, flags: u16 }`
       - `ComponentSlotRow { owner_entity: MetadataToken, component_type: MetadataToken }`
       - `LocaleDefRow { dlg_method: MetadataToken, locale: u32, loc_method: MetadataToken }`
       - `ExportDefRow { name: u32, item_kind: u8, item: MetadataToken }`
       - `AttributeDefRow { owner: MetadataToken, owner_kind: u8, name: u32, value: u32 }`
       - All structs: `#[derive(Debug, Clone, PartialEq)]`
       - Also define `TypeDefKind` enum: `Struct = 0, Enum = 1, Entity = 2, Component = 3`
       - Also define `TableId` enum with explicit u8 discriminants 0-20 matching the spec order. Include `fn from_u8(id: u8) -> Option<TableId>` and `fn as_u8(&self) -> u8`.

    6. Create `writ-module/src/heap.rs`:
       - `pub fn init_string_heap() -> Vec<u8>` — writes 4-byte zero-length record at offset 0 (null/empty string)
       - `pub fn intern_string(heap: &mut Vec<u8>, s: &str) -> u32` — appends u32(len) + bytes, returns offset
       - `pub fn read_string(heap: &[u8], offset: u32) -> Result<&str, DecodeError>` — offset 0 returns "", else reads u32 len + bytes
       - `pub fn init_blob_heap() -> Vec<u8>` — same pattern as string heap
       - `pub fn write_blob(heap: &mut Vec<u8>, data: &[u8]) -> u32` — appends u32(len) + data, returns offset
       - `pub fn read_blob(heap: &[u8], offset: u32) -> Result<&[u8], DecodeError>` — offset 0 returns &[], else reads u32 len + data
       - Use `byteorder::LittleEndian` + `WriteBytesExt` for heap writes

    7. Create `writ-module/src/module.rs`:
       - `pub struct ModuleHeader { pub format_version: u16, pub flags: u16, pub module_name: u32, pub module_version: u32, pub string_heap_offset: u32, pub string_heap_size: u32, pub blob_heap_offset: u32, pub blob_heap_size: u32, pub table_directory: [(u32, u32); 21] }`
       - `pub struct MethodBody { pub register_types: Vec<u32>, pub code: Vec<u8>, pub debug_locals: Vec<DebugLocal>, pub source_spans: Vec<SourceSpan> }`
       - `pub struct DebugLocal { pub register: u16, pub name: u32, pub start_pc: u32, pub end_pc: u32 }`
       - `pub struct SourceSpan { pub pc: u32, pub line: u32, pub column: u16 }`
       - `pub struct Module` with all 21 table Vecs (using the row types from tables.rs), `string_heap: Vec<u8>`, `blob_heap: Vec<u8>`, `header: ModuleHeader`, `method_bodies: Vec<MethodBody>`
       - Derive `Debug, Clone` on Module
       - Add `Module::new()` that creates an empty module with initialized heaps (offset 0 reserved) and format_version = 1

    8. Create `writ-module/src/lib.rs`:
       - `pub mod error;`, `pub mod token;`, `pub mod tables;`, `pub mod instruction;`, `pub mod module;`, `pub mod heap;`
       - Re-export key types: `pub use token::MetadataToken;`, `pub use module::Module;`, `pub use instruction::Instruction;`, `pub use error::{ModuleError, DecodeError, EncodeError};`

    9. Create `writ-module/tests/token_tests.rs` with tests for:
       - NULL token is_null, table_id == 0, row_index == None
       - new(2, 5) produces 0x02_000005, table_id == 2, row_index == Some(5)
       - new(0x07, 0x0A) produces 0x07_00000A
       - row_index of token with index 1 returns Some(1)
       - is_null on non-null token returns false
       - #[should_panic] test for new(0, 0x01000000) (exceeds 24 bits)

    Run `cargo build -p writ-module` to verify compilation. Run `cargo test -p writ-module` to verify token tests pass.
  </action>
  <verify>
    <automated>cargo test -p writ-module --test token_tests</automated>
  </verify>
  <done>
    - writ-module compiles with no errors
    - MetadataToken tests pass (null token, 1-based indexing, table_id extraction, row_index extraction, panic on overflow)
    - All 21 table row structs exist with fields matching the spec
    - Module struct compiles with all table Vecs and heap byte vecs
    - Heap utility functions compile
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Instruction enum with all 91 opcodes and encode/decode round-trip</name>
  <files>
    writ-module/src/instruction.rs,
    writ-module/tests/instruction_tests.rs
  </files>
  <behavior>
    - Every one of the 91 opcodes has an Instruction enum variant with correct operand types
    - Instruction::encode(&self, writer) writes the correct opcode u16 + operand bytes per spec shapes
    - Instruction::decode(reader) reads opcode u16 + operand bytes and returns the correct variant
    - For all 91 opcodes: encode(instr) then decode produces identical instr (round-trip identity)
    - SWITCH with n=0, n=1, n=5 round-trips correctly
    - Unknown opcode returns DecodeError::InvalidOpcode
    - LOAD_INT with i64::MAX and i64::MIN round-trips
    - LOAD_FLOAT with f64 values round-trips (bit-exact via to_bits/from_bits)
  </behavior>
  <action>
    Create `writ-module/src/instruction.rs` with:

    1. `pub enum Instruction` — flat enum, one variant per opcode. Group by category with comments. Use the exact opcode hex values from §4.2. Operand field names match the spec exactly.

    Instruction shapes and their variants (referencing §4.1 + §4.2):

    **Shape N (2B):** `Nop`, `RetVoid`, `AtomicBegin`, `AtomicEnd`, `DeferPop`, `DeferEnd`
    **Shape R (4B):** `Crash { r_msg: u16 }`, `LoadTrue { r_dst: u16 }`, `LoadFalse { r_dst: u16 }`, `LoadNull { r_dst: u16 }`, `Ret { r_src: u16 }`, `InitEntity { r_entity: u16 }`, `DestroyEntity { r_entity: u16 }`, `Cancel { r_task: u16 }`
    **Shape RR (6B):** `Mov { r_dst: u16, r_src: u16 }`, `NegI { r_dst: u16, r_src: u16 }`, `NegF { r_dst: u16, r_src: u16 }`, `Not { r_dst: u16, r_src: u16 }`, `ArrayLen { r_dst: u16, r_arr: u16 }`, `ArrayAdd { r_arr: u16, r_val: u16 }`, `ArrayRemove { r_arr: u16, r_idx: u16 }`, `WrapSome { r_dst: u16, r_val: u16 }`, `Unwrap { r_dst: u16, r_opt: u16 }`, `IsSome { r_dst: u16, r_opt: u16 }`, `IsNone { r_dst: u16, r_opt: u16 }`, `WrapOk { r_dst: u16, r_val: u16 }`, `WrapErr { r_dst: u16, r_err: u16 }`, `UnwrapOk { r_dst: u16, r_result: u16 }`, `IsOk { r_dst: u16, r_result: u16 }`, `IsErr { r_dst: u16, r_result: u16 }`, `ExtractErr { r_dst: u16, r_result: u16 }`, `GetTag { r_dst: u16, r_enum: u16 }`, `Join { r_dst: u16, r_task: u16 }`, `I2f { r_dst: u16, r_src: u16 }`, `F2i { r_dst: u16, r_src: u16 }`, `I2s { r_dst: u16, r_src: u16 }`, `F2s { r_dst: u16, r_src: u16 }`, `B2s { r_dst: u16, r_src: u16 }`, `StrLen { r_dst: u16, r_str: u16 }`, `Box { r_dst: u16, r_val: u16 }`, `Unbox { r_dst: u16, r_boxed: u16 }`, `EntityIsAlive { r_dst: u16, r_entity: u16 }`
    **Shape RRR (8B):** `AddI`, `SubI`, `MulI`, `DivI`, `ModI`, `AddF`, `SubF`, `MulF`, `DivF`, `ModF`, `BitAnd`, `BitOr`, `Shl`, `Shr`, `CmpEqI`, `CmpEqF`, `CmpEqB`, `CmpEqS`, `CmpLtI`, `CmpLtF`, `ArrayLoad`, `ArrayStore`, `ArrayInsert`, `StrConcat` — all with `{ r_dst: u16, r_a: u16, r_b: u16 }` (or appropriate field names per spec)
    **Shape RI32 (8B):** `LoadString { r_dst: u16, string_idx: u32 }`, `New { r_dst: u16, type_idx: u32 }`, `SpawnEntity { r_dst: u16, type_idx: u32 }`, `GetOrCreate { r_dst: u16, type_idx: u32 }`, `FindAll { r_dst: u16, type_idx: u32 }`, `NewArray { r_dst: u16, elem_type: u32 }`, `DeferPush { r_dst: u16, method_idx: u32 }`, `LoadGlobal { r_dst: u16, global_idx: u32 }`, `BrTrue { r_cond: u16, offset: i32 }`, `BrFalse { r_cond: u16, offset: i32 }`
    **Note on BrTrue/BrFalse:** The RI32 shape is `u16(op) u16(r) u32(imm)` but for branches the imm is i32. Store as i32, cast to/from u32 for encode/decode.
    **Shape RI64 (12B):** `LoadInt { r_dst: u16, value: i64 }`, `LoadFloat { r_dst: u16, value: f64 }`
    **Note on LoadFloat:** Encode/decode via `f64::to_bits()` / `f64::from_bits()` for bit-exact round-trip.
    **Shape I32 (8B):** `Br { offset: i32 }` — `u16(op) u16(pad=0) i32(offset)`
    **Shape CALL (12B):** `Call { r_dst: u16, method_idx: u32, r_base: u16, argc: u16 }`, `CallExtern { r_dst: u16, extern_idx: u32, r_base: u16, argc: u16 }`, `SpawnTask { r_dst: u16, method_idx: u32, r_base: u16, argc: u16 }`, `SpawnDetached { r_dst: u16, method_idx: u32, r_base: u16, argc: u16 }`
    **Var-layout:**
    - `Switch { r_tag: u16, offsets: Vec<i32> }` — 6 + 4n bytes: `u16(op) u16(r_tag) u16(n) i32[n]`
    - `CallVirt { r_dst: u16, r_obj: u16, contract_idx: u32, slot: u16, r_base: u16, argc: u16 }` — 14B
    - `NewDelegate { r_dst: u16, method_idx: u32, r_target: u16 }` — 10B
    - `CallIndirect { r_dst: u16, r_delegate: u16, r_base: u16, argc: u16 }` — 10B
    - `TailCall { method_idx: u32, r_base: u16, argc: u16 }` — 10B
    - `GetField { r_dst: u16, r_obj: u16, field_idx: u32 }` — 10B
    - `SetField { r_obj: u16, field_idx: u32, r_val: u16 }` — 10B
    - `GetComponent { r_dst: u16, r_entity: u16, comp_type_idx: u32 }` — 10B (same as GetField shape)
    - `ArrayInit { r_dst: u16, elem_type: u32, count: u16, r_base: u16 }` — 12B
    - `ArraySlice { r_dst: u16, r_arr: u16, r_start: u16, r_end: u16 }` — 10B
    - `NewEnum { r_dst: u16, type_idx: u32, tag: u16, field_count: u16, r_base: u16 }` — 14B
    - `ExtractField { r_dst: u16, r_enum: u16, field_idx: u16 }` — 8B
    - `StoreGlobal { global_idx: u32, r_src: u16 }` — 8B: `u16(op) u32(global_idx) u16(r_src)`
    - `Convert { r_dst: u16, r_src: u16, target_type: u32 }` — 10B
    - `StrBuild { r_dst: u16, count: u16, r_base: u16 }` — 8B

    2. `pub fn opcode(&self) -> u16` — returns the u16 opcode for each variant

    3. `pub fn encode<W: Write>(&self, writer: &mut W) -> Result<(), EncodeError>` — writes opcode + operands using `WriteBytesExt::<LittleEndian>`

    4. `pub fn decode<R: Read>(reader: &mut R) -> Result<Self, DecodeError>` — reads u16 opcode, matches, reads operands using `ReadBytesExt::<LittleEndian>`

    5. Create `writ-module/tests/instruction_tests.rs`:
       - A `round_trip(instr)` helper that encodes to Vec<u8>, decodes, asserts equality
       - One test per instruction shape exercising at least one representative instruction
       - A comprehensive `test_all_91_opcodes_round_trip` test that creates one instance of every variant with non-trivial operand values and round-trips each
       - Edge cases: SWITCH n=0, SWITCH n=5, LOAD_INT i64::MAX, LOAD_INT i64::MIN, LOAD_FLOAT with PI, BR with negative offset

    Implement `PartialEq` on `Instruction` (derive or manual for f64 — use `to_bits()` comparison for LOAD_FLOAT).
  </action>
  <verify>
    <automated>cargo test -p writ-module --test instruction_tests</automated>
  </verify>
  <done>
    - All 91 instruction variants exist in the enum
    - encode/decode round-trip test passes for all 91 opcodes
    - SWITCH with variable-length offsets round-trips correctly
    - Invalid opcode produces DecodeError::InvalidOpcode
    - `cargo test -p writ-module` passes all tests (token + instruction)
  </done>
</task>

</tasks>

<verification>
1. `cargo build -p writ-module` compiles with no errors or warnings
2. `cargo test -p writ-module` passes all tests
3. MetadataToken null/1-based tests pass
4. All 91 opcodes encode and decode without information loss
5. Module struct exists with all 21 table Vecs
</verification>

<success_criteria>
- writ-module crate compiles as a standalone workspace member
- MetadataToken enforces 1-based indexing with null = 0
- All 91 opcodes are represented and round-trip through encode/decode
- All 21 metadata table row structs match spec §2.16.5
- Module struct holds complete in-memory module representation
- Error types support downstream reader/writer needs
</success_criteria>

<output>
After completion, create `.planning/phases/16-module-format-foundation/16-01-SUMMARY.md`
</output>

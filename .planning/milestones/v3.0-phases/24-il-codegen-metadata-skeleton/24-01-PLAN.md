---
phase: 24-il-codegen-metadata-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-compiler/src/emit/mod.rs
  - writ-compiler/src/emit/metadata.rs
  - writ-compiler/src/emit/module_builder.rs
  - writ-compiler/src/emit/heaps.rs
  - writ-compiler/src/emit/type_sig.rs
  - writ-compiler/src/emit/collect.rs
  - writ-compiler/src/emit/error.rs
  - writ-compiler/src/lib.rs
  - writ-compiler/tests/emit_tests.rs
autonomous: true
requirements:
  - EMIT-01
  - EMIT-02
  - EMIT-04
must_haves:
  truths:
    - "Every struct, entity, enum, and component DefId has a TypeDef MetadataToken assigned"
    - "Every field on a type has a FieldDef row contiguous under its parent TypeDef"
    - "Every function, method, and hook has a MethodDef row with correct flags"
    - "Every method parameter has a ParamDef row contiguous under its parent MethodDef"
    - "GenericParam and GenericConstraint rows exist for all generic type/method parameters"
    - "ModuleDef row exists with module name; ModuleRef row exists for writ-runtime"
    - "ExportDef rows exist for all pub-visible declarations"
  artifacts:
    - path: "writ-compiler/src/emit/mod.rs"
      provides: "emit() entry point function"
    - path: "writ-compiler/src/emit/metadata.rs"
      provides: "MetadataToken, TableId, all 21 row structs"
    - path: "writ-compiler/src/emit/module_builder.rs"
      provides: "ModuleBuilder with add_* and finalize methods"
    - path: "writ-compiler/src/emit/heaps.rs"
      provides: "StringHeap and BlobHeap with dedup"
    - path: "writ-compiler/src/emit/type_sig.rs"
      provides: "Ty-to-TypeRef blob encoding"
    - path: "writ-compiler/src/emit/collect.rs"
      provides: "TypedAst/DefMap to ModuleBuilder translation"
    - path: "writ-compiler/tests/emit_tests.rs"
      provides: "Integration tests for metadata emission"
  key_links:
    - from: "writ-compiler/src/emit/collect.rs"
      to: "writ-compiler/src/emit/module_builder.rs"
      via: "calls builder.add_typedef/add_fielddef/add_methoddef etc."
      pattern: "builder\\.add_"
    - from: "writ-compiler/src/emit/mod.rs"
      to: "writ-compiler/src/check/ir.rs"
      via: "takes TypedAst as input"
      pattern: "TypedAst"
---

<objective>
Create the emit module foundation and emit TypeDef, FieldDef, MethodDef, ParamDef, GenericParam, GenericConstraint, ModuleDef, ModuleRef, and ExportDef metadata rows.

Purpose: Establish the ModuleBuilder infrastructure and populate the core type-system metadata tables. This is the foundation that Plan 24-02 builds on for contract/impl/global/extern/component/locale/attribute emission.

Output: New `writ-compiler/src/emit/` module with ModuleBuilder, metadata types, heaps, type signature encoding, and collection pass for core type tables. Integration tests verifying token assignment and row ordering.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-il-codegen-metadata-skeleton/24-RESEARCH.md
@.planning/phases/24-il-codegen-metadata-skeleton/24-CONTEXT.md

@writ-compiler/src/lib.rs
@writ-compiler/src/check/ir.rs
@writ-compiler/src/check/ty.rs
@writ-compiler/src/resolve/def_map.rs
@writ-compiler/src/check/env.rs
@writ-compiler/src/ast/decl.rs
@writ-compiler/Cargo.toml
@writ-compiler/tests/typecheck_tests.rs

<interfaces>
<!-- Key types the executor needs from existing code -->

From writ-compiler/src/check/ir.rs:
```rust
pub struct TypedAst {
    pub decls: Vec<TypedDecl>,
    pub def_map: DefMap,
}

pub enum TypedDecl {
    Fn { def_id: DefId, body: TypedExpr },
    Struct { def_id: DefId },
    Entity { def_id: DefId },
    Enum { def_id: DefId },
    Contract { def_id: DefId },
    Impl { def_id: DefId, methods: Vec<(DefId, TypedExpr)> },
    Const { def_id: DefId, value: TypedExpr },
    Global { def_id: DefId, value: TypedExpr },
    Component { def_id: DefId },
    ExternFn { def_id: DefId },
    ExternStruct { def_id: DefId },
    ExternComponent { def_id: DefId },
}
```

From writ-compiler/src/resolve/def_map.rs:
```rust
pub type DefId = id_arena::Id<DefEntry>;

pub struct DefEntry {
    pub id: Option<DefId>,
    pub kind: DefKind,
    pub vis: DefVis,
    pub file_id: FileId,
    pub namespace: String,
    pub name: String,
    pub name_span: SimpleSpan,
    pub generics: Vec<String>,
    pub span: SimpleSpan,
}

pub enum DefKind {
    Fn, Struct, Entity, Enum, Contract, Impl, Component,
    ExternFn, ExternStruct, ExternComponent, Const, Global,
}
```

From writ-compiler/src/check/env.rs:
```rust
pub struct TypeEnv {
    pub fn_sigs: FxHashMap<DefId, FnSig>,
    pub struct_fields: FxHashMap<DefId, Vec<(String, Ty, SimpleSpan)>>,
    pub entity_fields: FxHashMap<DefId, Vec<(String, Ty, SimpleSpan)>>,
    pub entity_components: FxHashMap<DefId, Vec<String>>,
    pub enum_variants: FxHashMap<DefId, Vec<EnumVariantSig>>,
    pub contract_methods: FxHashMap<DefId, Vec<FnSig>>,
    pub impl_index: FxHashMap<DefId, Vec<ImplEntry>>,
    pub const_types: FxHashMap<DefId, Ty>,
    pub global_types: FxHashMap<DefId, (Ty, bool)>,
    pub component_fields: FxHashMap<DefId, Vec<(String, Ty, SimpleSpan)>>,
}
```

Spec section 2.16.4 MetadataToken encoding:
```
Bits 31-24: table ID (0-20)
Bits 23-0:  row index (1-based; 0 = null token)
```

Spec section 2.16.5 table IDs:
```
0=ModuleDef, 1=ModuleRef, 2=TypeDef, 3=TypeRef, 4=TypeSpec,
5=FieldDef, 6=FieldRef, 7=MethodDef, 8=MethodRef, 9=ParamDef,
10=ContractDef, 11=ContractMethod, 12=ImplDef, 13=GenericParam,
14=GenericConstraint, 15=GlobalDef, 16=ExternDef, 17=ComponentSlot,
18=LocaleDef, 19=ExportDef, 20=AttributeDef
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create emit module foundation with MetadataToken, TableId, row structs, heaps, and ModuleBuilder</name>
  <files>
    writ-compiler/src/emit/mod.rs
    writ-compiler/src/emit/metadata.rs
    writ-compiler/src/emit/module_builder.rs
    writ-compiler/src/emit/heaps.rs
    writ-compiler/src/emit/type_sig.rs
    writ-compiler/src/emit/error.rs
    writ-compiler/src/lib.rs
    writ-compiler/tests/emit_tests.rs
  </files>
  <behavior>
    - MetadataToken::new(TableId::TypeDef, 5) encodes to 0x02_000005
    - MetadataToken::table() and MetadataToken::row() round-trip correctly
    - MetadataToken::NULL has value 0
    - StringHeap inserts "hello" twice and returns same offset both times (dedup)
    - StringHeap offset 0 is the empty/null string
    - BlobHeap inserts identical blobs and returns same offset (dedup)
    - TypeSig encodes TyKind::Int as single byte [0x01]
    - TypeSig encodes TyKind::Struct(def_id) as [0x10, typedef_row_le_bytes]
    - TypeSig encodes TyKind::Array(TyKind::Int) as [0x20, 0x01]
    - TypeSig encodes TyKind::Func as [0x30, blob_offset_bytes]
    - ModuleBuilder::add_typedef returns a handle; after finalize, handle maps to MetadataToken
    - ModuleBuilder::finalize assigns contiguous row indices (1-based)
  </behavior>
  <action>
  1. Add `pub mod emit;` to `writ-compiler/src/lib.rs`.

  2. Create `writ-compiler/src/emit/mod.rs`:
     - Declare submodules: `pub mod metadata; pub mod module_builder; pub mod heaps; pub mod type_sig; pub mod collect; pub mod error;`
     - Public entry point: `pub fn emit(typed_ast: &TypedAst, asts: &[(FileId, &Ast)]) -> (ModuleBuilder, Vec<Diagnostic>)`
     - The emit function creates a ModuleBuilder, calls `collect::collect_defs()`, then calls `builder.finalize()`, returns `(builder, diags)`.

  3. Create `writ-compiler/src/emit/metadata.rs`:
     - `#[repr(u8)] pub enum TableId` with all 21 values (ModuleDef=0 through AttributeDef=20).
     - `#[derive(Copy, Clone, Debug, PartialEq, Eq, Hash)] pub struct MetadataToken(pub u32)` with:
       - `const NULL: MetadataToken = MetadataToken(0)`
       - `fn new(table: TableId, row: u32) -> Self` — encodes `(table as u32) << 24 | row`, debug_assert row > 0 and row <= 0x00FF_FFFF
       - `fn table(self) -> TableId` — extracts bits 31-24
       - `fn row(self) -> u32` — extracts bits 23-0
     - Row structs for all 21 tables. Each struct holds heap offsets (u32) for string/blob references and MetadataToken for cross-table references. Use u32 for heap offsets and MetadataToken for table references. Key structs:
       - `ModuleDefRow { name: u32, version: u32, flags: u32 }`
       - `ModuleRefRow { name: u32, min_version: u32 }`
       - `TypeDefRow { name: u32, namespace: u32, kind: u8, flags: u16, field_list: u32, method_list: u32 }` (kind: 0=struct, 1=enum, 2=entity, 3=component)
       - `TypeRefRow { scope: MetadataToken, name: u32, namespace: u32 }`
       - `TypeSpecRow { signature: u32 }`
       - `FieldDefRow { name: u32, type_sig: u32, flags: u16 }`
       - `FieldRefRow { parent: MetadataToken, name: u32, type_sig: u32 }`
       - `MethodDefRow { name: u32, signature: u32, flags: u16, body_offset: u32, body_size: u32, reg_count: u16 }`
       - `MethodRefRow { parent: MetadataToken, name: u32, signature: u32 }`
       - `ParamDefRow { name: u32, type_sig: u32, sequence: u16 }`
       - `ContractDefRow { name: u32, namespace: u32, method_list: u32, generic_param_list: u32 }`
       - `ContractMethodRow { name: u32, signature: u32, slot: u16 }`
       - `ImplDefRow { type_token: MetadataToken, contract_token: MetadataToken, method_list: u32 }`
       - `GenericParamRow { owner: MetadataToken, owner_kind: u8, ordinal: u16, name: u32 }`
       - `GenericConstraintRow { param_row: u32, constraint: MetadataToken }`
       - `GlobalDefRow { name: u32, type_sig: u32, flags: u16, init_value: u32 }`
       - `ExternDefRow { name: u32, signature: u32, import_name: u32, flags: u16 }`
       - `ComponentSlotRow { owner_entity: MetadataToken, component_type: MetadataToken }`
       - `LocaleDefRow { dlg_method: MetadataToken, locale: u32, loc_method: MetadataToken }`
       - `ExportDefRow { name: u32, item_kind: u8, item: MetadataToken }`
       - `AttributeDefRow { owner: MetadataToken, owner_kind: u8, name: u32, value: u32 }`
     - All row structs derive `Debug, Clone`.
     - `TypeDefKind` enum: `Struct=0, Enum=1, Entity=2, Component=3`.
     - `HookKind` enum: `None=0, Create=1, Destroy=2, Finalize=3, Serialize=4, Deserialize=5, Interact=6`.
     - MethodDef flag helpers: `pub fn method_flags(is_pub: bool, is_static: bool, is_mut_self: bool, hook: HookKind) -> u16` — pack into u16 bits.
     - FieldDef flag helpers: `pub fn field_flags(is_pub: bool, has_default: bool, is_component_field: bool) -> u16`.

  4. Create `writ-compiler/src/emit/heaps.rs`:
     - `StringHeap`: Vec<u8> internally, FxHashMap<String, u32> for dedup. `intern(&mut self, s: &str) -> u32` returns offset. Offset 0 reserved for empty string (initialize with 4 zero bytes for length prefix of empty string). Each entry: u32 byte length + UTF-8 bytes.
     - `BlobHeap`: Same pattern — Vec<u8>, FxHashMap<Vec<u8>, u32> dedup. `intern(&mut self, data: &[u8]) -> u32` returns offset.

  5. Create `writ-compiler/src/emit/type_sig.rs`:
     - `pub fn encode_type(ty: Ty, interner: &TyInterner, token_map: &impl Fn(DefId) -> MetadataToken) -> Vec<u8>`
     - Encoding per spec 2.15.3:
       - Primitives (Void/Int/Float/Bool/String): single byte 0x00-0x04
       - Struct/Entity/Enum(def_id): 0x10 + u32 TypeDef row index (from token_map)
       - GenericParam(idx): 0x12 + u16 ordinal
       - Array(inner): 0x20 + recursive encode of inner
       - Func{params, ret}: 0x30 + u32 blob_offset (the blob is: u16 param_count + TypeRef[] + return TypeRef — encode this as a separate blob and use the blob heap offset)
       - Option/Result: These are generic types in writ-runtime. For Phase 24, encode as TypeSpec references (0x11 + u32 TypeSpec row). The actual TypeSpec rows may be deferred to Phase 25 when needed. For now, Option(inner) can be encoded as a placeholder or as a TypeRef to writ-runtime.
       - TaskHandle: Same as Option/Result — writ-runtime generic type.
       - Error/Infer: should not appear in final output; debug_assert and return error tag.

  6. Create `writ-compiler/src/emit/error.rs`:
     - `EmitError` enum with variants like `UnresolvedType`, `MissingDefEntry`, etc.
     - Implement `Into<Diagnostic>` for EmitError.

  7. Create initial tests in `writ-compiler/tests/emit_tests.rs`:
     - Test MetadataToken encoding/decoding round-trip
     - Test StringHeap dedup behavior
     - Test BlobHeap dedup behavior
     - Test TypeSig encoding for primitives, named types, arrays, generics
     - Test ModuleBuilder add_typedef + finalize produces correct tokens

  8. Create `writ-compiler/src/emit/module_builder.rs`:
     - `ModuleBuilder` struct with:
       - `string_heap: StringHeap`, `blob_heap: BlobHeap`
       - Vec for each of the 21 table row types (using the row structs from metadata.rs)
       - `def_token_map: FxHashMap<DefId, MetadataToken>` — DefId to final MetadataToken
       - Internal provisional handle types (just usize indices into the Vecs)
     - Methods: `add_module_def(...)`, `add_module_ref(...)`, `add_typedef(...)`, `add_fielddef(parent_handle, ...)`, `add_methoddef(parent_handle, ...)`, `add_paramdef(method_handle, ...)`, `add_generic_param(...)`, `add_generic_constraint(...)`, `add_export_def(...)`, plus stubs for the remaining tables (add_contract_def, add_contract_method, add_impl_def, add_global_def, add_extern_def, add_component_slot, add_locale_def, add_attribute_def).
     - `finalize(&mut self)` method:
       - For TypeDef: sort by source order (or collection order), assign 1-based row indices.
       - For FieldDef: group by parent TypeDef, assign contiguous 1-based rows. Set each TypeDef's field_list to first child row index.
       - Same pattern for MethodDef (grouped by parent TypeDef, contiguous), ParamDef (grouped by parent MethodDef).
       - For GenericParam: group by owner, assign contiguous rows.
       - For all other tables: assign 1-based row indices.
       - Populate `def_token_map` with DefId -> MetadataToken mappings.
     - The finalize step must handle the list-ownership pattern: children grouped contiguously under parents, and `xxx_list` fields on parents point to the first child row.

  NOTE: body_offset, body_size, and reg_count on MethodDef rows should be set to 0 in Phase 24. Phase 25 fills these in.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler --test emit_tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    MetadataToken encodes/decodes correctly. StringHeap and BlobHeap dedup works. TypeSig encodes all primitive and named types per spec 2.15.3. ModuleBuilder.finalize() assigns contiguous 1-based row indices with list-ownership grouping. All emit_tests pass. The emit module compiles with no errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement collect pass for TypeDef/FieldDef/MethodDef/ParamDef/GenericParam/GenericConstraint/ModuleDef/ModuleRef/ExportDef</name>
  <files>
    writ-compiler/src/emit/collect.rs
    writ-compiler/src/emit/mod.rs
    writ-compiler/src/check/mod.rs
    writ-compiler/tests/emit_tests.rs
  </files>
  <behavior>
    - Given a TypedAst with a struct "Potion" with 2 fields, emit produces TypeDef row + 2 FieldDef rows
    - Given a TypedAst with an entity "Guard" with 1 property and 1 hook, emit produces TypeDef + 1 FieldDef + 1 MethodDef (with hook_kind flag)
    - Given a TypedAst with an enum "Status" with 3 variants (each with payload fields), emit produces TypeDef + FieldDef rows for each variant's fields
    - Given a TypedAst with a fn "heal" with 2 params, emit produces MethodDef + 2 ParamDef rows
    - Given a TypedAst with a generic struct "Container<T>", emit produces TypeDef + GenericParam row
    - Given a TypedAst with a generic fn "transform<T: Add>", emit produces MethodDef + GenericParam + GenericConstraint
    - ModuleDef row always present with module name from namespace
    - ModuleRef row for writ-runtime always present
    - ExportDef rows present for all pub-visible items
    - All DefIds in typed_ast.def_map have corresponding MetadataTokens after emit
  </behavior>
  <action>
  1. Create `writ-compiler/src/emit/collect.rs`:
     - `pub fn collect_defs(typed_ast: &TypedAst, asts: &[(FileId, &Ast)], builder: &mut ModuleBuilder, diags: &mut Vec<Diagnostic>)`
     - This function needs both the TypedAst (for DefMap, TypedDecl list) AND the original ASTs (for field/param/hook details that TypedDecl doesn't carry — just like TypeEnv::build does).
     - The collect pass must rebuild type information similar to how `check/env.rs` does it, but calling ModuleBuilder methods instead of populating TypeEnv maps.

     Step-by-step collection:
     a. **ModuleDef**: Always emit exactly 1 row. Module name = first namespace found in DefMap, or "main" if none. Version = "0.1.0".
     b. **ModuleRef**: Always emit 1 row for "writ-runtime" with min_version "1.0.0".
     c. **Walk TypedDecl list** and for each:
        - `TypedDecl::Struct { def_id }`: Look up DefEntry. Find matching AST struct decl (reuse the find_struct_decl pattern from env.rs). Emit TypeDef(kind=Struct). For each field in AST: emit FieldDef with type_sig from encode_type. Track DefId -> typedef handle. For any `on` hooks in struct members: emit MethodDef with appropriate hook_kind flags.
        - `TypedDecl::Entity { def_id }`: Same but kind=Entity. Properties -> FieldDef. Hooks -> MethodDef with hook_kind. Entity hooks map: "create"->Create, "destroy"->Destroy, "finalize"->Finalize, "serialize"->Serialize, "deserialize"->Deserialize, "interact"->Interact.
        - `TypedDecl::Enum { def_id }`: kind=Enum. Variants are handled as: the enum is one TypeDef, variant payload fields become FieldDef rows under it (each variant's fields, in variant order).
        - `TypedDecl::Fn { def_id, .. }`: Top-level function. Emit MethodDef (standalone, not under a TypeDef — use a synthetic "module-level" parent or just emit directly). Emit ParamDef for each parameter.
        - `TypedDecl::Contract { def_id }`: SKIP in this task — handled by Plan 24-02.
        - `TypedDecl::Impl { def_id, methods }`: For each method (def_id, _body), emit MethodDef under the target type's TypeDef + ParamDefs. ImplDef row itself is deferred to Plan 24-02.
        - `TypedDecl::Component { def_id }`: Emit TypeDef(kind=Component). Fields -> FieldDef.
        - `TypedDecl::ExternFn { def_id }`: SKIP — handled by Plan 24-02 as ExternDef.
        - `TypedDecl::ExternStruct { def_id }`: Emit TypeDef(kind=Struct). Fields -> FieldDef.
        - `TypedDecl::ExternComponent { def_id }`: Emit TypeDef(kind=Component). Fields -> FieldDef.
        - `TypedDecl::Const { def_id, .. }`: SKIP — handled by Plan 24-02 as GlobalDef.
        - `TypedDecl::Global { def_id, .. }`: SKIP — handled by Plan 24-02 as GlobalDef.

     d. **GenericParam**: For each TypeDef and MethodDef that has generics (from DefEntry.generics), emit GenericParam rows with owner token, ordinal, and name.
     e. **GenericConstraint**: For generic params with bounds (from AST generic param bounds), emit GenericConstraint rows linking to the GenericParam row and the constraint contract's token.
     f. **ExportDef**: After all TypeDef/MethodDef emission, walk DefMap.by_fqn. For each pub DefId that has a MetadataToken, emit ExportDef with item_kind (0=type, 1=method, 2=field) and item token.
     g. **Register DefId -> handle mappings** in ModuleBuilder during collection so finalize() can populate def_token_map.

  2. Modify `writ-compiler/src/emit/mod.rs`:
     - The `emit()` function needs access to the TyInterner. Update to accept it (or reconstruct from TypedAst). Since TypedAst doesn't carry the interner, the `emit()` signature should accept the interner: `pub fn emit(typed_ast: &TypedAst, asts: &[(FileId, &Ast)], interner: &TyInterner) -> (ModuleBuilder, Vec<Diagnostic>)`.
     - Alternatively, store the interner in the TypedAst. The cleaner approach for Phase 24 is to pass it as a parameter since the interner is needed for type_sig encoding.

  3. Update `writ-compiler/src/check/mod.rs`:
     - The `typecheck()` function currently returns `(TypedAst, Vec<Diagnostic>)` and the TyInterner is local. Either:
       (a) Add `pub interner: TyInterner` to TypedAst, or
       (b) Change typecheck to return `(TypedAst, TyInterner, Vec<Diagnostic>)`.
     - Option (b) is less invasive: `pub fn typecheck(...) -> (TypedAst, TyInterner, Vec<Diagnostic>)`. Move `ctx.interner` into the return tuple. This requires updating existing test code.

  4. Add integration tests in `writ-compiler/tests/emit_tests.rs`:
     - Helper: `emit_src(src: &str) -> ModuleBuilder` — parse, lower, resolve, typecheck, emit.
     - Test struct emission: source with `pub struct Potion { potency: int, name: string }` -> verify 1 TypeDef, 2 FieldDefs, field_list contiguous.
     - Test entity emission: source with entity + hooks -> verify TypeDef(kind=Entity) + MethodDef rows with hook_kind flags set.
     - Test enum emission: source with enum and variants -> verify TypeDef(kind=Enum) + FieldDef rows for variant payloads.
     - Test function emission: source with `pub fn heal(target: int, amount: int) -> int { ... }` -> verify MethodDef + 2 ParamDefs.
     - Test generic emission: source with `pub struct Container<T> { item: T }` -> verify TypeDef + GenericParam row.
     - Test ModuleDef/ModuleRef always present.
     - Test ExportDef rows present for pub items.
     - Test def_token_map completeness: every DefId in DefMap has a MetadataToken.

  NOTE: For type signature encoding during collection, use a closure or method that looks up DefId -> MetadataToken from the builder's provisional handles. Since tokens aren't finalized until finalize(), type_sig encoding may need to use provisional handle indices. One approach: encode type sigs during finalize() after row indices are known, or use a two-step approach where type sigs are encoded with placeholder tokens then fixed up. The simplest approach: encode type sigs as part of finalize() — the collect pass stores Ty values, and finalize() encodes them into blob heap entries using the now-known token map.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler --test emit_tests -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>
    collect_defs walks TypedAst and populates ModuleBuilder with TypeDef, FieldDef, MethodDef, ParamDef, GenericParam, GenericConstraint, ModuleDef, ModuleRef, and ExportDef rows. Entity hooks produce MethodDef rows with correct hook_kind flags. List-ownership pattern is satisfied (children contiguous under parents). All DefIds from the DefMap have MetadataTokens after finalize(). The existing typecheck tests still pass (no regressions from interner signature change). All new emit_tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p writ-compiler` — all existing tests pass (no regressions from adding emit module or changing typecheck return signature)
2. `cargo test -p writ-compiler --test emit_tests` — all new emit tests pass
3. `cargo check -p writ-compiler` — no compilation errors or warnings
4. Manual inspection: emit module follows same code organization pattern as resolve/ and check/ modules
</verification>

<success_criteria>
- writ-compiler/src/emit/ module exists with 6 submodules
- MetadataToken correctly encodes table ID + row index per spec 2.16.4
- StringHeap and BlobHeap provide deduplication
- TypeSig encodes types per spec 2.15.3
- ModuleBuilder collects all core type-system rows from TypedAst
- finalize() assigns contiguous 1-based row indices respecting list-ownership
- Entity/struct hooks produce MethodDef rows with hook_kind flags
- ModuleDef and ModuleRef for writ-runtime always present
- ExportDef rows emitted for all pub-visible declarations
- All DefIds have MetadataTokens after emit
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/24-il-codegen-metadata-skeleton/24-01-SUMMARY.md`
</output>

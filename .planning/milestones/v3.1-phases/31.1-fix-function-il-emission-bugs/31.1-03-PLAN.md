---
phase: 31.1-fix-function-il-emission-bugs
plan: 03
type: execute
wave: 2
depends_on:
  - 31.1-01
  - 31.1-02
files_modified:
  - writ-golden/tests/golden/fn_basic_call.expected
  - writ-golden/tests/golden/fn_typed_params.expected
  - writ-golden/tests/golden/fn_recursion.expected
autonomous: false
requirements:
  - BUG-07
  - BUG-08
  - BUG-09
  - BUG-10
  - BUG-11
must_haves:
  truths:
    - All three function golden tests pass with cargo test -p writ-golden
    - The re-blessed .expected files are hand-validated by the user as spec-correct IL
    - CALL_INDIRECT does not appear for direct function calls in any .expected file
    - All branch offsets in fn_recursion.expected are non-zero computed values
    - Method signatures in fn_typed_params.expected show correct param and return types
  artifacts:
    - path: writ-golden/tests/golden/fn_basic_call.expected
      provides: Re-blessed IL with CALL for direct function calls
    - path: writ-golden/tests/golden/fn_typed_params.expected
      provides: Re-blessed IL with correct signature types and correct RET register
    - path: writ-golden/tests/golden/fn_recursion.expected
      provides: Re-blessed IL with non-zero branch offsets and correct MOV/RET registers
  key_links:
    - from: writ-golden/tests/golden_tests.rs
      to: writ-golden/tests/golden/fn_basic_call.expected
      via: run_golden_test compiles .writ and diffs against .expected
      pattern: "run_golden_test.*fn_basic_call"
---

<objective>
Re-bless the three function golden test fixtures with the corrected compiler output, then present the re-blessed IL to the user for human validation against the spec.

Purpose: The five bug fixes in Plans 01 and 02 change what the compiler emits. The .expected files locked during Phase 31 contain the buggy output. This plan regenerates them with correct IL and gets human sign-off that the output is spec-correct before locking them as regression anchors.
Output: Three updated .expected files containing spec-correct IL, locked after human approval.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-fix-function-il-emission-bugs/31.1-CONTEXT.md
@.planning/phases/31.1-fix-function-il-emission-bugs/31.1-01-SUMMARY.md
@.planning/phases/31.1-fix-function-il-emission-bugs/31.1-02-SUMMARY.md

<interfaces>
<!-- Golden test infrastructure from Phase 31. -->

writ-golden/tests/golden_tests.rs:
- run_golden_test(name) compiles writ-golden/tests/golden/{name}.writ, disassembles, diffs against {name}.expected
- BLESS=1 cargo test -p writ-golden regenerates all .expected files from compiler output
- Three test functions exist: fn_basic_call, fn_typed_params, fn_recursion

.writ fixture sources (do NOT modify):
- writ-golden/tests/golden/fn_basic_call.writ
- writ-golden/tests/golden/fn_typed_params.writ
- writ-golden/tests/golden/fn_recursion.writ

What the old (buggy) .expected files contained:
- fn_basic_call.expected: CALL_INDIRECT r1, r0, r2, 0 and .reg r0 ? (spurious delegate reg)
- fn_typed_params.expected: .method "is_positive" (bool) -> int (swapped) and RET r3 (void)
- fn_recursion.expected: MOV r4, r6 (void src), RET r15 (void), BR_FALSE r3, 0, BR 0

What the new (fixed) .expected files should contain:
- fn_basic_call.expected: CALL r_dst, method_idx, r_base, argc (direct call instruction)
- fn_typed_params.expected: .method "is_positive" (int) -> bool and RET r_N (typed register)
- fn_recursion.expected: MOV r_dst, r_typed_src, RET r_typed, BR_FALSE r, non-zero, BR non-zero

Spec byte sizes for manual offset verification:
  BR = 8B, BR_FALSE = 8B, BR_TRUE = 8B
  CALL = 12B, CALL_INDIRECT = 10B
  MOV = 6B, RET = 4B, RET_VOID = 2B
  LOAD_INT = 10B, LOAD_FLOAT = 10B, LOAD_TRUE = 4B, LOAD_FALSE = 4B
  ADD_I = 8B, MUL_I = 8B, SUB_I = 8B
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-bless all three golden test fixtures with fixed compiler output</name>
  <files>writ-golden/tests/golden/fn_basic_call.expected, writ-golden/tests/golden/fn_typed_params.expected, writ-golden/tests/golden/fn_recursion.expected</files>
  <action>
Run the bless command to regenerate all three .expected files from the fixed compiler output:

```bash
BLESS=1 cargo test -p writ-golden 2>&1
```

This overwrites the three .expected files with the compiler's current output. If the bless command panics or errors, read the error message carefully and diagnose:
- A compilation error means a bug fix in Plan 01 or 02 introduced a regression — check cargo build -p writ-compiler first
- A panic in the test harness means the golden test infrastructure itself has an issue — check golden_tests.rs

After BLESS=1 completes successfully, run the golden tests without BLESS to confirm they pass:

```bash
cargo test -p writ-golden 2>&1
```

All three tests must pass. Then read and print the full contents of all three .expected files so the human reviewer can inspect them in the next task:

```bash
cat writ-golden/tests/golden/fn_basic_call.expected
cat writ-golden/tests/golden/fn_typed_params.expected
cat writ-golden/tests/golden/fn_recursion.expected
```
  </action>
  <verify>
    <automated>cargo test -p writ-golden 2>&1</automated>
  </verify>
  <done>cargo test -p writ-golden passes with 0 failures. All three .expected files have been regenerated (git diff shows changed content from the buggy versions). File contents are displayed for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-validate re-blessed golden IL output against spec</name>
  <what-built>
Five IL emission bugs fixed (BUG-07 through BUG-11) and three golden test fixtures re-blessed with corrected compiler output. The .expected files now contain different IL from Phase 31's blessed output.
  </what-built>
  <how-to-verify>
Review the re-blessed .expected IL files printed above. For each file, confirm:

**fn_basic_call.expected — verify BUG-07 fix:**
- Every call to a named function uses CALL (not CALL_INDIRECT)
- No .reg r? with type "?" (spurious delegate register is gone)

**fn_typed_params.expected — verify BUG-08 and BUG-09 fixes:**
- .method "is_positive" shows (int) -> bool (params first, return last — not swapped)
- RET instruction references a register declared with type int or bool (not void)

**fn_recursion.expected — verify BUG-10 and BUG-11 fixes:**
- MOV instructions in if/else branches use a typed (non-void) register as source
- RET instruction references the register holding the actual recursion result (not void)
- BR_FALSE and BR instructions have non-zero i32 offsets
- Optional spot-check: pick one branch offset and manually verify it equals the byte distance from the branch instruction to its target, using these instruction sizes: BR=8B, BR_FALSE=8B, CALL=12B, MOV=6B, RET=4B, LOAD_INT=10B, ADD_I=8B, MUL_I=8B, SUB_I=8B

Type "approved" if all five bugs are confirmed fixed, or describe any remaining issues.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the re-blessed .expected files as the new regression anchors, or describe any issues found.</resume-signal>
  <files>writ-golden/tests/golden/fn_basic_call.expected, writ-golden/tests/golden/fn_typed_params.expected, writ-golden/tests/golden/fn_recursion.expected</files>
  <action>Human review: inspect the re-blessed .expected files and confirm IL is spec-correct per the five bug fix criteria above.</action>
  <verify>
    <automated>cargo test -p writ-golden 2>  <resume-signal>Type "approved" to lock the re-blessed .expected files as the new regression anchors, or describe any issues found.</resume-signal>1</automated>
  </verify>
  <done>User types "approved" confirming all five bugs are fixed in the re-blessed .expected files.</done>
</task>

</tasks>

<verification>
Phase 31.1 complete when:
1. cargo test -p writ-golden passes — all three function golden tests pass
2. User has approved the re-blessed IL as spec-correct
3. The three .expected files are committed to git with the corrected content
</verification>

<success_criteria>
- cargo test -p writ-golden passes with 0 failures after re-blessing
- User approves: fn_basic_call uses CALL (not CALL_INDIRECT), fn_typed_params has correct sig types and RET register, fn_recursion has correct MOV/RET registers and non-zero branch offsets
- Re-blessed .expected files committed to git as the new regression anchors for Phase 32+
</success_criteria>

<output>
After completion, create `.planning/phases/31.1-fix-function-il-emission-bugs/31.1-03-SUMMARY.md`
</output>

---
phase: 40-spec-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/27_26_standard_library_builtins.md
  - language-spec/spec/14_13_dialogue_blocks_dlg.md
  - language-spec/spec/29_28_lowering_reference.md
autonomous: true
requirements:
  - SPEC-02
must_haves:
  truths:
    - "A spec section explicitly states that log, say, and choice are root-namespace inbuilt calls with no qualifier needed"
    - "Section 13.9 no longer says these functions live in the Runtime namespace or are not callable directly"
    - "Section 28.5 no longer says the runtime must provide these core functions in the Runtime namespace"
    - "The three spec locations now consistently describe log/say/choice as root-namespace inbuilts"
  artifacts:
    - path: "language-spec/spec/27_26_standard_library_builtins.md"
      provides: "New section 26.4 Root-Namespace Inbuilt Calls"
      contains: "26.4 Root-Namespace Inbuilt Calls"
    - path: "language-spec/spec/14_13_dialogue_blocks_dlg.md"
      provides: "Updated section 13.9 without Runtime namespace wording"
      contains: "root-namespace inbuilt calls"
    - path: "language-spec/spec/29_28_lowering_reference.md"
      provides: "Updated section 28.5 without Runtime namespace wording"
      contains: "root-namespace inbuilts"
  key_links:
    - from: "language-spec/spec/27_26_standard_library_builtins.md"
      to: "language-spec/spec/14_13_dialogue_blocks_dlg.md"
      via: "cross-reference from section 13.9 to section 26.4"
      pattern: "26.4"
---

<objective>
Add §26.4 Root-Namespace Inbuilt Calls to the standard library builtins spec section, then update §13.9 and §28.5 to remove contradictory "Runtime namespace" wording.

Purpose: The spec currently says log/say/choice live in a "Runtime namespace" and are "not callable directly" — this contradicts every code example and the actual compiler behavior. The fix makes the spec consistent: log/say/choice are root-namespace inbuilt calls, no qualifier needed.

Output: Three spec files updated; no code changes.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current spec content in the three files that must be changed. -->

=== language-spec/spec/27_26_standard_library_builtins.md (current end) ===
### 26.3 Standard Library Types
| Type            | Description |
|-----------------|-------------|
| `List<T>`       | ...         |
| `Map<K, V>`     | ...         |
| `Set<T>`        | ...         |
| `EntityList<T>` | ...         |
---   <- file ends with this horizontal rule

New section 26.4 is inserted immediately BEFORE the trailing `---`.

=== language-spec/spec/14_13_dialogue_blocks_dlg.md section 13.9 (current — WRONG) ===
### 13.9 Dialogue Suspension

Dialogue operations are **transition points** — the runtime suspends execution and yields control to the host engine.
The core dialogue functions live in the `Runtime` namespace and are provided by the runtime, not the script:

- `Runtime.say(speaker, text)` — Display a line of dialogue. Suspends until the host signals the player has advanced.
- `Runtime.say_localized(speaker, key, fallback)` — Localized variant. Same suspension semantics.
- `Runtime.choice(options)` — Present choices to the player. Suspends until the host signals which option was selected.

These functions are not callable directly from user code under the `Runtime` prefix — the compiler lowers dialogue
syntax (`@Speaker text`, `$ choice { ... }`) into calls to these functions automatically. See §28.5 for the full
lowering.

The host is responsible for presenting the dialogue UI...

=== language-spec/spec/29_28_lowering_reference.md section 28.5 (current — WRONG) ===
### 28.5 Runtime Functions

The runtime must provide these core functions in the `Runtime` namespace. Dialogue functions are **transition points** —
they suspend execution until the host responds (see §13.9, IL spec §1.14.2).

| Function             | Signature | Behavior |
...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add §26.4 and fix §13.9 and §28.5</name>
  <files>language-spec/spec/27_26_standard_library_builtins.md, language-spec/spec/14_13_dialogue_blocks_dlg.md, language-spec/spec/29_28_lowering_reference.md</files>
  <action>
**Change 1 — Add section 26.4 to language-spec/spec/27_26_standard_library_builtins.md:**

Append the following section immediately before the trailing `---` line at the end of the file (after the 26.3 table):

```markdown
### 26.4 Root-Namespace Inbuilt Calls

Three functions are always available in the root namespace without any qualifier:

| Function | Signature | Purpose |
|----------|-----------|---------|
| `log`    | `fn log(msg: string)` | Write a message to the host's debug log |
| `say`    | `fn say(speaker: Entity, text: string)` | Display dialogue (transition point — suspends) |
| `choice` | `fn choice(options: ...) -> int` | Present choices (transition point — suspends) |

These are **inbuilt calls** — the compiler resolves them from the root namespace. They are callable as
`log(msg)`, `say(speaker, text)`, and `choice(options)`. No `writ::`, `Runtime::`, or any other
qualifier is needed or accepted.

`say` and `choice` are dialogue transition points — the VM suspends until the host responds (§13.9).
`log` is a fire-and-forget debug output call; it does not suspend.

The compiler lowers `dlg` syntax (`@Speaker text`, `$ choice { ... }`) into calls to `say` and
`choice` automatically — user code in `dlg` blocks does not call them directly. In `fn` bodies,
`log` may be called freely.
```

**Change 2 — Rewrite section 13.9 in language-spec/spec/14_13_dialogue_blocks_dlg.md:**

Replace the entire section 13.9 Dialogue Suspension block (from `### 13.9 Dialogue Suspension` through the paragraph ending with `...the suspend-and-confirm model (see IL spec §1.14.2).`) with:

```markdown
### 13.9 Dialogue Suspension

Dialogue operations are **transition points** — the runtime suspends execution and yields control to the host engine.
The core dialogue functions are root-namespace inbuilt calls (§26.4) provided by the runtime:

- `say(speaker, text)` — Display a line of dialogue. Suspends until the host signals the player has advanced.
- `say_localized(speaker, key, fallback)` — Localized variant. Same suspension semantics.
- `choice(options)` — Present choices to the player. Suspends until the host signals which option was selected.

The compiler lowers dialogue syntax (`@Speaker text`, `$ choice { ... }`) into calls to these functions
automatically — user code in `dlg` blocks does not call them directly. In `fn` bodies, `log` may be
called freely without qualification. See §28.5 for the full lowering and §26.4 for the inbuilt call contract.

The host is responsible for presenting the dialogue UI, advancing text, and returning choice selections. The runtime
does not prescribe how the host implements these — only that the runtime suspends until the host responds. This follows
the suspend-and-confirm model (see IL spec §1.14.2).
```

**Change 3 — Rewrite the section 28.5 preamble in language-spec/spec/29_28_lowering_reference.md:**

Replace only the opening paragraph of section 28.5 (the two sentences before the table: "The runtime must provide..." through "...IL spec §1.14.2).") with:

```markdown
The runtime must provide these core functions as root-namespace inbuilts (§26.4). Dialogue functions are
**transition points** — they suspend execution until the host responds (see §13.9, IL spec §1.14.2).
```

The `### 28.5 Runtime Functions` heading and the table that follows are kept unchanged.

**Consistency check:** After all three changes, search for occurrences of "Runtime namespace" in all spec files under language-spec/spec/ — there should be zero remaining occurrences that refer to log/say/choice. The `writ-runtime` module name (used in IL spec sections about the binary format) is unrelated and must not be changed.
  </action>
  <verify>
    <automated>MISSING — markdown-only changes; no automated test. Verify manually: (1) language-spec/spec/27_26_standard_library_builtins.md contains "### 26.4 Root-Namespace Inbuilt Calls", (2) language-spec/spec/14_13_dialogue_blocks_dlg.md section 13.9 no longer contains "Runtime namespace" or "not callable directly", (3) language-spec/spec/29_28_lowering_reference.md section 28.5 preamble no longer contains "Runtime namespace".</automated>
  </verify>
  <done>Section 26.4 exists in 27_26_standard_library_builtins.md with a table of log/say/choice. Section 13.9 refers to "root-namespace inbuilt calls (§26.4)" instead of "Runtime namespace". Section 28.5 preamble says "root-namespace inbuilts (§26.4)" instead of "Runtime namespace". All three sections are internally consistent.</done>
</task>

</tasks>

<verification>
- language-spec/spec/27_26_standard_library_builtins.md contains "### 26.4 Root-Namespace Inbuilt Calls"
- language-spec/spec/14_13_dialogue_blocks_dlg.md contains "root-namespace inbuilt calls (§26.4)" in section 13.9 and does NOT contain "Runtime namespace" or "not callable directly"
- language-spec/spec/29_28_lowering_reference.md section 28.5 does NOT contain "Runtime namespace" in its opening paragraph
- Cross-reference to section 26.4 appears in both section 13.9 and section 28.5 preamble
</verification>

<success_criteria>
SPEC-02 is met: A spec section explicitly states that log, say, and choice are root-namespace inbuilt calls — callable as log(msg), say(speaker, text), choice(options) without any qualifier. Sections 13.9 and 28.5 no longer contradict this.
</success_criteria>

<output>
After completion, create `.planning/phases/40-spec-cleanup/40-02-SUMMARY.md` summarizing:
- New section 26.4 content added
- Changes made to section 13.9 (what was removed, what replaced it)
- Changes made to section 28.5 (what was removed, what replaced it)
</output>

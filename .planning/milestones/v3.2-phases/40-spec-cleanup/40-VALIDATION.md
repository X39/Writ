---
phase: 40
slug: spec-cleanup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-06
---

# Phase 40 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` (built-in) |
| **Config file** | `Cargo.toml` per crate |
| **Quick run command** | `cargo test -p writ-compiler config` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p writ-compiler config`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 40-01-01 | 01 | 1 | SPEC-01 | manual | manual inspection of spec files | N/A | ⬜ pending |
| 40-02-01 | 02 | 1 | SPEC-02 | manual | manual inspection of spec section | N/A | ⬜ pending |
| 40-03-01 | 03 | 1 | SPEC-03 | unit | `cargo test -p writ-compiler config` | ✅ exists (needs update) | ⬜ pending |
| 40-03-02 | 03 | 1 | SPEC-03 | unit | `cargo test -p writ-compiler config::tests::scaffold_toml_round_trips` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `compiler/src/config.rs` tests updated — update `parse_basic_config` TOML string from `default_locale`/`locales` keys to `default`/`supported`
- [ ] `config::tests::locale_without_supported` — new test confirming absent `supported` field deserializes to empty `Vec`
- [ ] `config::tests::scaffold_toml_round_trips` — parse the exact TOML generated by `cmd_new` through `load_config` and assert no error

*SPEC-01 and SPEC-02 are markdown-only changes with no automated verification.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| §2.8 file deleted from spec | SPEC-01 | Markdown file deletion — no automated test | Verify `language-spec/spec/37_2_8_serialization_critical_sections_removed.md` does not exist; verify TOC entry removed |
| §26.4 (or equivalent) section exists with correct inbuilt prose | SPEC-02 | Spec prose correctness — semantic review | Check new spec section states `log`, `say`, `choice` are root-namespace calls; verify §13.9 and §28.5 no longer contradict |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

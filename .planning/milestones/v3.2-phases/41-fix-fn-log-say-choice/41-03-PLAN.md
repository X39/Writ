---
phase: 41-fix-fn-log-say-choice
plan: 03
type: execute
wave: 2
depends_on:
  - 41-01
  - 41-02
files_modified:
  - writ-golden/tests/golden/fn_log_say_choice.writil
  - .planning/phases/41-fix-fn-log-say-choice/41-NOTES.md
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "cargo test -p writ-golden passes — all tests green including test_fn_log_say_choice"
    - "fn_log_say_choice.writil contains non-empty method bodies with IL instructions for log, say, and choice calls"
    - "fn_log_say_choice.writil is UTF-8 encoded with no BOM (first bytes are NOT FF FE)"
    - "41-NOTES.md documents the root cause chain and the fix applied"
  artifacts:
    - path: "writ-golden/tests/golden/fn_log_say_choice.writil"
      provides: "Blessed golden snapshot with correct IL for BUG-01 fix"
      contains: "CALL_EXTERN"
    - path: ".planning/phases/41-fix-fn-log-say-choice/41-NOTES.md"
      provides: "Root cause documentation for BUG-01"
  key_links:
    - from: "fn_log_say_choice.writil"
      to: "test_fn_log_say_choice"
      via: "run_golden_test reads .writil and compares byte-for-byte"
      pattern: "test_fn_log_say_choice"
    - from: "BLESS=1 cargo test"
      to: "fn_log_say_choice.writil"
      via: "bless_golden writes actual disassembly to .writil"
      pattern: "bless_golden"
---

<objective>
Bless the fn_log_say_choice golden snapshot with the correct IL output (after Plans 01+02 fix the harness and codegen), confirm the full golden suite passes, verify UTF-8 encoding, and document the root cause in 41-NOTES.md.

Purpose: This is the acceptance and documentation step — it proves the fix is complete and locks in the regression anchor.
Output: Blessed fn_log_say_choice.writil with correct non-empty IL; 41-NOTES.md root cause record.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-fix-fn-log-say-choice/41-CONTEXT.md
@.planning/phases/41-fix-fn-log-say-choice/41-RESEARCH.md
@.planning/phases/41-fix-fn-log-say-choice/41-01-SUMMARY.md
@.planning/phases/41-fix-fn-log-say-choice/41-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bless fn_log_say_choice.writil and verify full golden suite</name>
  <files>writ-golden/tests/golden/fn_log_say_choice.writil</files>
  <action>
    1. Run the bless command to overwrite the placeholder .writil with the correct IL output:
       `BLESS=1 cargo test -p writ-golden -- test_fn_log_say_choice`
       This will run compile_and_disassemble on fn_log_say_choice.writ, serialize the module,
       round-trip through Module::from_bytes, disassemble, and write the result to
       writ-golden/tests/golden/fn_log_say_choice.writil via bless_golden.

    2. After blessing, run the full golden suite to confirm all tests pass:
       `cargo test -p writ-golden`

    3. Verify the blessed .writil file is UTF-8 (no BOM):
       Read the first 4 bytes of fn_log_say_choice.writil. Confirm:
       - First two bytes are NOT [0xFF, 0xFE] (no UTF-16 LE BOM)
       - First byte is NOT 0xEF (no UTF-8 BOM)
       The file should start with a `.` or `;` character (disassembly directives start with `.module`).

    4. Verify the blessed file contains non-empty method bodies:
       Read fn_log_say_choice.writil and confirm it contains at least one `CALL_EXTERN` instruction
       and that the `main` method body has more than zero instructions.

    CONSTRAINT: Do NOT edit fn_log_say_choice.writil manually — only BLESS=1 may write it.
    CONSTRAINT: If BLESS=1 produces an error (compile failure), stop and report the error rather
    than overwriting the file with garbage.
  </action>
  <verify>
    <automated>cargo test -p writ-golden</automated>
  </verify>
  <done>cargo test -p writ-golden passes with all tests green. fn_log_say_choice.writil contains non-empty method bodies. File is UTF-8, no BOM.</done>
</task>

<task type="auto">
  <name>Task 2: Write 41-NOTES.md documenting root cause and fix</name>
  <files>.planning/phases/41-fix-fn-log-say-choice/41-NOTES.md</files>
  <action>
    Create .planning/phases/41-fix-fn-log-say-choice/41-NOTES.md documenting:

    1. **Root cause chain** (BUG-01): Summarize the 5-step chain:
       - lower/expr.rs encodes `::log` as `AstExpr::Path { segments: ["::log"] }` (prepends "::" to first segment)
       - check_path does `segments.join("::")` → `"::log"` which is not in DefMap (registered as `"log"`)
       - check_path falls through with `TypedExpr::Path { ty: error_ty }` and NO diagnostic emitted
       - check_call sees error-typed callee, returns `TypedExpr::Call { ty: error_ty, callee_def_id: None }`
       - has_error_nodes treats TypedExpr::Path as a leaf (no error detected) → codegen proceeds
       - Codegen for error-typed call with None callee_def_id produces zero instructions

    2. **Fix applied**: check_path in check_expr.rs now strips the leading "::" from the first
       segment before DefMap lookup — normalizing "::log" → "log" at the canonical resolution point.

    3. **Ancillary fixes**:
       - fn_log_say_choice.writ: UTF-8 BOM removed (was EF BB BF, now clean UTF-8)
       - fn_log_say_choice.writil: re-blessed with correct IL (was UTF-16 LE with empty bodies)
       - bless_golden: fixed extension mismatch (.expected → .writil)
       - run_golden_test: BOM-strip added to expected-file read path

    4. **Scope boundary**: The check_path normalization intentionally does NOT emit a diagnostic
       for unresolved paths. The existing fall-through behavior (silent error type) is unchanged for
       cases that legitimately fall through (e.g. enum variants — noted in the existing code comment).
       A future phase may add proper diagnostics for unresolved paths.

    Keep the notes concise (under 100 lines). Use the same markdown style as other .planning docs.
  </action>
  <verify>
    <automated>cargo test -p writ-golden</automated>
  </verify>
  <done>41-NOTES.md exists and documents the root cause, fix, and ancillary changes. Full golden suite still passes.</done>
</task>

</tasks>

<verification>
- `cargo test -p writ-golden` — all tests green (including test_fn_log_say_choice)
- fn_log_say_choice.writil: first bytes confirm UTF-8, no BOM
- fn_log_say_choice.writil: contains CALL_EXTERN instructions (non-empty method bodies)
- 41-NOTES.md exists with root cause documentation
</verification>

<success_criteria>
- All four Phase 41 success criteria from ROADMAP.md are satisfied:
  1. cargo test -p writ-golden passes with non-empty method bodies
  2. Root cause documented in 41-NOTES.md
  3. Blessed .writil is UTF-8, no BOM
  4. Round-trip through Module::from_bytes in compile_and_disassemble satisfies the disasm fidelity check
</success_criteria>

<output>
After completion, create `.planning/phases/41-fix-fn-log-say-choice/41-03-SUMMARY.md`
</output>

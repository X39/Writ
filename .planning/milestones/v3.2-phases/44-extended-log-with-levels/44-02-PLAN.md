---
phase: 44-extended-log-with-levels
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - writ-cli/src/cli_host.rs
  - writ-golden/tests/golden/fn_log_say_choice.writ
  - writ-golden/tests/golden/fn_log_say_choice.writil
  - writ-cli/tests/fixtures/hello.writ
  - writ-parser/tests/cases/07_functions.writ
  - writ-parser/tests/cases/09_entities.writ
  - writ-parser/tests/cases/11_error_handling.writ
  - writ-parser/tests/cases/14_attributes.writ
  - writ-parser/tests/cases/15_ranges_indexing.writ
  - writ-parser/tests/cases/16_generics.writ
  - writ-parser/tests/cases/17_globals_atomic.writ
  - writ-parser/tests/cases/18_extern.writ
  - writ-parser/tests/cases/20_comprehensive.writ
  - language-spec/spec/27_26_standard_library_builtins.md
autonomous: true
requirements: [TOOL-03]

must_haves:
  truths:
    - "CliHost prints '[DEBUG] msg' (uppercase prefix) when log::debug is called"
    - "CliHost routes log::trace/debug/info/warn/error to on_log with correct LogLevel variant"
    - "All golden tests pass after fixture update and re-bless"
    - "All parser tests pass after log(msg) to log::info(msg) migration"
    - "The spec documents the leveled log:: namespace as the sole logging interface"
    - "hello.writ CLI fixture compiles with the new log::info syntax"
  artifacts:
    - path: "writ-cli/src/cli_host.rs"
      provides: "log::level name matching in on_request + UPPERCASE on_log format"
      contains: "log::trace"
    - path: "writ-golden/tests/golden/fn_log_say_choice.writ"
      provides: "Updated fixture using log::info instead of log"
      contains: "log::info"
    - path: "writ-cli/tests/fixtures/hello.writ"
      provides: "Updated CLI fixture using log::info"
      contains: "log::info"
    - path: "language-spec/spec/27_26_standard_library_builtins.md"
      provides: "Updated spec with log:: namespace documentation"
      contains: "log::trace"
  key_links:
    - from: "writ-cli/src/cli_host.rs"
      to: "writ-runtime/src/host.rs"
      via: "on_request matches extern name, calls on_log(LogLevel::X, msg)"
      pattern: "on_log.*LogLevel"
    - from: "writ-golden/tests/golden/fn_log_say_choice.writ"
      to: "writ-golden/tests/golden/fn_log_say_choice.writil"
      via: "BLESS=1 re-blesses the snapshot from the compiled fixture"
      pattern: "CALL_EXTERN"
---

<objective>
Update CliHost dispatch for leveled log names, migrate all fixtures from `log(msg)` to `log::info(msg)`, re-bless golden snapshots, and update the language spec.

Purpose: Plan 01 made `log::level(msg)` compile correctly. This plan connects the runtime dispatch so CliHost actually prints log messages with level prefixes, updates all test fixtures to use the new API, and documents the change in the spec. Without this, the compiled calls would hit the unknown-extern fallback and existing tests would fail.

Output: Full end-to-end logging works: Writ source `log::info("hello")` compiles to CALL_EXTERN, runtime dispatches to `on_log(Info, "hello")`, CliHost prints `[INFO] hello`. All tests pass. Spec is updated.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-extended-log-with-levels/44-CONTEXT.md
@.planning/phases/44-extended-log-with-levels/44-RESEARCH.md
@.planning/phases/44-extended-log-with-levels/44-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 output. -->

From writ-cli/src/cli_host.rs (existing on_request dispatch):
```rust
HostRequest::ExternCall { extern_idx, args, display_args, .. } => {
    let name = self.resolve_extern_name(*extern_idx);
    // match on name for "say", "choice", and unknown extern fallback
    // NEW: must also match "log::trace", "log::debug", "log::info", "log::warn", "log::error"
}
```

From writ-cli/src/cli_host.rs (existing on_log):
```rust
fn on_log(&mut self, level: LogLevel, message: &str) {
    eprintln!("[{level:?}] {message}");  // currently prints "[Debug] msg" -- needs UPPERCASE
}
```

From writ-runtime/src/host.rs:
```rust
pub enum LogLevel { Trace, Debug, Info, Warn, Error }
// RuntimeHost::on_log(level: LogLevel, message: &str)
```

From writ-golden/tests/golden/fn_log_say_choice.writ (BEFORE):
```writ
pub extern fn log(msg: string);
pub extern fn say(text: string);
pub extern fn choice();

pub fn main() {
    ::log("saying Test");
    ::say("Test");
    ::log("showing choice");
    ::choice();
}
```

From writ-cli/tests/fixtures/hello.writ (BEFORE):
```writ
pub fn main() {
    let x: int = 42;
    ::log(test_string);
}
```

Golden test bless command:
```bash
BLESS=1 cargo test -p writ-golden -- test_fn_log_say_choice
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CliHost dispatch for log::level names + UPPERCASE format</name>
  <files>writ-cli/src/cli_host.rs</files>
  <action>
1. In `writ-cli/src/cli_host.rs`, update the `on_request` method's `ExternCall` match arm to handle log-level extern names. Add match arms for `"log::trace"`, `"log::debug"`, `"log::info"`, `"log::warn"`, `"log::error"` BEFORE the existing unknown-extern fallback. Each arm should:
   - Extract `msg` from `display_args.first().cloned().unwrap_or_default()`
   - Call `self.on_log(LogLevel::Trace, &msg)` (or Debug/Info/Warn/Error as appropriate)
   - Return `HostResponse::Value(Value::Void)`

2. Update `on_log` to use UPPERCASE level prefix instead of debug format:
   ```rust
   fn on_log(&mut self, level: LogLevel, message: &str) {
       let prefix = match level {
           LogLevel::Trace => "TRACE",
           LogLevel::Debug => "DEBUG",
           LogLevel::Info  => "INFO",
           LogLevel::Warn  => "WARN",
           LogLevel::Error => "ERROR",
       };
       eprintln!("[{prefix}] {message}");
   }
   ```

3. Also remove the old `"log"` match arm if one exists in on_request (it may be handled by the generic extern fallback currently -- check and ensure bare `"log"` is NOT special-cased, so it falls through to the unknown-extern handler).

4. Add a unit test `test_on_log_uppercase_format` in the existing `#[cfg(test)]` module at the bottom of cli_host.rs. The test should verify:
   - Create a CliHost with an empty module
   - Call `host.on_log(LogLevel::Debug, "test message")`
   - Capture stderr and verify output contains `[DEBUG] test message`
   - Note: if capturing stderr is impractical, simply verify the match arms compile and the format string is correct. A simpler approach: test that `resolve_extern_name` returns the expected name for a known extern index (this pattern already exists in the test module).
  </action>
  <verify>
    <automated>cargo test -p writ-cli 2>&1</automated>
  </verify>
  <done>CliHost matches "log::trace" through "log::error" in on_request and calls on_log with the correct LogLevel variant. on_log prints "[TRACE]" / "[DEBUG]" / "[INFO]" / "[WARN]" / "[ERROR]" UPPERCASE prefix. CLI tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all fixtures from log(msg) to log::info(msg), re-bless golden, update spec</name>
  <files>writ-golden/tests/golden/fn_log_say_choice.writ, writ-golden/tests/golden/fn_log_say_choice.writil, writ-cli/tests/fixtures/hello.writ, writ-parser/tests/cases/07_functions.writ, writ-parser/tests/cases/09_entities.writ, writ-parser/tests/cases/11_error_handling.writ, writ-parser/tests/cases/14_attributes.writ, writ-parser/tests/cases/15_ranges_indexing.writ, writ-parser/tests/cases/16_generics.writ, writ-parser/tests/cases/17_globals_atomic.writ, writ-parser/tests/cases/18_extern.writ, writ-parser/tests/cases/20_comprehensive.writ, language-spec/spec/27_26_standard_library_builtins.md</files>
  <action>
1. **Golden test fixture** (`writ-golden/tests/golden/fn_log_say_choice.writ`):
   - Remove `pub extern fn log(msg: string);` declaration (log is now compiler-injected, not user-declared)
   - Change `::log("saying Test")` to `::log::info("saying Test")`
   - Change `::log("showing choice")` to `::log::info("showing choice")`
   - Keep `pub extern fn say(text: string);` and `pub extern fn choice();` unchanged
   - Result:
     ```writ
     pub extern fn say(text: string);
     pub extern fn choice();

     pub fn main() {
         ::log::info("saying Test");
         ::say("Test");
         ::log::info("showing choice");
         ::choice();
     }
     ```

2. **Re-bless golden snapshot**: Run `BLESS=1 cargo test -p writ-golden -- test_fn_log_say_choice` to re-generate `fn_log_say_choice.writil`. The new snapshot will have different ExternDef token numbers (log::info gets its own ExternDef row, injected after say/choice). Verify the re-blessed `.writil` contains `CALL_EXTERN` instructions referencing the log::info token.

3. **CLI fixture** (`writ-cli/tests/fixtures/hello.writ`):
   - Change `::log(test_string)` to `::log::info(test_string)`
   - Note: `hello.writ` calls `::log(test_string)` where `test_string` is a function name, NOT a string literal. The new form `::log::info(test_string)` will call `log::info` with the result of `test_string()`. But wait -- `test_string` is defined as `pub fn test_string() -> string`. In the current code `::log(test_string)` passes the function name as an identifier, which resolves to the function. Check whether this passes the function value or calls it. In either case, update to `::log::info(test_string)` and let the typechecker validate. If `test_string` here is being called (common in Writ), it should be `::log::info(test_string())`. Inspect the context: since `log(msg: string)` expects a string and `test_string` returns string, it was likely being called. Update to `::log::info(test_string())` if needed for type correctness, or match the existing calling convention.

4. **Parser test cases** -- Update ALL `log(...)` calls to `log::info(...)` in these files:
   - `07_functions.writ`: `log($"Took {amount} damage")` -> `log::info($"Took {amount} damage")` (3 occurrences); `log(item.name)` -> `log::info(item.name)`; `log($"Step {i}")` -> `log::info($"Step {i}")`
   - `09_entities.writ`: `log($"Guard spawned: {self.name}")` -> `log::info($"Guard spawned: {self.name}")`
   - `11_error_handling.writ`: `log(err.message())` -> `log::info(err.message())`; `log("Entity is dead")` -> `log::info("Entity is dead")`
   - `14_attributes.writ`: `log($"[DEBUG] {msg}")` -> `log::info($"[DEBUG] {msg}")`; `log($"[{category}] {msg}")` -> `log::info($"[{category}] {msg}")`
   - `15_ranges_indexing.writ`: `log($"Step {i}")` -> `log::info($"Step {i}")` (2 occurrences); `log($"Value: {f}")` -> `log::info($"Value: {f}")`
   - `16_generics.writ`: `log(item.into<string>())` -> `log::info(item.into<string>())`; `log(item.display())` -> `log::info(item.display())`
   - `17_globals_atomic.writ`: `log($"{name} has {gold} gold")` -> `log::info($"{name} has {gold} gold")`
   - `18_extern.writ`: Remove `extern fn log(msg: string);` declaration; change `log("Hello from Writ!")` to `log::info("Hello from Writ!")`
   - `20_comprehensive.writ`: `log($"Shopkeeper {self.name} created with {self.inventory.length} items")` -> `log::info($"Shopkeeper {self.name} created with {self.inventory.length} items")`
   - Note: Parser tests only test parsing, not semantics. The syntax `log::info(...)` is a valid path-call expression that the parser already handles. These changes keep the examples consistent with the new API.

5. **Spec update** (`language-spec/spec/27_26_standard_library_builtins.md`):
   - Rewrite the `log` entry in the `## 26.4 Inbuilt Functions` table (or equivalent section).
   - Remove `log(msg: string)` as a single inbuilt function.
   - Add the `log::` namespace description:
     - `log` is a compiler-known namespace, not a callable function
     - Five leveled functions: `log::trace(msg)`, `log::debug(msg)`, `log::info(msg)`, `log::warn(msg)`, `log::error(msg)`
     - Each accepts a single `string` argument and returns `void`
     - Root-qualified forms (`::log::info(msg)`) are also valid
     - All route to `RuntimeHost::on_log(level, message)` with the corresponding `LogLevel` variant
     - Standard shadowing: a user `let log = 5;` shadows the namespace at that scope
   - Update any existing spec examples that show `log("msg")` to use `log::info("msg")`
   - Keep the existing `say` and `choice` entries unchanged

6. **Verify all tests pass**: Run `cargo test --workspace` to confirm no regressions across the entire workspace.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1</automated>
  </verify>
  <done>All golden tests pass with re-blessed snapshot. All parser tests pass with updated log::info syntax. CLI integration tests pass with updated hello.writ. Spec documents the leveled log:: namespace. `cargo test --workspace` is fully green.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` passes with zero failures
- `writ-golden/tests/golden/fn_log_say_choice.writil` contains `CALL_EXTERN` for log::info
- `writ-golden/tests/golden/fn_log_say_choice.writ` does NOT contain `extern fn log` or bare `::log(`
- `writ-cli/tests/fixtures/hello.writ` uses `::log::info` syntax
- No parser test file contains bare `log(` calls (except inside string literals)
- Spec `27_26_standard_library_builtins.md` documents `log::trace`, `log::debug`, `log::info`, `log::warn`, `log::error`
</verification>

<success_criteria>
1. CliHost prints `[INFO] saying Test` when `log::info("saying Test")` executes (not `[Info]`)
2. All 5 log levels are dispatched correctly in CliHost
3. Golden snapshot re-blessed with correct CALL_EXTERN tokens
4. All parser test cases updated -- no bare `log(` calls remain
5. Spec documents the leveled log:: namespace
6. `cargo test --workspace` is fully green
</success_criteria>

<output>
After completion, create `.planning/phases/44-extended-log-with-levels/44-02-SUMMARY.md`
</output>

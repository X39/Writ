---
phase: 47-spec-amendments
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/38_2_9_memory_model.md
  - language-spec/spec/40_2_11_construction_model.md
  - language-spec/spec/44_2_15_il_type_system.md
  - language-spec/spec/45_2_16_il_module_format.md
autonomous: true
requirements: [SPEC-02, SPEC-03, SPEC-04, SPEC-05, SPEC-06]
must_haves:
  truths:
    - "Section 2.9.1 memory model table shows Structs as Value type and includes a Classes row as Reference type"
    - "Section 2.9.3 closure captures use class keyword for compiler-generated capture environment types"
    - "Section 2.9.8 IL implications describe MOV and NEW as type-dependent normative behavior without any v4.0 callout"
    - "Section 2.11 construction model documents kind-dependent NEW semantics for structs (inline) and classes (heap)"
    - "Section 2.15.1 register model lists structs as value types alongside int/float/bool/enums"
    - "Section 2.16.5 TypeDef kind table includes kind=4 for class and format_version history includes version 3"
  artifacts:
    - path: "language-spec/spec/38_2_9_memory_model.md"
      provides: "Updated memory model with struct=value, class=reference"
      contains: "Classes"
    - path: "language-spec/spec/40_2_11_construction_model.md"
      provides: "Kind-dependent NEW semantics"
      contains: "kind"
    - path: "language-spec/spec/44_2_15_il_type_system.md"
      provides: "Updated register model with value-type structs"
      contains: "structs"
    - path: "language-spec/spec/45_2_16_il_module_format.md"
      provides: "TypeDef kind=4 class, format_version 3"
      contains: "kind=4"
  key_links:
    - from: "language-spec/spec/38_2_9_memory_model.md"
      to: "language-spec/spec/44_2_15_il_type_system.md"
      via: "memory model references register model for value-type struct storage"
      pattern: "register"
    - from: "language-spec/spec/40_2_11_construction_model.md"
      to: "language-spec/spec/45_2_16_il_module_format.md"
      via: "construction model references TypeDef.kind for NEW behavior"
      pattern: "TypeDef"
---

<objective>
Update IL foundation sections (memory model, construction model, type system, module format) to reflect the struct/class split as normative prose.

Purpose: These four IL spec sections define the foundational semantics that instruction-level sections (Plan 03) and the runtime/compiler (Phases 48-51) depend on. Updating them transforms the struct/class split from a design record into normative specification.

Output: Four updated spec files with clean v4.0 semantics — no forward-looking callouts, no version history notes except format_version.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spec-amendments/47-CONTEXT.md

Source files:
@language-spec/spec/38_2_9_memory_model.md
@language-spec/spec/40_2_11_construction_model.md
@language-spec/spec/44_2_15_il_type_system.md
@language-spec/spec/45_2_16_il_module_format.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update memory model and construction model</name>
  <files>language-spec/spec/38_2_9_memory_model.md, language-spec/spec/40_2_11_construction_model.md</files>
  <action>
**Update `38_2_9_memory_model.md`:**

**Section 2.9.1 (Value Types vs Reference Types):** Replace the existing table with the v4.0 table. Remove the `> **v4.0 Change**` blockquote entirely. The table should now read:

| Type                   | Kind                       | Storage                                  | Assignment                                                          | GC Traced                           |
|------------------------|----------------------------|------------------------------------------|---------------------------------------------------------------------|-------------------------------------|
| `int`, `float`, `bool` | **Value**                  | Register (direct bits)                   | Copy bits                                                           | No                                  |
| `string`               | **Reference, immutable**   | Heap (GC-managed)                        | Copy reference                                                      | Yes                                 |
| Structs                | **Value**                  | Register/inline (no heap alloc)          | Shallow copy -- copies all fields; ref fields copy pointer          | Payload ref fields traced           |
| Classes                | **Reference**              | Heap (GC-managed)                        | Copy reference (shared object)                                      | Yes                                 |
| Enums                  | **Value**                  | Register/stack (tag + inline payload)    | Copy tag + payload                                                  | Payload fields traced if references |
| Arrays                 | **Reference**              | Heap (GC-managed)                        | Copy reference (shared)                                             | Yes                                 |
| Entities               | **Reference (handle)**     | Entity runtime + GC heap                 | Copy handle                                                         | Yes                                 |
| Components             | **Extern (host-owned)**    | Host-managed, accessed via entity handle | Via entity reference                                                | Host responsibility                 |
| Closures/Delegates     | **Reference**              | Heap (GC-managed)                        | Copy reference                                                      | Yes                                 |

After the table, add a paragraph about struct value semantics: "Struct value semantics: Structs are value types with inline storage. Assignment copies all fields by value. Reference-typed fields within a struct copy the pointer -- both copies share the referenced object, but the struct values themselves are independent. This is analogous to enum value semantics but with named fields instead of tagged payloads."

Keep the existing "Enum value semantics:" paragraph unchanged.

**Section 2.9.2 (Assignment and Mutability):** Update the example to use `class` instead of struct for the reference-type example. The `Merchant` example showing shared-on-assign should use `class Merchant`:
```
let mut a = new Merchant { name: "Tim", gold: 100 };  // Merchant is a class (reference type)
let mut b = a;     // b and a point to the same object
b.gold += 50;      // a.gold is ALSO now 150
```
Keep the explanation about standard GC-language behavior.

**Section 2.9.3 (Closure Captures):** Remove the `> **v4.0 Note:**` blockquote entirely. Replace the compiler-generated capture struct example to use `class` keyword:
```
class __closure_env_0 {
    count: int,   // shared mutable field
}
```
Update the prose that says "using standard structs" to say "using a compiler-generated class type" (since the capture environment is a reference type, it must be a class).

**Section 2.9.8 (IL Implications):** Remove the `**v4.0 Note:**` bullet at the end entirely. Instead, update the existing `MOV` and `NEW` bullets to describe the type-dependent behavior normatively:
- Change the first bullet to: "`MOV` copies register contents. For reference types (classes, strings, arrays, entities, delegates), this copies the pointer -- no deep copy. For value types (int, float, bool, enums, structs), this copies the full value. For value-type structs, this is a multi-word copy of all fields."
- Change the `NEW` bullet to: "`NEW`, `NEW_ARRAY`, `NEW_ENUM`, `SPAWN_ENTITY` are allocation points. `NEW` behavior is kind-dependent: for classes (kind=4), it allocates on the GC heap; for structs (kind=0), it initializes the value inline with no heap allocation. The GC may trigger during any heap allocation."

**Update `40_2_11_construction_model.md`:**

Rewrite to describe kind-dependent construction:

Opening paragraph stays similar but replace "for both structs and entities" with "for structs, classes, and entities".

Replace the **Struct construction:** section with two sections:

**Struct construction (value type):**
1. `NEW type_idx` -- initialize value inline (no heap allocation).
2. `SET_FIELD` / `LOAD_*` for every field (defaults + overrides).
No lifecycle hooks are called for structs. Construction is complete after field initialization.

**Class construction (reference type):**
1. `NEW type_idx` -- allocate zeroed memory on the GC heap.
2. `SET_FIELD` / `LOAD_*` for every field (defaults + overrides).
3. `CALL __on_create` -- run the `on create` hook body, if defined.

Keep the **Entity construction:** section unchanged (entities are specialized classes).

Update the **No constructors:** paragraph to say "for structs, classes, and entities."

Update the **Lifecycle hooks:** paragraph: "Classes and entities support lifecycle hooks (`on create`, `on finalize`, `on serialize`, `on deserialize`). Entities additionally support `on destroy` and `on interact`. Structs have no lifecycle hooks. All hooks receive implicit `mut self`. Hooks lower to regular methods stored in the TypeDef metadata."

Update the **Default field values:** paragraph: change "NEW allocates zeroed memory" to "For classes and entities, `NEW`/`SPAWN_ENTITY` allocates zeroed memory. For structs, `NEW` initializes the value inline -- the compiler emits explicit code for all field initialization in both cases."
  </action>
  <verify>
    <automated>grep -c "v4.0" language-spec/spec/38_2_9_memory_model.md && grep -c "v4.0" language-spec/spec/40_2_11_construction_model.md</automated>
  </verify>
  <done>Memory model table shows Structs as Value type with Classes row added. No "v4.0 Change" or "v4.0 Note" callouts remain. Construction model describes kind-dependent NEW semantics for structs (inline) vs classes (heap). Closure captures use class keyword.</done>
</task>

<task type="auto">
  <name>Task 2: Update IL type system and module format</name>
  <files>language-spec/spec/44_2_15_il_type_system.md, language-spec/spec/45_2_16_il_module_format.md</files>
  <action>
**Update `44_2_15_il_type_system.md`:**

**Section 2.15.1 (Register Model):** Update the bullet list to reflect the three-way split per the CONTEXT.md decision:
- For **value types** (int, float, bool, enums, structs), the register holds the value directly. For value-type structs, the register holds all fields inline as a single abstract typed slot. The runtime determines physical storage from the register's declared type.
- For **reference types** (string, classes, entities, arrays, closures/delegates), the register holds a GC-managed reference.

Note the change: `structs` moved from reference types to value types. `classes` takes the reference-types position that `structs` formerly held.

Keep the rest of section 2.15.1 unchanged (the paragraph about abstract registers and MOV is already correct in spirit -- it already says "the runtime uses the register's declared type to determine storage requirements").

**Section 2.15.3 (Type Reference Encoding):** In the table, update the description for kind `0x10` from:
"Named type -- struct, enum, entity, or component"
to:
"Named type -- struct, class, enum, entity, or component"

**Update `45_2_16_il_module_format.md`:**

**Section 2.16.1 (Binary Container):** Update the format version history line to include version 3:
"**Format version history:** Version 1 -- initial format (MethodDef row: 20 bytes). Version 2 -- added `param_count(u16)` to MethodDef (row: 24 bytes, padded from 22). Version 3 -- TypeDef.kind=4 (class) added; kind=0 (struct) now means value type."

**Section 2.16.5 (Metadata Tables):** Update the **TypeDef.kind** line after the table from:
`**TypeDef.kind:** 0 = struct, 1 = enum, 2 = entity, 3 = component.`
to:
`**TypeDef.kind:** 0 = struct (value type), 1 = enum, 2 = entity, 3 = component, 4 = class (reference type).`

In the TypeDef table row (#2), update the Purpose column from "Types defined in this module" to "Types defined in this module (kind distinguishes struct/class/enum/entity/component)".

**Section 2.16.6 (Method Body Layout) -- Register type table paragraph at the end:** Add a sentence: "For value-type struct registers, the runtime uses the TypeRef to locate the struct's TypeDef and FieldDef list, enabling multi-word copy on MOV and precise GC tracing of embedded reference fields."
  </action>
  <verify>
    <automated>grep "kind=4" language-spec/spec/45_2_16_il_module_format.md && grep "structs" language-spec/spec/44_2_15_il_type_system.md | grep -i "value"</automated>
  </verify>
  <done>Section 2.15.1 lists structs as value types. Section 2.16.5 includes kind=4 for class. Format version history includes version 3. TypeRef encoding mentions class.</done>
</task>

</tasks>

<verification>
- No "v4.0 Change", "v4.0 Note", or "v4.0" callouts in any of the four files (except format_version history which naturally mentions v3)
- Memory model table has Structs=Value, Classes=Reference
- Construction model has struct (inline) vs class (heap) sections
- Register model lists structs under value types
- TypeDef.kind includes kind=4 for class
- Format version history includes version 3
</verification>

<success_criteria>
All four IL foundation spec files describe the struct/class split as normative truth. The memory model, construction model, type system, and module format are consistent and cross-reference correctly. No historical callouts or design record references remain.
</success_criteria>

<output>
After completion, create `.planning/phases/47-spec-amendments/47-02-SUMMARY.md`
</output>

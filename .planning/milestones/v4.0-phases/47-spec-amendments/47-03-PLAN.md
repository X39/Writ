---
phase: 47-spec-amendments
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/49_3_1_data_movement.md
  - language-spec/spec/56_3_8_object_model.md
  - language-spec/spec/63_3_15_boxing.md
  - language-spec/spec/69_b_il_decision_log.md
autonomous: true
requirements: [SPEC-07, SPEC-08, SPEC-09, SPEC-10]
must_haves:
  truths:
    - "Section 3.1 MOV description includes multi-word copy semantics for value-type struct registers"
    - "Section 3.8 NEW instruction description includes kind-dependent behavior (inline for struct, heap for class)"
    - "Section 3.15 BOX/UNBOX lists value-type structs alongside int/float/bool/enums as types requiring boxing through generics"
    - "Appendix B decision log has updated Structs row (value types, no version note) and updated Classes row (reference types, no version note)"
  artifacts:
    - path: "language-spec/spec/49_3_1_data_movement.md"
      provides: "MOV with multi-word copy for value structs"
      contains: "value-type struct"
    - path: "language-spec/spec/56_3_8_object_model.md"
      provides: "NEW with kind-dependent behavior"
      contains: "kind"
    - path: "language-spec/spec/63_3_15_boxing.md"
      provides: "BOX/UNBOX for value-type structs"
      contains: "structs"
    - path: "language-spec/spec/69_b_il_decision_log.md"
      provides: "Updated decision log rows"
      contains: "Value types"
  key_links:
    - from: "language-spec/spec/56_3_8_object_model.md"
      to: "language-spec/spec/45_2_16_il_module_format.md"
      via: "NEW references TypeDef.kind for dispatch"
      pattern: "kind"
    - from: "language-spec/spec/63_3_15_boxing.md"
      to: "language-spec/spec/44_2_15_il_type_system.md"
      via: "BOX/UNBOX references value-type register model"
      pattern: "value type"
---

<objective>
Update IL instruction sections (data movement, object model, boxing) and Appendix B decision log to reflect the struct/class split as normative prose.

Purpose: These sections define the instruction-level behavior that the VM runtime (Phase 49) will implement. Updating them ensures MOV, NEW, and BOX/UNBOX semantics are correctly specified for both value-type structs and reference-type classes.

Output: Four updated spec files with normative v4.0 instruction semantics and a clean decision log.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spec-amendments/47-CONTEXT.md

Source files:
@language-spec/spec/49_3_1_data_movement.md
@language-spec/spec/56_3_8_object_model.md
@language-spec/spec/63_3_15_boxing.md
@language-spec/spec/69_b_il_decision_log.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MOV, NEW, and BOX/UNBOX instruction sections</name>
  <files>language-spec/spec/49_3_1_data_movement.md, language-spec/spec/56_3_8_object_model.md, language-spec/spec/63_3_15_boxing.md</files>
  <action>
**Update `49_3_1_data_movement.md` (Section 3.1):**

Update the `MOV` row in the instruction table. Change the Description column from:
"Copy register to register. Semantics depend on type: value copy for primitives, reference copy for heap types."
to:
"Copy register to register. Semantics depend on type: for value types (primitives, enums, structs), copies the full value -- for value-type structs this is a multi-word copy of all fields. For reference types (classes, strings, arrays, entities, delegates), copies the reference pointer."

All other rows in the table remain unchanged.

**Update `56_3_8_object_model.md` (Section 3.8):**

Update the `NEW` row in the instruction table. Change the Description column from:
"Allocate struct instance. Memory is zeroed; defaults and overrides applied via subsequent SET_FIELD instructions."
to:
"Allocate or initialize a type instance. Behavior is kind-dependent: for structs (kind=0, value type), initializes the value inline in the destination register with no heap allocation. For classes (kind=4, reference type), allocates zeroed memory on the GC heap and stores the reference in the destination register. In both cases, field defaults and overrides are applied via subsequent SET_FIELD instructions."

Update the construction sequence example at the bottom of the file. The current example shows entity construction with `SPAWN_ENTITY`. Add a struct construction example BEFORE the entity one to show the contrast:

```
Construction sequence for `new Vec2 { x: 1.0, y: 2.0 }` (struct -- value type):

```
NEW           r0, Vec2_type        // initialize value inline (no heap allocation)
LOAD_FLOAT    r1, 1.0
SET_FIELD     r0, x_field, r1
LOAD_FLOAT    r1, 2.0
SET_FIELD     r0, y_field, r1
// No on_create call -- value-type structs have no lifecycle hooks
```

Construction sequence for `new Merchant { name: "Tim", gold: 100 }` (class -- reference type):

```
NEW           r0, Merchant_type    // allocate on GC heap
LOAD_STRING   r1, "Tim"_idx
SET_FIELD     r0, name_field, r1
LOAD_INT      r1, 100
SET_FIELD     r0, gold_field, r1
CALL          r_, Merchant::__on_create, r0  // run on_create hook
```

Keep the existing entity construction example (`new Guard { name: "Steve" }`) unchanged below the class example.

**Update `63_3_15_boxing.md` (Section 3.15):**

Update the opening sentence from:
"Boxing is required when value types (`int`, `float`, `bool`, enums) pass through generic parameters."
to:
"Boxing is required when value types (`int`, `float`, `bool`, enums, structs) pass through generic parameters."

Update the last paragraph from:
"Reference types (`string`, structs, arrays, entities, delegates) are already references and pass through generics without boxing."
to:
"Reference types (`string`, classes, arrays, entities, delegates) are already references and pass through generics without boxing."

Note the two changes: (1) `structs` added to the value types list, (2) `structs` replaced with `classes` in the reference types list.
  </action>
  <verify>
    <automated>grep "multi-word" language-spec/spec/49_3_1_data_movement.md && grep "kind-dependent\|kind=0\|kind=4" language-spec/spec/56_3_8_object_model.md && grep "structs" language-spec/spec/63_3_15_boxing.md | head -3</automated>
  </verify>
  <done>MOV describes multi-word copy for value-type structs. NEW is kind-dependent (inline for struct, heap for class). BOX/UNBOX lists structs as value types requiring boxing.</done>
</task>

<task type="auto">
  <name>Task 2: Update Appendix B IL Decision Log</name>
  <files>language-spec/spec/69_b_il_decision_log.md</files>
  <action>
Update the two existing rows in the decision log table for Structs and Classes. Remove any version references and write them as normative decisions.

Find the **Structs** row (currently says "v4.0" in both Choice and Rationale). Replace:
- Choice: `**Value types**`
- Rationale: `Inline storage, copy-on-assign. Reference-typed fields copy pointer (shallow copy). Structural equality auto-derived. No lifecycle hooks. Recursive value-type structs are illegal (infinite size).`

Find the **Classes** row (currently says "v4.0" in both). Replace:
- Choice: `**Reference types**`
- Rationale: `Heap-allocated, GC-managed, shared-on-assign. Support lifecycle hooks (on create/finalize/serialize/deserialize). The `class` keyword fills the reference-type role.`

Also verify/update the **Lifecycle hooks** row. Currently says "on create/finalize/serialize/deserialize on structs and entities". Change to: "on create/finalize/serialize/deserialize on classes and entities". (Structs no longer have lifecycle hooks.)

Also verify/update the **Closure mut captures** row. Currently says "Shared capture struct". Change Rationale to: "Compiler generates a class; both outer scope and closure reference it" (was "struct").

Also verify/update the **Construction syntax** row. Currently says "same syntax for structs and entities". Update to: "same syntax for structs, classes, and entities".

Ensure no "(v4.0)" parentheticals appear anywhere in the decision log. Every row should read as normative present-tense truth.
  </action>
  <verify>
    <automated>grep -c "v4.0" language-spec/spec/69_b_il_decision_log.md</automated>
  </verify>
  <done>Decision log has clean Structs row (value types), Classes row (reference types), updated lifecycle hooks row (classes and entities), updated closure captures row (class), updated construction syntax row. Zero "v4.0" references.</done>
</task>

</tasks>

<verification>
- `49_3_1_data_movement.md` MOV description includes "multi-word copy" for structs
- `56_3_8_object_model.md` NEW description is kind-dependent, has both struct and class construction examples
- `63_3_15_boxing.md` lists structs under value types requiring boxing, classes under reference types
- `69_b_il_decision_log.md` has zero occurrences of "v4.0"
- All four files use normative present-tense prose with no version callouts
</verification>

<success_criteria>
All instruction sections and the decision log reflect the struct/class split as normative truth. MOV, NEW, and BOX/UNBOX have correct type-dependent semantics documented. The decision log is a clean record with no version annotations.
</success_criteria>

<output>
After completion, create `.planning/phases/47-spec-amendments/47-03-SUMMARY.md`
</output>

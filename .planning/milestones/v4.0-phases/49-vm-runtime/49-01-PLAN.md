---
phase: 49-vm-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-runtime/src/value.rs
  - writ-runtime/src/heap.rs
  - writ-runtime/src/gc.rs
  - writ-runtime/src/runtime.rs
  - writ-runtime/src/dispatch/arith.rs
  - writ-runtime/src/dispatch/objects.rs
  - writ-runtime/src/dispatch/mod.rs
  - writ-runtime/src/dispatch/calls.rs
  - writ-runtime/src/dispatch/intrinsics.rs
  - writ-runtime/src/dispatch/concurrency.rs
  - writ-runtime/src/dispatch/entities.rs
  - writ-runtime/src/scheduler.rs
autonomous: true
requirements: [VM-01, VM-02, VM-05, VM-06]

must_haves:
  truths:
    - "Value::InlineStruct variant exists with type_idx and fields"
    - "Value no longer derives Copy — only Clone"
    - "All existing tests pass after mechanical .clone() fixes"
    - "MOV clones the source register value (multi-word copy for InlineStruct)"
    - "BOX accepts InlineStruct values and UNBOX clones them back out"
    - "PartialEq handles InlineStruct comparison"
  artifacts:
    - path: "writ-runtime/src/value.rs"
      provides: "Value::InlineStruct variant, Copy removed, PartialEq updated"
      contains: "InlineStruct"
    - path: "writ-runtime/src/dispatch/arith.rs"
      provides: "exec_mov with .clone(), exec_convert with .clone(), exec_unbox with .clone()"
      contains: ".clone()"
    - path: "writ-runtime/src/heap.rs"
      provides: ".copied() changed to .cloned() in get_field"
      contains: ".cloned()"
    - path: "writ-runtime/src/gc.rs"
      provides: ".copied() changed to .cloned() in get_field"
      contains: ".cloned()"
  key_links:
    - from: "writ-runtime/src/value.rs"
      to: "all writ-runtime source files"
      via: "Value enum used everywhere"
      pattern: "use crate::value::Value"
    - from: "writ-runtime/src/dispatch/arith.rs"
      to: "writ-runtime/src/value.rs"
      via: "exec_mov clones register values"
      pattern: "\\.clone\\(\\)"
---

<objective>
Add Value::InlineStruct variant to the Value enum, remove the Copy derive (keep Clone), and mechanically fix all compilation errors across writ-runtime. This is the foundational change — every other Phase 49 task depends on Value being Clone-only with the InlineStruct variant present.

Purpose: Structs must live inline in registers as value types (VM-01). Removing Copy is the prerequisite for Vec<Value> fields inside InlineStruct (Vec is not Copy). MOV becomes an explicit .clone() which gives InlineStruct correct multi-word copy semantics (VM-02). BOX/UNBOX already work with owned Values but UNBOX needs .clone() instead of Copy dereference (VM-05).

Output: All writ-runtime source files compile and all existing tests pass with the new Value enum.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-vm-runtime/49-CONTEXT.md
@.planning/phases/49-vm-runtime/49-RESEARCH.md

@writ-runtime/src/value.rs
@writ-runtime/src/heap.rs
@writ-runtime/src/gc.rs
@writ-runtime/src/dispatch/arith.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From writ-runtime/src/value.rs (CURRENT — will be modified):
```rust
#[derive(Debug, Clone, Copy)]  // Copy will be REMOVED
pub enum Value {
    Void,
    Int(i64),
    Float(f64),
    Bool(bool),
    Ref(HeapRef),
    Entity(EntityId),
    // InlineStruct will be ADDED here
}
```

From writ-runtime/src/heap.rs:
```rust
pub enum HeapObject {
    String(String),
    Struct { fields: Vec<Value> },       // Reused for class (kind=4)
    Array { elem_type: u32, elements: Vec<Value> },
    Delegate { method_idx: usize, target: Option<Value> },
    Enum { type_idx: u32, tag: u16, fields: Vec<Value> },
    Boxed(Value),                        // Works for boxing InlineStruct
}
```

From writ-runtime/src/gc.rs (GcHeap trait):
```rust
pub trait GcHeap {
    fn get_field(&self, href: HeapRef, idx: usize) -> Result<Value, RuntimeError>;
    fn set_field(&mut self, href: HeapRef, idx: usize, val: Value) -> Result<(), RuntimeError>;
    // ... other methods
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add InlineStruct variant and remove Copy from Value</name>
  <files>writ-runtime/src/value.rs</files>
  <action>
1. Change `#[derive(Debug, Clone, Copy)]` to `#[derive(Debug, Clone)]` on the Value enum.
2. Add the new variant: `InlineStruct { type_idx: u32, fields: Vec<Value> }` after the `Entity(EntityId)` variant.
3. Update the doc comment on Value to remove "All variants are Copy-safe" — change to "Clone-only. InlineStruct holds fields inline (value-type struct semantics)."
4. Update the `PartialEq` impl to add a new match arm:
   ```rust
   (Value::InlineStruct { type_idx: a, fields: fa }, Value::InlineStruct { type_idx: b, fields: fb }) => a == b && fa == fb,
   ```
5. Do NOT change Default impl (still returns Value::Void).
6. Do NOT add any helper methods yet (Claude's discretion — keep this task minimal).
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo check -p writ-runtime 2>&1 | head -5</automated>
  </verify>
  <done>Value enum has InlineStruct variant, Copy is removed, PartialEq handles InlineStruct. Compilation will fail in downstream files (expected — Task 2 fixes those).</done>
</task>

<task type="auto">
  <name>Task 2: Fix all Copy-removal compilation errors across writ-runtime</name>
  <files>
    writ-runtime/src/heap.rs,
    writ-runtime/src/gc.rs,
    writ-runtime/src/runtime.rs,
    writ-runtime/src/dispatch/arith.rs,
    writ-runtime/src/dispatch/objects.rs,
    writ-runtime/src/dispatch/mod.rs,
    writ-runtime/src/dispatch/calls.rs,
    writ-runtime/src/dispatch/intrinsics.rs,
    writ-runtime/src/dispatch/concurrency.rs,
    writ-runtime/src/dispatch/entities.rs,
    writ-runtime/src/scheduler.rs
  </files>
  <action>
Run `cargo build -p writ-runtime` repeatedly and fix every compilation error caused by removing Copy from Value. The compiler will enumerate all sites. Key categories:

**A. `.copied()` to `.cloned()` (6 known sites):**
- `heap.rs` lines ~101, ~110: `fields.get(idx).copied()` → `.cloned()` (BumpHeap get_field, both Struct and Enum arms)
- `gc.rs` lines ~214, ~221: `fields.get(idx).copied()` → `.cloned()` (MarkSweepHeap get_field, both Struct and Enum arms)
- `runtime.rs` line ~295: `f.registers.get(reg as usize).copied()` → `.cloned()` (register_value method)

**B. Direct register assignments that implicitly copy (add `.clone()`):**
- `dispatch/arith.rs` line ~23: `exec_mov` — `frame.registers[r_src as usize]` → add `.clone()`. This is the VM-02 multi-word copy semantics.
- `dispatch/arith.rs` line ~363: `exec_convert` — `frame.registers[r_src as usize]` → add `.clone()`
- `dispatch/arith.rs` lines ~421-435: `exec_box` — `frame.registers[r_val as usize]` → add `.clone()`. `exec_unbox` — `let val = *val` → `let val = val.clone()` (VM-05 UNBOX clone)
- `dispatch/objects.rs`: All `let val = frame.registers[idx]` patterns — add `.clone()`. Includes exec_wrap_some, exec_unwrap, exec_wrap_ok, exec_wrap_err, exec_unwrap_ok, exec_extract_err, exec_extract_field, exec_array_init, exec_array_store, exec_array_add, exec_array_insert, exec_set_field, exec_new_enum, exec_array_load (reading from array elements which are also Value).
- `dispatch/mod.rs` line ~289: `let ret_val = ctx.task.call_stack.last().unwrap().registers[r_src as usize]` → add `.clone()` (Ret instruction)
- `dispatch/intrinsics.rs`: All `let val = frame.registers[idx]` and `frame.registers[r_dst] = frame.registers[r_obj]` patterns — add `.clone()`. Includes StringIntoString, ArrayIndex, ArrayIndexSet, ArrayIterable (~lines 279, 290, 304, 345).
- `dispatch/calls.rs`: All argument-passing patterns `frame.registers[r_base + i]` → add `.clone()`
- `dispatch/concurrency.rs`: Register reads — add `.clone()`
- `dispatch/entities.rs`: Register reads — add `.clone()`
- `scheduler.rs`: Register reads and return_value accesses — add `.clone()`

**C. `return_value: Option<Value>` reads:**
- `runtime.rs` line ~312: `t.return_value` (moves out) → use `.clone()` or adjust to `t.return_value.clone()`
- `runtime.rs` line ~386: same pattern in call_sync
- `runtime.rs` line ~238: `frame.registers[0] = *val` in confirm() — `*val` dereferences a &Value, add `.clone()` instead
- `runtime.rs` line ~468: `if let Some(Value::Ref(href)) = task.return_value` — this pattern-matches by value. Use `task.return_value.as_ref()` and match `Some(Value::Ref(href))` borrowing.

**D. entity.rs:**
- `entity.rs` line ~311: `.copied()` on `self.singletons.get()` — this returns `Option<&HeapRef>` not `Option<&Value>`, so HeapRef is still Copy. Leave unchanged.

**Approach:** Run `cargo build -p writ-runtime` after each batch of fixes. The compiler output tells you exactly which files and lines need changes. Fix all errors until the build succeeds, then run `cargo test -p writ-runtime` to verify all existing tests pass.

Do NOT change any test files yet (those will need .clone() too — fix them as part of this task since they must compile).
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-runtime 2>&1 | tail -5</automated>
  </verify>
  <done>All writ-runtime source files compile. All existing tests pass (the full test suite for writ-runtime, including vm_tests, gc_tests, task_tests, hook_dispatch_tests). Zero test failures. The Value enum is now Clone-only with the InlineStruct variant available for Plan 02 to use.</done>
</task>

</tasks>

<verification>
- `cargo build -p writ-runtime` succeeds with zero errors
- `cargo test -p writ-runtime` passes all existing tests (no regressions)
- `cargo build --workspace` succeeds (no downstream breakage — writ-compiler and writ-assembler don't use Value directly)
</verification>

<success_criteria>
1. Value::InlineStruct { type_idx: u32, fields: Vec<Value> } variant exists
2. Value does NOT derive Copy (only Clone)
3. All .copied() on Value fields changed to .cloned()
4. All direct register assignments use .clone()
5. All existing writ-runtime tests pass without modification to test assertions (only mechanical .clone() fixes in test code if needed)
6. Full workspace builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/49-vm-runtime/49-01-SUMMARY.md`
</output>

---
status: testing
phase: v5.0-lsp-and-dap
source: 52-01-SUMMARY.md, 52-02-SUMMARY.md, 52-03-SUMMARY.md, 53-01-SUMMARY.md, 53-02-SUMMARY.md, 53-03-SUMMARY.md, 54-01-SUMMARY.md, 54-02-SUMMARY.md, 54-03-SUMMARY.md, 54-04-SUMMARY.md, 55-01-SUMMARY.md, 55-02-SUMMARY.md, 56-01-SUMMARY.md, 56-02-SUMMARY.md, 57-01-SUMMARY.md, 57-02-SUMMARY.md, 58-01-SUMMARY.md, 59-01-SUMMARY.md
started: 2026-03-16T23:30:00Z
updated: 2026-03-17T12:00:00Z
---

## Current Test

number: 6
name: Cross-File Diagnostics (re-verify)
expected: |
  In a multi-file project (with writ.toml), create an error in file A that is referenced by file B. Diagnostics appear in the correct file (file A), not misattributed to file B.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Build writ-lsp and writ-dap from source (cargo build -p writ-lsp -p writ-dap). Run the binaries — they start without crash and wait for protocol input on stdin.
result: pass

### 2. VSIX Install and Extension Activation
expected: Install writ-0.1.0.vsix via VS Code ("Extensions: Install from VSIX..."). Open any .writ file — the Writ extension activates (visible in status bar or Extensions panel showing "Writ" as active).
result: pass

### 3. Syntax Highlighting (TextMate Grammar)
expected: Open a .writ file containing keywords (fn, let, struct, entity), strings ("hello"), comments (// and /* */), numbers (42, 0xFF), and dialogue blocks. All are highlighted with distinct colors matching their category.
result: pass

### 4. Diagnostics on Parse Error
expected: Introduce a syntax error in a .writ file (e.g., remove a closing brace). A red squiggle appears under the error location within 1-2 seconds. The Problems panel shows the error with a message.
result: issue
reported: "Only inside of functions squiggle appears"
severity: minor

### 5. Diagnostics Clear on Fix
expected: Fix the syntax error inside a function (restore the missing token). The red squiggle disappears within 1-2 seconds. The Problems panel clears the error.
result: pass

### 6. Cross-File Diagnostics (re-verify)
expected: In a multi-file project (with writ.toml), create an error in file A that is referenced by file B. Diagnostics appear in the correct file (file A), not misattributed to file B.
result: [pending]

### 7. Hover Shows Type Info (re-verify)
expected: Hover over a variable name — a tooltip appears showing the variable's type in a markdown code block (e.g., x: int). Hover over a function call — tooltip shows the full function signature. This should work both with and without writ.toml present.
result: [pending]

### 8. Go to Definition
expected: Ctrl+click (or F12) on a function name used in a call expression. The editor navigates to the function's declaration. Works for struct/entity/enum names too.
result: issue
reported: "Works on new X but not on variable types"
severity: major

### 9. Find All References
expected: Right-click a function name > "Find All References" (or Shift+F12). A references panel shows all locations where the function is used, including the declaration.
result: issue
reported: "Works at actual usage position (call) but not at declaration (fn ... () ...)"
severity: minor

### 10. Identifier Completion
expected: Start typing a partial keyword or function name and press Ctrl+Space. A completion list appears with keywords (fn, let, if, etc.), primitive types (int, float, string, bool), and user-defined names from the file.
result: pass

### 11. Dot Completion
expected: Type a struct-typed variable followed by . — a completion list appears showing the struct's fields and methods. For entity-typed variables, component names also appear in the list.
result: pass

### 12. Signature Help
expected: Type a function name followed by ( — a parameter hint popup appears showing the function's parameter names and types. As you type commas between arguments, the active parameter highlights.
result: issue
reported: "fail"
severity: major

### 13. Semantic Highlighting — Entities vs Types
expected: In a file with both struct Foo {} and entity Bar {}, the entity name "Bar" is highlighted with a distinct color from the struct name "Foo" (entity uses a different semantic token type).
result: issue
reported: "Both share the same color"
severity: cosmetic

### 14. Dialogue Speaker Highlighting
expected: In a dialogue block, @SpeakerName is highlighted with a distinct color (different from regular identifiers and types). This works for speakers in nested Choice/If/Match arms too.
result: pass

### 15. Set Breakpoint
expected: Open a .writ file in VS Code. Click in the gutter (left margin) next to a line with executable code. A red dot appears indicating a breakpoint is set.
result: pass

### 16. Launch Debugger (re-verify)
expected: Create a launch.json with "type": "writ". Press F5 — the debugger starts, compiles the .writ file, and either hits a breakpoint or runs to completion. Both pub and non-pub main should work.
result: [pending]

### 17. Step Over (F10)
expected: With a breakpoint hit, press F10 (Step Over). The yellow highlight moves to the next source line.
result: [pending]

### 18. Variables Panel
expected: When stopped at a breakpoint, the Variables panel shows local variables with their names, current values, and types.
result: [pending]

### 19. Stack Trace
expected: When stopped at a breakpoint inside a function called from another function, the Call Stack panel shows multiple frames with function names and source locations.
result: [pending]

## Summary

total: 19
passed: 8
issues: 5
pending: 6
skipped: 0

## Additional Notes

- Extension icon not used (resources/*.svg not referenced in package.json)
- `class` keyword not syntax highlighted in TextMate grammar
- Completion trigger thresholds: `let` requires 2+ chars, `new` requires 1+ char before completions appear (args work immediately)
- `say` function injected with 1 param but dialogue lowering generates 2-arg calls — pre-existing compiler bug (FIXED by runtime agent)

## Gaps

- truth: "Diagnostics appear for parse errors at all scopes (top-level and inside functions)"
  status: failed
  reason: "User reported: Only inside of functions squiggle appears"
  severity: minor
  test: 4
  root_cause: "Zero-width diagnostic spans at end-of-input. Parser recovery consumes everything (especially for entities where fn is valid member), errors at EOF with span N..N. VS Code cannot render zero-width squiggles."
  artifacts:
    - path: "writ-lsp/src/convert.rs"
      issue: "parse_error_to_diag does not expand zero-width spans"
    - path: "writ-parser/src/parser.rs"
      issue: "Item-level recovery produces zero-width error spans for entities"
  missing:
    - "Expand zero-width spans in parse_error_to_diag to cover last non-empty line or previous token"
  debug_session: ".planning/debug/top-level-diagnostics.md"

- truth: "Cross-file diagnostics work when writ.toml project file is present"
  status: fixed
  reason: "User reported: No output cross-files occurs when a writ.toml is present, either workspace root or subfolder"
  severity: major
  test: 6
  root_cause: "analyze_project silently excluded trigger file if not in src/ directory. Also read from disk instead of editor buffer."
  artifacts:
    - path: "writ-lsp/src/analysis_host.rs"
      issue: "trigger file excluded from source list; disk read instead of buffer"
    - path: "writ-lsp/src/backend.rs"
      issue: "did not pass editor buffer to analyze_project"
  missing: []
  debug_session: ".planning/debug/writ-toml-project-mode.md"

- truth: "Hover tooltips work regardless of writ.toml presence; variables show correct types"
  status: partially-fixed
  reason: "User reported: tooltips appear if no writ.toml is present. If present, no tooltips appear. All variables report as void during declaration phase"
  severity: major
  test: 7
  root_cause: "writ.toml issue FIXED (same as test 6). Variable void: expr_at_offset only walks TypedExpr nodes. Variable declarations/params are TypedStmt fields, not expressions. Cursor falls through to enclosing Block which has type void."
  artifacts:
    - path: "writ-lsp/src/queries.rs"
      issue: "expr_at_offset and find_in_stmt do not check Let.name_span, For.binding_span, or fn param spans"
    - path: "writ-lsp/src/backend.rs"
      issue: "hover handler has no fallback for statement/parameter bindings"
  missing:
    - "Add binding_at_offset function checking Let.name_span, For.binding_span, fn param spans"
    - "Use as fallback in hover handler when expr_at_offset returns void block"
  debug_session: ".planning/debug/variable-void-types.md"

- truth: "Go-to-definition works on variable type references"
  status: failed
  reason: "User reported: Works on new X but not on variable types"
  severity: major
  test: 8
  root_cause: "TypedStmt::Let discards type annotation spans during type checking. CST has Spanned<TypeExpr> but TypedIR only stores resolved Ty (interned ID). expr_at_offset never walks type annotations."
  artifacts:
    - path: "writ-compiler/src/check/ir.rs"
      issue: "TypedStmt::Let has no type_ann_span or type_ann_def_id field"
    - path: "writ-lsp/src/queries.rs"
      issue: "find_in_stmt for Let only recurses into value expression"
  missing:
    - "Build side-table mapping type-annotation spans to DefIds during type checking"
    - "Or add type_ann_span/type_ann_def_id to TypedStmt::Let, fn params, struct fields"
  debug_session: ".planning/debug/goto-def-references-gaps.md"

- truth: "Find all references works from declaration site"
  status: failed
  reason: "User reported: Works at actual usage position (call) but not at declaration (fn ... () ...)"
  severity: minor
  test: 9
  root_cause: "Declaration names exist only in DefMap as DefEntry.name_span, not as TypedExpr nodes. expr_at_offset returns None when cursor is on a declaration name."
  artifacts:
    - path: "writ-lsp/src/queries.rs"
      issue: "expr_at_offset never checks DefEntry.name_span values"
    - path: "writ-lsp/src/backend.rs"
      issue: "references handler depends entirely on expr_at_offset with no fallback"
  missing:
    - "Add def_at_offset(def_map, offset) scanning DefEntry.name_span values as fallback"
  debug_session: ".planning/debug/goto-def-references-gaps.md"

- truth: "Signature help popup appears when typing function call parenthesis"
  status: failed
  reason: "User reported: fail"
  severity: major
  test: 12
  root_cause: "signature_help depends on find_enclosing_call finding a Call node in cached AST, but when ( is typed the AST is either stale (pre-() or broken (parser requires closing )). Handler never finds a Call node during real editing."
  artifacts:
    - path: "writ-lsp/src/backend.rs"
      issue: "signature_help reads stale analysis_cache"
    - path: "writ-lsp/src/queries.rs"
      issue: "build_signature_help depends on find_enclosing_call which needs complete Call node"
    - path: "writ-parser/src/parser.rs"
      issue: "args parser requires closing ) via delimited_by"
  missing:
    - "Text-based callee resolution: backward scan from ( to extract callee name, look up in DefMap/TypeEnv"
    - "Or: insert closing ) and re-analyze (like dot-completion strips dot)"
  debug_session: ".planning/debug/signature-help-broken.md"

- truth: "Entity names highlighted with distinct color from struct names"
  status: failed
  reason: "User reported: Both share the same color"
  severity: cosmetic
  test: 13
  root_cause: "semanticTokenScopes maps entity to entity.name.type.writ which is child scope of entity.name.type (VS Code's built-in type fallback). All themes color both the same via prefix matching."
  artifacts:
    - path: "writ-vscode/package.json"
      issue: "semanticTokenScopes maps all custom tokens into entity.name.type.* family"
  missing:
    - "Change scope mappings to distinct families (e.g., support.class.writ for entity)"
    - "Add configurationDefaults with semantic token color overrides for custom types"
  debug_session: ".planning/debug/entity-semantic-color.md"

- truth: "Debugger can launch and execute .writ files"
  status: fixed
  reason: "User reported: Runtime fails to execute files: non-pub main reports main not found; dialogue builtins cause bad opcode"
  severity: blocker
  test: 16
  root_cause: "Three bugs: (1) non-pub main excluded from export_defs, (2) lambda closures produced corrupt binary due to DefId=None matching, (3) say/say_localized arity mismatch between lowering (2-3 args) and type checker (1-2 params)"
  artifacts:
    - path: "writ-cli/src/main.rs"
      issue: "No fallback for non-pub main"
    - path: "writ-dap/src/server.rs"
      issue: "No fallback for non-pub main"
    - path: "writ-compiler/src/emit/serialize.rs"
      issue: "Lambda body serialization matched by DefId which is None for lambdas"
    - path: "writ-compiler/src/lower/dialogue.rs"
      issue: "say() lowered with 2 args but type checker expects 1"
  missing: []
  debug_session: ".planning/debug/runtime-execution-failures.md"

---
phase: 63-writ-compiler-file-splits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-compiler/src/check/check_expr/mod.rs
  - writ-compiler/src/check/check_expr/ident.rs
  - writ-compiler/src/check/check_expr/path.rs
  - writ-compiler/src/check/check_expr/binary.rs
  - writ-compiler/src/check/check_expr/call.rs
  - writ-compiler/src/check/check_expr/control.rs
  - writ-compiler/src/check/check_expr/access.rs
  - writ-compiler/src/check/check_expr/match_.rs
  - writ-compiler/src/check/check_expr/lambda.rs
  - writ-compiler/src/check/check_expr/construction.rs
  - writ-compiler/src/check/mod.rs
autonomous: true
requirements: [SPLIT-03]

must_haves:
  truths:
    - "check_expr.rs (2134 lines) is replaced by a check_expr/ folder module with 10 files, none exceeding 500 lines"
    - "All typecheck tests pass without modification after the split"
    - "No pub use * glob re-exports exist in any new file"
  artifacts:
    - path: "writ-compiler/src/check/check_expr/mod.rs"
      provides: "CheckCtx struct, check_expr dispatch, check_block, check_block_stmts, check_assignment_mutability, find_root_binding, find_fn_def_id"
      contains: "pub struct CheckCtx"
    - path: "writ-compiler/src/check/check_expr/ident.rs"
      provides: "check_ident function"
      contains: "pub(super) fn check_ident"
    - path: "writ-compiler/src/check/check_expr/call.rs"
      provides: "check_call, check_call_with_sig, check_contract_bounds, check_generic_call"
      contains: "pub(super) fn check_call"
    - path: "writ-compiler/src/check/check_expr/binary.rs"
      provides: "check_binary, check_unary_prefix"
      contains: "pub(super) fn check_binary"
  key_links:
    - from: "writ-compiler/src/check/check_expr/mod.rs"
      to: "writ-compiler/src/check/check_expr/ident.rs"
      via: "mod ident; use ident::check_ident;"
      pattern: "mod ident"
    - from: "writ-compiler/src/check/mod.rs"
      to: "writ-compiler/src/check/check_expr/mod.rs"
      via: "pub mod check_expr"
      pattern: "pub mod check_expr"
---

<objective>
Split writ-compiler/src/check/check_expr.rs (2,134 lines) into a check_expr/ folder module with 10 focused subfiles, each under 500 lines.

Purpose: SPLIT-03 — the largest file in writ-compiler becomes navigable with single-responsibility submodules (ident, path, binary, call, control, access, match, lambda, construction).
Output: check_expr/ directory with mod.rs + 9 submodule files. Original check_expr.rs deleted.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-writ-compiler-file-splits/63-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From writ-compiler/src/check/check_expr.rs (lines 18-32):
```rust
pub struct CheckCtx<'def> {
    pub interner: TyInterner,
    pub diags: Vec<Diagnostic>,
    pub def_map: &'def DefMap,
    pub type_env: &'def TypeEnv,
    pub unify: UnifyCtx,
    pub local_env: LocalEnv,
    pub current_fn_ret: Option<Ty>,
    pub current_file: FileId,
    pub self_type: Option<Ty>,
    pub current_namespace: String,
}
```

External callers of check_expr module (from check/mod.rs line 52):
```rust
let mut ctx = check_expr::CheckCtx { ... };
```
From check_decl.rs, check_stmt.rs, desugar.rs — all use `super::check_expr::{check_expr, CheckCtx}` or similar.

Public API that MUST remain accessible at `check_expr::` path:
- `CheckCtx` struct
- `check_expr` function
- `check_block_stmts` function
- `check_assignment_mutability` function
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create check_expr/ folder module by splitting check_expr.rs into 10 focused subfiles</name>
  <files>
    writ-compiler/src/check/check_expr.rs (DELETE after split)
    writ-compiler/src/check/check_expr/mod.rs (NEW)
    writ-compiler/src/check/check_expr/ident.rs (NEW)
    writ-compiler/src/check/check_expr/path.rs (NEW)
    writ-compiler/src/check/check_expr/binary.rs (NEW)
    writ-compiler/src/check/check_expr/call.rs (NEW)
    writ-compiler/src/check/check_expr/control.rs (NEW)
    writ-compiler/src/check/check_expr/access.rs (NEW)
    writ-compiler/src/check/check_expr/match_.rs (NEW)
    writ-compiler/src/check/check_expr/lambda.rs (NEW)
    writ-compiler/src/check/check_expr/construction.rs (NEW)
  </files>
  <read_first>
    writ-compiler/src/check/check_expr.rs
    writ-compiler/src/check/mod.rs
    writ-compiler/src/check/check_decl.rs
    writ-compiler/src/check/check_stmt.rs
    writ-compiler/src/check/desugar.rs
  </read_first>
  <action>
Split check_expr.rs into a check_expr/ folder module. The procedure:

1. **Create directory** `writ-compiler/src/check/check_expr/`

2. **Create `check_expr/mod.rs`** containing:
   - ALL `use` imports from the top of check_expr.rs (lines 1-16)
   - Submodule declarations: `mod ident; mod path; mod binary; mod call; mod control; mod access; mod match_; mod lambda; mod construction;`
   - `use` statements to import `pub(super)` items from each submodule into scope so the `check_expr` dispatch function can call them: `use ident::check_ident; use path::check_path; use binary::{check_binary, check_unary_prefix}; use call::check_call; use control::check_if; use access::{check_member_access, check_bracket_access}; use match_::{check_match, check_pattern}; use lambda::check_lambda; use construction::{check_new_construction, check_array_lit};`
   - `CheckCtx` struct definition + `impl CheckCtx` block (lines 18-50)
   - `pub fn check_expr` dispatch function (lines 53-351)
   - `fn check_block` (lines 1277-1287) — private, called by check_expr dispatch and by check_if
   - `pub fn check_block_stmts` (lines 1288-1319) — public, called from check_stmt
   - `pub fn check_assignment_mutability` (lines 2061-2086) — public, called from check_stmt
   - `fn find_root_binding` (lines 2089-2101) — private helper for check_assignment_mutability
   - `fn find_fn_def_id` (lines 2104-2134) — private helper for check_call (used via `super::find_fn_def_id` from call.rs, so make it `pub(super)`)

3. **Create `check_expr/ident.rs`** containing:
   - `use super::*;` to import types from mod.rs's namespace (CheckCtx, TypedExpr, etc.)
   - Or explicit imports: `use super::CheckCtx;` + `use crate::check::ir::*;` + `use crate::check::ty::{Ty, TyKind};` + `use crate::resolve::def_map::{DefId, DefKind};` + `use super::super::error::TypeError;` + `use writ_diagnostics::FileId;` + `use chumsky::span::SimpleSpan;`
   - `pub(super) fn check_ident(ctx: &mut CheckCtx, name: &str, span: SimpleSpan) -> TypedExpr` (lines 353-468)
   - Avoid `use super::*;` — use explicit imports only (per phase requirement: no `pub use *` re-exports)

4. **Create `check_expr/path.rs`** containing:
   - Explicit imports from super and crate
   - `pub(super) fn check_path(ctx: &mut CheckCtx, segments: &[String], span: SimpleSpan) -> TypedExpr` (lines 469-583)

5. **Create `check_expr/binary.rs`** containing:
   - `pub(super) fn check_binary(ctx: &mut CheckCtx, left: &AstExpr, op: &BinaryOp, right: &AstExpr, span: SimpleSpan) -> TypedExpr` (lines 584-713)
   - `pub(super) fn check_unary_prefix(ctx: &mut CheckCtx, op: &PrefixOp, expr: &AstExpr, span: SimpleSpan) -> TypedExpr` (lines 714-787)

6. **Create `check_expr/call.rs`** containing:
   - `pub(super) fn check_call(ctx: &mut CheckCtx, callee: &AstExpr, args: &[AstExpr], span: SimpleSpan) -> TypedExpr` (lines 788-974)
   - `pub(super) fn check_call_with_sig(...)` (lines 975-1054)
   - `pub(super) fn check_contract_bounds(...)` (lines 1055-1114)
   - `pub(super) fn check_generic_call(...)` (lines 1115-1210)
   - NOTE: check_call calls `super::check_expr` for recursive checking and `super::find_fn_def_id` for name lookup

7. **Create `check_expr/control.rs`** containing:
   - `pub(super) fn check_if(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 1211-1276)
   - NOTE: check_if calls `super::check_expr` for condition/branches and `super::check_block` for block bodies

8. **Create `check_expr/access.rs`** containing:
   - `pub(super) fn check_member_access(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 1324-1451)
   - `pub(super) fn check_bracket_access(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 1456-1591)

9. **Create `check_expr/match_.rs`** (trailing underscore to avoid Rust keyword) containing:
   - `pub(super) fn check_match(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 1596-1667)
   - `pub(super) fn check_pattern(ctx: &mut CheckCtx, ...) -> TypedPattern` (lines 1668-1815)

10. **Create `check_expr/lambda.rs`** containing:
    - `pub(super) fn check_lambda(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 1821-1895)

11. **Create `check_expr/construction.rs`** containing:
    - `pub(super) fn check_new_construction(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 1900-2004)
    - `pub(super) fn check_array_lit(ctx: &mut CheckCtx, ...) -> TypedExpr` (lines 2009-2055)

12. **Delete** the original `writ-compiler/src/check/check_expr.rs` file.

13. **No changes needed to check/mod.rs** — it already declares `pub mod check_expr;` which will now resolve to the `check_expr/` directory.

**Visibility rules:**
- ALL functions moved to submodules that are called from mod.rs's `check_expr` dispatch or from sibling submodules: use `pub(super)`
- `CheckCtx`, `check_expr`, `check_block_stmts`, `check_assignment_mutability` remain `pub` (they were already public)
- `check_block` remains private in mod.rs (only used within check_expr module tree — called from control.rs via `super::check_block`)
- `find_fn_def_id` becomes `pub(super)` in mod.rs (called from call.rs via `super::find_fn_def_id`)

**Import pattern for submodules:** Each submodule imports from `super::` for CheckCtx and other mod.rs items, and from `crate::` for types from other modules (ast, resolve, etc.). Do NOT use `use super::*;`.

**Cross-submodule calls:** Submodules that need to call `check_expr` (e.g., call.rs, control.rs, access.rs, construction.rs) use `super::check_expr`. Submodules that need `check_block` use `super::check_block`.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `writ-compiler/src/check/check_expr.rs` does NOT exist
    - Directory `writ-compiler/src/check/check_expr/` exists with 10 .rs files
    - `writ-compiler/src/check/check_expr/mod.rs` contains `pub struct CheckCtx`
    - `writ-compiler/src/check/check_expr/mod.rs` contains `pub fn check_expr`
    - `writ-compiler/src/check/check_expr/mod.rs` contains `pub fn check_block_stmts`
    - `writ-compiler/src/check/check_expr/mod.rs` contains `pub fn check_assignment_mutability`
    - `writ-compiler/src/check/check_expr/ident.rs` contains `pub(super) fn check_ident`
    - `writ-compiler/src/check/check_expr/path.rs` contains `pub(super) fn check_path`
    - `writ-compiler/src/check/check_expr/binary.rs` contains `pub(super) fn check_binary`
    - `writ-compiler/src/check/check_expr/binary.rs` contains `pub(super) fn check_unary_prefix`
    - `writ-compiler/src/check/check_expr/call.rs` contains `pub(super) fn check_call`
    - `writ-compiler/src/check/check_expr/call.rs` contains `pub(super) fn check_generic_call`
    - `writ-compiler/src/check/check_expr/control.rs` contains `pub(super) fn check_if`
    - `writ-compiler/src/check/check_expr/access.rs` contains `pub(super) fn check_member_access`
    - `writ-compiler/src/check/check_expr/access.rs` contains `pub(super) fn check_bracket_access`
    - `writ-compiler/src/check/check_expr/match_.rs` contains `pub(super) fn check_match`
    - `writ-compiler/src/check/check_expr/match_.rs` contains `pub(super) fn check_pattern`
    - `writ-compiler/src/check/check_expr/lambda.rs` contains `pub(super) fn check_lambda`
    - `writ-compiler/src/check/check_expr/construction.rs` contains `pub(super) fn check_new_construction`
    - `writ-compiler/src/check/check_expr/construction.rs` contains `pub(super) fn check_array_lit`
    - No file in check_expr/ contains `pub use` followed by `*`
    - `cargo test -p writ-compiler` exits 0 with all tests passing
    - `cargo clippy -p writ-compiler` exits with zero warnings
  </acceptance_criteria>
  <done>check_expr.rs (2,134 lines) replaced by check_expr/ folder module with 10 files, each under 500 lines. All typecheck tests pass. No glob re-exports.</done>
</task>

</tasks>

<verification>
1. `cargo test -p writ-compiler` — all tests pass
2. `cargo clippy -p writ-compiler` — zero warnings
3. `wc -l writ-compiler/src/check/check_expr/*.rs` — no file exceeds 500 lines
4. `grep -r "pub use.*\*" writ-compiler/src/check/check_expr/` — returns empty
</verification>

<success_criteria>
- check_expr.rs no longer exists as a single file
- check_expr/ directory has 10 .rs files (mod.rs + 9 submodules)
- No file exceeds 500 lines
- All writ-compiler tests pass without modification
- Zero clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/63-writ-compiler-file-splits/63-01-SUMMARY.md`
</output>

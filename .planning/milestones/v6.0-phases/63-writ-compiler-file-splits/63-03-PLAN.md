---
phase: 63-writ-compiler-file-splits
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-compiler/src/emit/body/expr/mod.rs
  - writ-compiler/src/emit/body/expr/literal.rs
  - writ-compiler/src/emit/body/expr/binary.rs
  - writ-compiler/src/emit/body/expr/control.rs
  - writ-compiler/src/emit/body/expr/construction.rs
  - writ-compiler/src/emit/body/expr/builtins.rs
  - writ-compiler/src/emit/body/expr/string.rs
  - writ-compiler/src/emit/body/expr/eq.rs
  - writ-compiler/src/emit/body/mod.rs
  - writ-compiler/src/check/env.rs
  - writ-compiler/src/check/env_build.rs
autonomous: true
requirements: [SPLIT-05, SPLIT-09]

must_haves:
  truths:
    - "emit/body/expr.rs (1,470 lines) is replaced by an expr/ folder module with 8 files, none exceeding 500 lines"
    - "check/env.rs (1,032 lines) is split into env.rs (~300 lines of public types) + env_build.rs (~720 lines of private helpers)"
    - "All emit body tests and typecheck tests pass without modification"
    - "No pub use * glob re-exports exist in any new file"
  artifacts:
    - path: "writ-compiler/src/emit/body/expr/mod.rs"
      provides: "emit_expr dispatch function"
      contains: "pub fn emit_expr"
    - path: "writ-compiler/src/emit/body/expr/binary.rs"
      provides: "emit_binary function"
      contains: "pub(super) fn emit_binary"
    - path: "writ-compiler/src/check/env.rs"
      provides: "TypeEnv, FnSig, LocalEnv, Mutability — public type definitions"
      contains: "pub struct TypeEnv"
    - path: "writ-compiler/src/check/env_build.rs"
      provides: "Private builder helpers: build_fn_sig, build_struct_fields, resolve_ast_type, find_* functions"
      contains: "pub(super) fn build_fn_sig"
  key_links:
    - from: "writ-compiler/src/emit/body/expr/mod.rs"
      to: "writ-compiler/src/emit/body/expr/binary.rs"
      via: "mod binary; use binary::emit_binary;"
      pattern: "mod binary"
    - from: "writ-compiler/src/check/env.rs"
      to: "writ-compiler/src/check/env_build.rs"
      via: "mod env_build;"
      pattern: "mod env_build"
---

<objective>
Split writ-compiler/src/emit/body/expr.rs (1,470 lines) into an expr/ folder module with 8 focused subfiles. Additionally, split check/env.rs (1,032 lines) into env.rs (public types) + env_build.rs (private builder helpers).

Purpose: SPLIT-05 (expr.rs emission split) + SPLIT-09 (env.rs type/builder separation). Both files get clear single-responsibility submodules.
Output: expr/ directory with mod.rs + 7 submodule files. env_build.rs sibling file. Original expr.rs deleted.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-writ-compiler-file-splits/63-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From writ-compiler/src/emit/body/expr.rs — external API consumed by body/mod.rs:
```rust
pub fn emit_expr(emitter: &mut BodyEmitter<'_>, expr: &TypedExpr) -> u16
```

From writ-compiler/src/emit/body/mod.rs — BodyEmitter used by expr.rs submodules:
```rust
pub struct BodyEmitter<'a> { ... }
// Methods: emit(), alloc_reg(), alloc_void_reg(), new_label(), mark_label_here(), add_fixup(), etc.
```

From writ-compiler/src/check/env.rs — public API:
```rust
pub struct FnSig { pub name, pub params, pub ret, pub generics, pub self_param, pub bounds }
pub struct EnumVariantSig { pub name, pub fields }
pub struct ImplEntry { pub impl_def_id, pub contract_def_id, pub methods }
pub struct TypeEnv { ... 15+ fields ... }
pub struct LocalEnv { ... }
pub enum Mutability { Mutable, Immutable }
pub fn resolve_ast_type(...) -> Ty
pub fn resolve_ast_type_with_file(...) -> Ty
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create emit/body/expr/ folder module by splitting expr.rs into 8 focused subfiles</name>
  <files>
    writ-compiler/src/emit/body/expr.rs (DELETE after split)
    writ-compiler/src/emit/body/expr/mod.rs (NEW)
    writ-compiler/src/emit/body/expr/literal.rs (NEW)
    writ-compiler/src/emit/body/expr/binary.rs (NEW)
    writ-compiler/src/emit/body/expr/control.rs (NEW)
    writ-compiler/src/emit/body/expr/construction.rs (NEW)
    writ-compiler/src/emit/body/expr/builtins.rs (NEW)
    writ-compiler/src/emit/body/expr/string.rs (NEW)
    writ-compiler/src/emit/body/expr/eq.rs (NEW)
    writ-compiler/src/emit/body/mod.rs (MODIFY — update `pub mod expr;` remains valid)
  </files>
  <read_first>
    writ-compiler/src/emit/body/expr.rs
    writ-compiler/src/emit/body/mod.rs
    writ-compiler/src/emit/body/call.rs
    writ-compiler/src/emit/body/stmt.rs
  </read_first>
  <action>
Split emit/body/expr.rs into an expr/ folder module. The procedure:

1. **Create directory** `writ-compiler/src/emit/body/expr/`

2. **Create `expr/mod.rs`** containing:
   - Imports: `use writ_module::instruction::Instruction;`, `use crate::ast::expr::{BinaryOp, PrefixOp};`, `use crate::check::ir::{TypedExpr, TypedLiteral};`, `use crate::check::ty::{Ty, TyKind};`, `use super::BodyEmitter;`, `use super::call::{emit_call_indirect, pack_args_consecutive};`
   - Submodule declarations: `mod literal; mod binary; mod control; mod construction; mod builtins; mod string; mod eq;`
   - `use` statements importing pub(super) items from submodules: `use literal::emit_literal; use binary::emit_binary; use control::{emit_if, emit_spawn, emit_defer}; use construction::{emit_range, emit_array_lit, emit_new}; use builtins::try_emit_builtin_method; use string::{try_collect_str_build_parts, emit_str_build}; use eq::{emit_struct_eq, emit_struct_neq};`
   - `pub fn emit_expr(emitter: &mut BodyEmitter<'_>, expr: &TypedExpr) -> u16` — the dispatch function (lines 21-396)
   - NOTE: the dispatch uses `super::stmt::emit_stmt` inline (line 89) — keep that `use` inline or import it at the top

3. **Create `expr/literal.rs`** containing:
   - `use writ_module::instruction::Instruction;`, `use crate::check::ir::TypedLiteral;`, `use crate::check::ty::Ty;`, `use super::super::BodyEmitter;`
   - `pub(super) fn emit_literal(emitter: &mut BodyEmitter<'_>, ty: Ty, value: &TypedLiteral) -> u16` (lines 399-434)

4. **Create `expr/binary.rs`** containing:
   - Imports for BinaryOp, PrefixOp, Instruction, BodyEmitter, TypedExpr, Ty, TyKind, emit_expr
   - `pub(super) fn emit_binary(emitter: &mut BodyEmitter<'_>, left: &TypedExpr, op: &BinaryOp, right: &TypedExpr, ty: Ty, span: ...) -> u16` (lines 438-685)
   - NOTE: emit_binary calls `super::emit_expr` for left/right operands, and calls `super::eq::emit_struct_eq`/`emit_struct_neq` for structural equality

5. **Create `expr/control.rs`** containing:
   - `pub(super) fn emit_if(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 688-738 approximately)
   - `pub(super) fn emit_spawn(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 749-805)
   - `pub(super) fn emit_defer(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 806-844)
   - NOTE: emit_if calls `super::emit_expr` for condition/branches

6. **Create `expr/construction.rs`** containing:
   - `pub(super) fn emit_range(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 858-911)
   - `pub(super) fn emit_array_lit(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 912-943)
   - `pub(super) fn emit_new(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 1158-1235)
   - NOTE: these call `super::emit_expr` and `super::super::call::pack_args_consecutive`

7. **Create `expr/builtins.rs`** containing:
   - `pub(super) fn try_emit_builtin_method(emitter: &mut BodyEmitter<'_>, expr: &TypedExpr) -> Option<u16>` (lines 944-1157)
   - This function handles Option/Result/Array builtin method shortcuts
   - Calls `super::emit_expr` for arguments

8. **Create `expr/string.rs`** containing:
   - `pub(super) fn try_collect_str_build_parts<'a>(expr: &'a TypedExpr, interner: &TyInterner) -> Option<Vec<&'a TypedExpr>>` (lines 1247-1263)
   - `fn collect_string_chain<'a>(...)` (lines 1264-1287) — private helper
   - `pub(super) fn emit_str_build(emitter: &mut BodyEmitter<'_>, ty: Ty, parts: &[&TypedExpr]) -> u16` (lines 1288-1315)
   - Also include the `fn extract_ty_def_id` and `fn extract_def_id_from_call` helper functions (lines 1301-1315) if they exist as separate functions — check the actual source

9. **Create `expr/eq.rs`** containing:
   - `pub(super) fn emit_struct_eq(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 1325-1375)
   - `pub(super) fn emit_struct_neq(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 1376-1433)
   - `fn emit_field_eq(emitter: &mut BodyEmitter<'_>, ...) -> u16` (lines 1434-end) — private helper

10. **Delete** the original `writ-compiler/src/emit/body/expr.rs` file.

11. **Update `emit/body/mod.rs`** — it already has `pub mod expr;` which will resolve to `expr/` directory. Verify no other changes needed. The `expr::emit_expr` path used by mod.rs's `emit_all_bodies` remains valid.

**Visibility rules:**
- `emit_expr` remains `pub` (called from body/mod.rs)
- All submodule functions become `pub(super)` (called from mod.rs's dispatch or from sibling submodules)
- Private helpers within submodules (like `collect_string_chain`, `emit_field_eq`) stay `fn` (no visibility modifier needed beyond default private)

**Cross-submodule calls:** binary.rs calls `super::emit_expr` and `super::eq::{emit_struct_eq, emit_struct_neq}`. control.rs/construction.rs/builtins.rs call `super::emit_expr`. Construction calls `super::super::call::pack_args_consecutive`.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `writ-compiler/src/emit/body/expr.rs` does NOT exist
    - Directory `writ-compiler/src/emit/body/expr/` exists with 8 .rs files
    - `writ-compiler/src/emit/body/expr/mod.rs` contains `pub fn emit_expr`
    - `writ-compiler/src/emit/body/expr/literal.rs` contains `pub(super) fn emit_literal`
    - `writ-compiler/src/emit/body/expr/binary.rs` contains `pub(super) fn emit_binary`
    - `writ-compiler/src/emit/body/expr/control.rs` contains `pub(super) fn emit_if`
    - `writ-compiler/src/emit/body/expr/control.rs` contains `pub(super) fn emit_spawn`
    - `writ-compiler/src/emit/body/expr/construction.rs` contains `pub(super) fn emit_range`
    - `writ-compiler/src/emit/body/expr/construction.rs` contains `pub(super) fn emit_new`
    - `writ-compiler/src/emit/body/expr/builtins.rs` contains `pub(super) fn try_emit_builtin_method`
    - `writ-compiler/src/emit/body/expr/string.rs` contains `pub(super) fn try_collect_str_build_parts`
    - `writ-compiler/src/emit/body/expr/string.rs` contains `pub(super) fn emit_str_build`
    - `writ-compiler/src/emit/body/expr/eq.rs` contains `pub(super) fn emit_struct_eq`
    - `writ-compiler/src/emit/body/expr/eq.rs` contains `pub(super) fn emit_struct_neq`
    - No file in expr/ contains `pub use` followed by `*`
    - `cargo test -p writ-compiler` exits 0 with all tests passing
    - `cargo clippy -p writ-compiler` exits with zero warnings
  </acceptance_criteria>
  <done>emit/body/expr.rs (1,470 lines) replaced by expr/ folder module with 8 files, each under 500 lines. All emit tests pass. No glob re-exports.</done>
</task>

<task type="auto">
  <name>Task 2: Split check/env.rs into public types (env.rs) and private builder helpers (env_build.rs)</name>
  <files>
    writ-compiler/src/check/env.rs (MODIFY — trim to ~300 lines)
    writ-compiler/src/check/env_build.rs (NEW — ~720 lines of builder helpers)
  </files>
  <read_first>
    writ-compiler/src/check/env.rs
    writ-compiler/src/check/mod.rs
    writ-compiler/src/check/check_expr.rs
    writ-compiler/src/check/check_decl.rs
  </read_first>
  <action>
Split check/env.rs using the sibling-file pattern. The file has two distinct halves:
- Lines 1-280: Public type definitions (FnSig, EnumVariantSig, ImplEntry, TypeEnv, TypeEnv::build) + LocalEnv (lines 984-1032)
- Lines 281-982: Private AST-lookup helpers (find_*_decl) and builder helpers (build_fn_sig, build_struct_fields, resolve_ast_type, etc.)

Procedure:

1. **Create `check/env_build.rs`** (NEW) containing ALL private helper functions currently in env.rs lines 281-982:
   - `pub(super) fn decl_def_id(decl: &ResolvedDecl) -> DefId` (line 281)
   - `pub(super) fn find_fn_decl<'a>(...)` (line 305)
   - `pub(super) fn find_struct_decl<'a>(...)` (line 320)
   - `pub(super) fn find_entity_decl<'a>(...)` (line 338)
   - `pub(super) fn find_enum_decl<'a>(...)` (line 356)
   - `pub(super) fn find_contract_decl<'a>(...)` (line 371)
   - `pub(super) fn find_impl_decl<'a>(...)` (line 389)
   - `pub(super) fn find_component_decl<'a>(...)` (line 404)
   - `pub(super) fn find_extern_fn_sig<'a>(...)` (line 431)
   - `pub(super) fn find_extern_struct_decl<'a>(...)` (line 446)
   - `pub(super) fn find_class_decl<'a>(...)` (line 464)
   - `pub(super) fn find_extern_class_decl<'a>(...)` (line 482)
   - `pub(super) fn find_const_decl<'a>(...)` (line 500)
   - `pub(super) fn find_global_decl<'a>(...)` (line 515)
   - `pub(super) fn build_generic_map(...)` (line 537)
   - `pub(super) fn resolve_ast_type(...)` (line 550) — currently `pub fn`, keep it `pub` since it's used outside check module
   - `pub(super) fn resolve_ast_type_with_file(...)` (line 562) — currently `pub fn`, keep it `pub`
   - `fn resolve_named_def_id(...)` (line 572) — private
   - `fn def_id_to_ty(...)` (line 590) — private
   - `fn resolve_ast_type_inner(...)` (line 601) — private
   - `pub(super) fn build_fn_sig(...)` (line 694)
   - `pub(super) fn build_fn_sig_from_ast_sig(...)` (line 733)
   - `pub(super) fn build_generic_bounds(...)` (line 772)
   - `pub(super) fn build_struct_fields(...)` (line 790)
   - `pub(super) fn build_entity_fields(...)` (line 807)
   - `pub(super) fn build_enum_variants(...)` (line 823)
   - `pub(super) fn build_contract_methods(...)` (line 852)
   - `pub(super) fn build_impl_entry(...)` (line 892)
   - `pub(super) fn build_component_fields(...)` (line 965)

   Imports at top of env_build.rs: `use chumsky::span::SimpleSpan;`, `use rustc_hash::FxHashMap;`, `use crate::ast::decl::*;`, `use crate::ast::types::AstType;`, `use crate::ast::Ast;`, `use crate::resolve::def_map::{DefEntry, DefId, DefKind, DefMap};`, `use crate::resolve::ir::NameResolvedAst;`, `use super::ty::{Ty, TyInterner, TyKind};`, `use writ_diagnostics::{Diagnostic, FileId};`, and `use super::env::{FnSig, EnumVariantSig, ImplEntry, TypeEnv};` as needed.

2. **Trim `check/env.rs`** to contain ONLY:
   - Module declaration: `mod env_build;`
   - All existing imports at the top
   - `pub struct FnSig { ... }` (lines 21-30)
   - `pub struct EnumVariantSig { ... }` (lines 34-37)
   - `pub struct ImplEntry { ... }` (lines 41-45)
   - `pub struct TypeEnv { ... }` (lines 49-60)
   - `impl TypeEnv { pub fn build(...) }` (lines 62-280) — this calls the env_build:: helpers
   - `pub struct LocalEnv { ... }` (lines 984-988)
   - `pub enum Mutability { ... }` (lines 990-993)
   - `impl Default for LocalEnv { ... }` (lines 995-1001)
   - `impl LocalEnv { ... }` (lines 1001-1032)
   - Re-export public items from env_build: `pub use env_build::{resolve_ast_type, resolve_ast_type_with_file};`

   Inside `TypeEnv::build()`, replace all direct calls to helper functions with `env_build::` prefixed calls:
   - `decl_def_id(...)` becomes `env_build::decl_def_id(...)`
   - `find_fn_decl(...)` becomes `env_build::find_fn_decl(...)`
   - `build_fn_sig(...)` becomes `env_build::build_fn_sig(...)`
   - etc.

3. **Update `check/mod.rs`** — add `mod env_build;` declaration? No — env_build is a submodule of env (declared inside env.rs as `mod env_build;`), so it's accessed at `check::env::env_build`. The `check/mod.rs` needs NO changes since it already has `pub mod env;`.

   Wait — actually, for the Rust module system: `env.rs` declaring `mod env_build;` means Rust looks for `check/env/env_build.rs` OR `check/env_build.rs` (in newer editions). Since the project uses Rust 2024 edition, check the path resolution. In Rust 2018+, if `env.rs` is a file (not a directory), then `mod env_build;` looks for `env/env_build.rs`. But `env.rs` is currently a sibling file in the `check/` directory. So declaring `mod env_build;` inside `env.rs` will look for `check/env/env_build.rs`.

   **CORRECTION**: The sibling-file pattern requires that `env_build.rs` lives at `check/env_build.rs`, but `mod env_build;` declared in `check/env.rs` resolves to `check/env/env_build.rs` — this won't work. Instead:

   **Approach A (recommended):** Declare `mod env_build;` in `check/mod.rs` (not inside env.rs). Then env.rs uses `use super::env_build::*;` or explicit imports. env_build.rs lives at `check/env_build.rs`.

   **Approach B:** Convert env.rs to env/mod.rs (folder module). Then `mod env_build;` in env/mod.rs resolves to `env/env_build.rs`.

   Use Approach A — it's simpler and doesn't require creating a new directory:
   - In `check/mod.rs`: add `pub(crate) mod env_build;` after `pub mod env;`
   - In `check/env.rs` TypeEnv::build(): use `super::env_build::build_fn_sig(...)` etc.
   - The `resolve_ast_type` and `resolve_ast_type_with_file` functions remain public: `pub use super::env_build::{resolve_ast_type, resolve_ast_type_with_file};` in env.rs. Or better — just keep them `pub` in env_build.rs and re-export from env.rs.

4. **Update `check/mod.rs`** — add `pub(crate) mod env_build;` declaration.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `writ-compiler/src/check/env_build.rs` exists
    - `writ-compiler/src/check/env.rs` contains `pub struct TypeEnv`
    - `writ-compiler/src/check/env.rs` contains `pub struct FnSig`
    - `writ-compiler/src/check/env.rs` contains `pub struct LocalEnv`
    - `writ-compiler/src/check/env.rs` contains `pub enum Mutability`
    - `writ-compiler/src/check/env_build.rs` contains `fn build_fn_sig`
    - `writ-compiler/src/check/env_build.rs` contains `fn build_struct_fields`
    - `writ-compiler/src/check/env_build.rs` contains `fn find_fn_decl`
    - `writ-compiler/src/check/env_build.rs` contains `fn resolve_ast_type`
    - `writ-compiler/src/check/env_build.rs` contains `fn decl_def_id`
    - `writ-compiler/src/check/mod.rs` contains `mod env_build`
    - `wc -l writ-compiler/src/check/env.rs` shows 350 lines or fewer
    - No file contains `pub use` followed by `*`
    - `cargo test -p writ-compiler` exits 0 with all tests passing
    - `cargo clippy -p writ-compiler` exits with zero warnings
  </acceptance_criteria>
  <done>check/env.rs (1,032 lines) split into env.rs (~300 lines of public types) + env_build.rs (~720 lines of private helpers). All typecheck tests pass. No glob re-exports.</done>
</task>

</tasks>

<verification>
1. `cargo test -p writ-compiler` — all tests pass
2. `cargo clippy -p writ-compiler` — zero warnings
3. `wc -l writ-compiler/src/emit/body/expr/*.rs` — no file exceeds 500 lines
4. `wc -l writ-compiler/src/check/env.rs writ-compiler/src/check/env_build.rs` — env.rs under 350, env_build.rs under 750
5. `grep -r "pub use.*\*" writ-compiler/src/emit/body/expr/ writ-compiler/src/check/env_build.rs` — returns empty
</verification>

<success_criteria>
- emit/body/expr.rs no longer exists as a single file
- expr/ directory has 8 .rs files (mod.rs + 7 submodules)
- env.rs trimmed to ~300 lines, env_build.rs has ~720 lines
- No file exceeds 500 lines
- All writ-compiler tests pass without modification
- Zero clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/63-writ-compiler-file-splits/63-03-SUMMARY.md`
</output>

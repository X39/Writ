---
phase: 63-writ-compiler-file-splits
plan: 04
type: execute
wave: 2
depends_on: [63-01, 63-02, 63-03]
files_modified:
  - writ-compiler/src/emit/module_builder.rs
  - writ-compiler/src/lower/dialogue.rs
  - writ-compiler/src/resolve/resolver.rs
autonomous: true
requirements: [SPLIT-08, SPLIT-10, SPLIT-11]

must_haves:
  truths:
    - "module_builder.rs, dialogue.rs, and resolver.rs each have a documented rationale comment explaining why they were not split"
    - "All workspace tests pass after all 4 plans complete"
    - "cargo clippy --workspace reports zero warnings"
  artifacts:
    - path: "writ-compiler/src/emit/module_builder.rs"
      provides: "ModuleBuilder struct — documented as reviewed, no split needed"
      contains: "SPLIT-08 review"
    - path: "writ-compiler/src/lower/dialogue.rs"
      provides: "Dialogue lowering — documented as reviewed, no split needed"
      contains: "SPLIT-10 review"
    - path: "writ-compiler/src/resolve/resolver.rs"
      provides: "Name resolver pass 2 — documented as reviewed, no split needed"
      contains: "SPLIT-11 review"
  key_links: []
---

<objective>
Document "no split needed" rationale for SPLIT-08 (module_builder.rs, 1,063 lines), SPLIT-10 (dialogue.rs, 858 lines), and SPLIT-11 (resolver.rs, 849 lines). Run full workspace verification to confirm all splits from plans 01-03 plus these reviews leave the codebase clean.

Purpose: Satisfy the SPLIT-08/10/11 requirements which call for "reviewed for split opportunities" — the review conclusion is documented justification for keeping these files intact, plus final workspace-wide verification.
Output: Rationale comments in each file's module-level doc comment. Full test + clippy green confirmation.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-writ-compiler-file-splits/63-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document "no split" rationale for module_builder.rs, dialogue.rs, and resolver.rs</name>
  <files>
    writ-compiler/src/emit/module_builder.rs (MODIFY — add rationale comment)
    writ-compiler/src/lower/dialogue.rs (MODIFY — add rationale comment)
    writ-compiler/src/resolve/resolver.rs (MODIFY — add rationale comment)
  </files>
  <read_first>
    writ-compiler/src/emit/module_builder.rs
    writ-compiler/src/lower/dialogue.rs
    writ-compiler/src/resolve/resolver.rs
  </read_first>
  <action>
Add a structured review comment to the module-level doc comment (`//!` block at the top) of each file. The comment documents the split review conclusion per Phase 63 success criterion 1: "each file has a documented rationale for remaining larger [than 500 lines]."

1. **`writ-compiler/src/emit/module_builder.rs`** — Add the following immediately after the existing `//!` doc comment block (before any `use` statements):

```rust
//!
//! ## SPLIT-08 review (Phase 63)
//!
//! Reviewed for split opportunities at 1,063 lines. Conclusion: no split.
//! `ModuleBuilder` is a single struct with 40+ fields. Its impl block contains
//! constructor, 14 "add" methods (Pass 1), `finalize` (Pass 2), and ~30 query
//! methods (post-finalize). All methods read/write `self` fields directly.
//! Splitting impl blocks across files would add file navigation overhead without
//! reducing complexity — callers still use `builder.add_typedef(...)`,
//! `builder.finalize()`, etc. regardless of file layout.
```

2. **`writ-compiler/src/lower/dialogue.rs`** — Add the following immediately after the existing `//!` doc comment block:

```rust
//!
//! ## SPLIT-10 review (Phase 63)
//!
//! Reviewed for split opportunities at 858 lines. Conclusion: no split.
//! The file implements a single cohesive operation: lowering a `DlgDecl` CST node
//! into an `AstFnDecl`. All internal sections (speaker collection, line lowering,
//! loc-key computation, text lowering, choice lowering, control flow, transition)
//! share `DlgLowerState` and participate in a single-pass transformation. Splitting
//! would create artificial file boundaries in what is a tightly-coupled algorithmic
//! pipeline.
```

3. **`writ-compiler/src/resolve/resolver.rs`** — Add the following immediately after the existing `//!` doc comment block:

```rust
//!
//! ## SPLIT-11 review (Phase 63)
//!
//! Reviewed for split opportunities at 849 lines. Conclusion: no split.
//! The file contains Pass 2 of name resolution: `resolve_bodies`, `process_usings`,
//! `resolve_decl_list` (413 lines — a match on all AstDecl variants), `resolve_ast_type`,
//! and small helpers. `resolve_decl_list` is the core algorithm — extracting individual
//! variant handlers (30-50 lines each) into separate files would fragment the resolver's
//! control flow without clarity gain. The file is under 2x the 500-line target.
```
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && grep -c "SPLIT-08 review" writ-compiler/src/emit/module_builder.rs && grep -c "SPLIT-10 review" writ-compiler/src/lower/dialogue.rs && grep -c "SPLIT-11 review" writ-compiler/src/resolve/resolver.rs</automated>
  </verify>
  <acceptance_criteria>
    - `writ-compiler/src/emit/module_builder.rs` contains the string `SPLIT-08 review`
    - `writ-compiler/src/emit/module_builder.rs` contains the string `no split`
    - `writ-compiler/src/lower/dialogue.rs` contains the string `SPLIT-10 review`
    - `writ-compiler/src/lower/dialogue.rs` contains the string `no split`
    - `writ-compiler/src/resolve/resolver.rs` contains the string `SPLIT-11 review`
    - `writ-compiler/src/resolve/resolver.rs` contains the string `no split`
    - No behavior changes — only doc comments added
  </acceptance_criteria>
  <done>All three "reviewed" files have documented rationale for remaining unsplit. SPLIT-08, SPLIT-10, SPLIT-11 requirements satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Full workspace verification — tests + clippy + line counts</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    writ-compiler/src/check/mod.rs
    writ-compiler/src/emit/mod.rs
    writ-compiler/src/emit/body/mod.rs
  </read_first>
  <action>
Run the complete verification suite to confirm all Phase 63 splits are clean:

1. **Full workspace tests:** `cargo test --workspace`
   - All existing tests must pass without any test file modifications
   - Snapshot tests (insta) must not require review

2. **Clippy zero warnings:** `cargo clippy --workspace`
   - Must exit with zero warnings (baseline established in Phase 62)

3. **Line count verification:** Run `wc -l` on all newly created files:
   - `wc -l writ-compiler/src/check/check_expr/*.rs` — no file over 500
   - `wc -l writ-compiler/src/emit/collect/*.rs` — no file over 500
   - `wc -l writ-compiler/src/emit/body/expr/*.rs` — no file over 500
   - `wc -l writ-compiler/src/check/env.rs writ-compiler/src/check/env_build.rs` — env.rs under 350, env_build.rs under 750
   - `wc -l writ-compiler/src/emit/module_builder.rs writ-compiler/src/lower/dialogue.rs writ-compiler/src/resolve/resolver.rs` — documented as reviewed

4. **No glob re-exports:** `grep -rn "pub use.*\*" writ-compiler/src/check/check_expr/ writ-compiler/src/emit/collect/ writ-compiler/src/emit/body/expr/ writ-compiler/src/check/env_build.rs`
   - Must return empty

5. **Explicit mod declarations:** Verify `mod` declarations in parent modules are explicit (not hidden behind glob imports):
   - `grep "pub mod check_expr" writ-compiler/src/check/mod.rs` — present
   - `grep "pub mod collect" writ-compiler/src/emit/mod.rs` — present
   - `grep "pub mod expr" writ-compiler/src/emit/body/mod.rs` — present

If any check fails, diagnose and fix. This task is the final gate before phase completion.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test --workspace 2>&1 | tail -3 && cargo clippy --workspace 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0 — all tests pass
    - `cargo clippy --workspace` exits 0 — zero warnings
    - No file in check_expr/, collect/, or expr/ exceeds 500 lines
    - `grep -rn "pub use.*\*" writ-compiler/src/check/check_expr/ writ-compiler/src/emit/collect/ writ-compiler/src/emit/body/expr/` returns empty
    - `grep "pub mod check_expr" writ-compiler/src/check/mod.rs` returns a match
    - `grep "pub mod collect" writ-compiler/src/emit/mod.rs` returns a match
    - `grep "pub mod expr" writ-compiler/src/emit/body/mod.rs` returns a match
  </acceptance_criteria>
  <done>Full workspace verification passes. All 7 SPLIT requirements satisfied: 3 mandatory splits complete, 1 partial split complete (env.rs), 3 reviewed with documented rationale.</done>
</task>

</tasks>

<verification>
1. `cargo test --workspace` — all tests pass
2. `cargo clippy --workspace` — zero warnings
3. All line counts verified under 500 (or documented rationale present)
4. No glob re-exports found
5. Explicit mod declarations confirmed
</verification>

<success_criteria>
- All 7 SPLIT requirements for Phase 63 are satisfied
- Full workspace test suite green
- Zero clippy warnings
- No file exceeds 500 lines without documented rationale
</success_criteria>

<output>
After completion, create `.planning/phases/63-writ-compiler-file-splits/63-04-SUMMARY.md`
</output>

---
phase: 65-code-duplication-and-module-boundaries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-compiler/src/check/check_stmt.rs
  - writ-compiler/src/check/desugar.rs
  - writ-compiler/src/check/check_decl.rs
  - writ-compiler/src/check/check_expr/mod.rs
  - writ-compiler/src/check/env_build.rs
  - writ-compiler/src/resolve/resolver.rs
  - writ-assembler/src/assembler.rs
  - writ-assembler/src/parser.rs
  - writ-compiler/src/emit/module_builder.rs
  - writ-compiler/src/emit/serialize.rs
  - writ-module/src/builder.rs
  - writ-module/src/module.rs
  - writ-module/src/reader.rs
  - writ-module/src/writer.rs
  - writ-parser/src/lib.rs
autonomous: true
requirements: [MOD-02]
must_haves:
  truths:
    - "Tier 1 internal wildcards are replaced with explicit import lists"
    - "Tier 3 domain-vocabulary wildcards have documenting comments explaining why they remain"
    - "All existing tests pass without modification"
    - "No internal wildcard imports remain undocumented"
  artifacts:
    - path: "writ-compiler/src/check/check_stmt.rs"
      provides: "Explicit ir:: imports replacing wildcard"
    - path: "writ-compiler/src/check/desugar.rs"
      provides: "Explicit ir:: imports replacing wildcard"
    - path: "writ-compiler/src/resolve/resolver.rs"
      provides: "Explicit resolve::ir:: imports replacing wildcard"
  key_links:
    - from: "writ-compiler/src/check/check_stmt.rs"
      to: "writ-compiler/src/check/ir.rs"
      via: "explicit use super::ir::{TypedStmt, TypedExpr, ...}"
      pattern: "use super::ir::\\{"
---

<objective>
Replace internal wildcard imports (`use super::ir::*`, `use crate::ast::decl::*`, etc.) with explicit import lists where the export set is small enough to enumerate, and document the remaining domain-vocabulary wildcards with intent comments.

Purpose: Satisfy MOD-02 by making imports explicit where practical and documenting intentional wildcards. External library preludes (chumsky, lsp_types, dap) and test-file wildcards are exempt per research findings.
Output: 7 files with explicit imports replacing wildcards, 7 files with documented wildcard intent comments.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-code-duplication-and-module-boundaries/65-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tier 1 and Tier 2 internal wildcards with explicit imports</name>
  <files>
    writ-compiler/src/check/check_stmt.rs,
    writ-compiler/src/check/desugar.rs,
    writ-compiler/src/check/check_decl.rs,
    writ-compiler/src/check/check_expr/mod.rs,
    writ-compiler/src/check/env_build.rs,
    writ-compiler/src/resolve/resolver.rs,
    writ-assembler/src/assembler.rs,
    writ-assembler/src/parser.rs
  </files>
  <read_first>
    - writ-compiler/src/check/ir.rs (exports: TypedAst, TypedExpr, TypedStmt, TypedDecl, TypedArm, TypedPattern, Capture, CaptureMode, TypedLiteral — 9 items)
    - writ-compiler/src/resolve/ir.rs (exports: NameResolvedAst, ResolvedType, PrimitiveTag, ResolvedDecl — 4 items)
    - writ-compiler/src/check/check_stmt.rs (current imports, to see what is used)
    - writ-compiler/src/check/desugar.rs (current imports)
    - writ-compiler/src/check/check_decl.rs (current imports — has BOTH `use super::ir::*` and `use crate::ast::decl::*`)
    - writ-compiler/src/check/check_expr/mod.rs (current imports — has BOTH `use super::ir::*` and `use crate::ast::expr::*`)
    - writ-compiler/src/check/env_build.rs (current imports — has `use crate::ast::decl::*`)
    - writ-compiler/src/resolve/resolver.rs (current imports — has BOTH `use crate::ast::decl::*` and `use crate::resolve::ir::*`)
    - writ-assembler/src/assembler.rs (current imports — has `use crate::ast::*`)
    - writ-assembler/src/parser.rs (current imports — has `use crate::ast::*`)
    - writ-assembler/src/ast.rs (full export list — ~20 items)
    - writ-compiler/src/ast/decl.rs (first 50 lines — to see export list, ~32 items)
    - writ-compiler/src/ast/expr.rs (first 30 lines — to see export list, ~10 items)
  </read_first>
  <action>
**Procedure for each file:** Remove the wildcard import, run `cargo check -p {crate}`, collect all "not found in scope" names from the error output, then write the explicit import list using those names.

**Tier 1 — Small export sets, straightforward replacement:**

1. **writ-compiler/src/check/check_stmt.rs** — Replace `use super::ir::*;` with explicit list. The ir module exports 9 items (TypedAst, TypedExpr, TypedStmt, TypedDecl, TypedArm, TypedPattern, Capture, CaptureMode, TypedLiteral). Remove the wildcard, run `cargo check -p writ-compiler`, note which names are undefined, and write the explicit import. Expected to use ~5 of 9.

2. **writ-compiler/src/check/desugar.rs** — Replace `use super::ir::*;` with explicit list. Expected to use ~6 of 9.

3. **writ-compiler/src/resolve/resolver.rs** — Replace `use crate::resolve::ir::*;` with explicit list. The resolve::ir module exports only 4 items (NameResolvedAst, ResolvedType, PrimitiveTag, ResolvedDecl). Expected to use all 4.

4. **writ-assembler/src/assembler.rs** — Replace `use crate::ast::*;` with explicit list. The ast module exports ~20 items. Run `cargo check -p writ-assembler` after removal to get exact list.

5. **writ-assembler/src/parser.rs** — Replace `use crate::ast::*;` with explicit list. Same ~20-item ast module.

**Tier 2 — Large export sets, most items used:**

6. **writ-compiler/src/check/check_decl.rs** — Has TWO wildcards:
   - Replace `use super::ir::*;` with explicit list (expected ~8 of 9 ir items used).
   - For `use crate::ast::decl::*;` (32 items): Remove the wildcard, run `cargo check -p writ-compiler`, enumerate the missing names. If the list exceeds 20 items, use a grouped import with a comment: `// Intentional wildcard: decl module exports 32 AST node types — declaration checking uses most of them.` and keep the wildcard. If under 20, write the explicit list.

7. **writ-compiler/src/check/check_expr/mod.rs** — Has TWO wildcards:
   - Replace `use super::ir::*;` with explicit list.
   - Replace `use crate::ast::expr::*;` with explicit list (10 items in ast::expr — all likely used).

8. **writ-compiler/src/check/env_build.rs** — Replace `use crate::ast::decl::*;` with explicit list (same decl module, 32 items). Same approach as check_decl.rs.

9. **writ-compiler/src/resolve/resolver.rs** — Also has `use crate::ast::decl::*;`. Same approach.

**For all Tier 2 files:** If the explicit import list exceeds 25 items, keep the wildcard but add a documenting comment: `// Intentional wildcard: {N} of {M} items used — this module works with the full AST vocabulary`. This is acceptable under the research recommendation for Pitfall 4.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - check_stmt.rs does NOT contain `use super::ir::*`
    - check_stmt.rs contains `use super::ir::{` (explicit import list)
    - desugar.rs does NOT contain `use super::ir::*`
    - desugar.rs contains `use super::ir::{` (explicit import list)
    - resolver.rs does NOT contain `use crate::resolve::ir::*`
    - resolver.rs contains `use crate::resolve::ir::{` (explicit import list)
    - assembler.rs does NOT contain `use crate::ast::*`
    - parser.rs (writ-assembler) does NOT contain `use crate::ast::*`
    - check_decl.rs `use super::ir::*` is replaced with explicit list
    - check_expr/mod.rs `use super::ir::*` is replaced with explicit list
    - Any remaining wildcards (Tier 2 with 25+ items) have a `// Intentional wildcard:` comment
    - `cargo check --workspace` exits 0
    - `cargo test --workspace --no-fail-fast` exits 0
  </acceptance_criteria>
  <done>All Tier 1 wildcards replaced with explicit import lists; Tier 2 wildcards either replaced or documented; workspace compiles and all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Document Tier 3 domain-vocabulary wildcards with intent comments</name>
  <files>
    writ-module/src/builder.rs,
    writ-module/src/module.rs,
    writ-module/src/reader.rs,
    writ-module/src/writer.rs,
    writ-compiler/src/emit/module_builder.rs,
    writ-compiler/src/emit/serialize.rs,
    writ-parser/src/lib.rs
  </files>
  <read_first>
    - writ-module/src/builder.rs (line 3 — `use crate::tables::*;`)
    - writ-module/src/module.rs (line 3 — `use crate::tables::*;`)
    - writ-module/src/reader.rs (line 6 — `use crate::tables::*;`)
    - writ-module/src/writer.rs (line 6 — `use crate::tables::*;`)
    - writ-compiler/src/emit/module_builder.rs (line 22 — `use super::metadata::*;`)
    - writ-compiler/src/emit/serialize.rs (line 10 — `use writ_module::tables::*;`)
    - writ-parser/src/lib.rs (line 6 — `pub use cst::*;`)
    - writ-module/src/tables.rs (first 30 lines — to count exported types)
    - writ-compiler/src/emit/metadata.rs (first 30 lines — to count exported types)
  </read_first>
  <action>
Add a documenting comment above each Tier 3 wildcard import explaining why it remains as a wildcard. The comment format is:

```rust
// Intentional wildcard: tables module is the domain vocabulary for this crate —
// all {N} row-struct types are used throughout this file.
use crate::tables::*;
```

Specific files and comments:

1. **writ-module/src/builder.rs** — Add above `use crate::tables::*;`:
   ```rust
   // Intentional wildcard: tables module exports 23 row-struct types that form
   // the domain vocabulary for module building — all are used in this file.
   ```

2. **writ-module/src/module.rs** — Same comment adjusted for "module access and querying".

3. **writ-module/src/reader.rs** — Same comment adjusted for "binary decoding".

4. **writ-module/src/writer.rs** — Same comment adjusted for "binary encoding".

5. **writ-compiler/src/emit/module_builder.rs** — Add above `use super::metadata::*;`:
   ```rust
   // Intentional wildcard: metadata module exports 21 table-row structs that mirror
   // the writ-module tables vocabulary — all are used during IL emission.
   ```

6. **writ-compiler/src/emit/serialize.rs** — Add above `use writ_module::tables::*;`:
   ```rust
   // Intentional wildcard: tables module exports 23 row-struct types — serialization
   // consumes all table types during binary module encoding.
   ```

7. **writ-parser/src/lib.rs** — Add above `pub use cst::*;`:
   ```rust
   // Intentional re-export: cst module is the public API surface of writ-parser —
   // all CST types are re-exported for downstream consumers (compiler, LSP, DAP).
   ```
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo check --workspace 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - writ-module/src/builder.rs contains `// Intentional wildcard:` comment above `use crate::tables::*`
    - writ-module/src/module.rs contains `// Intentional wildcard:` comment above `use crate::tables::*`
    - writ-module/src/reader.rs contains `// Intentional wildcard:` comment above `use crate::tables::*`
    - writ-module/src/writer.rs contains `// Intentional wildcard:` comment above `use crate::tables::*`
    - writ-compiler/src/emit/module_builder.rs contains `// Intentional wildcard:` comment above `use super::metadata::*`
    - writ-compiler/src/emit/serialize.rs contains `// Intentional wildcard:` comment above `use writ_module::tables::*`
    - writ-parser/src/lib.rs contains `// Intentional re-export:` comment above `pub use cst::*`
    - `cargo check --workspace` exits 0
  </acceptance_criteria>
  <done>All Tier 3 domain-vocabulary wildcards documented with intent comments; writ-parser pub use cst::* documented; workspace compiles cleanly</done>
</task>

</tasks>

<verification>
- `cargo check --workspace` — zero errors
- `cargo test --workspace --no-fail-fast` — all tests pass
- `grep -rn "use super::ir::\*" writ-compiler/src/` returns zero results (all ir wildcards replaced)
- `grep -rn "use crate::resolve::ir::\*" writ-compiler/src/` returns zero results
- `grep -rn "use crate::ast::\*" writ-assembler/src/` returns zero results
- `grep -rn "Intentional wildcard" writ-module/src/` returns 4 results (builder, module, reader, writer)
- `grep -rn "Intentional wildcard" writ-compiler/src/emit/` returns 2 results (module_builder, serialize)
</verification>

<success_criteria>
- MOD-02 satisfied: All internal wildcard imports are either replaced with explicit lists or documented with intent comments
- External library preludes (chumsky, lsp_types, dap) are untouched (exempt)
- Test-file wildcards (`use super::*` in #[cfg(test)]) are untouched (exempt)
- Zero compilation errors, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/65-code-duplication-and-module-boundaries/65-02-SUMMARY.md`
</output>

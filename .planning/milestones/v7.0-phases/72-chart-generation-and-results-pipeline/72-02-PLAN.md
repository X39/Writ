---
phase: 72-chart-generation-and-results-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - benchmark/runner/run.sh
  - benchmark/runner/run.ps1
autonomous: true
requirements:
  - REPORT-05

must_haves:
  truths:
    - "run.sh auto-invokes generate.py after container exits, producing charts without manual steps"
    - "run.ps1 auto-invokes generate.py after container exits, producing charts without manual steps"
    - "If python3 is not available, both scripts print a warning instead of failing"
    - "All output files land in benchmark/results/YYYY-MM-DD/ and are ready for commit"
  artifacts:
    - path: "benchmark/runner/run.sh"
      provides: "POSIX sh host launcher with generate.py integration"
      contains: "generate.py"
    - path: "benchmark/runner/run.ps1"
      provides: "PowerShell host launcher with generate.py integration"
      contains: "generate.py"
  key_links:
    - from: "benchmark/runner/run.sh"
      to: "benchmark/generate.py"
      via: "python3 invocation after container exit"
      pattern: "python3.*generate\\.py"
    - from: "benchmark/runner/run.ps1"
      to: "benchmark/generate.py"
      via: "python3 invocation after container exit"
      pattern: "python3.*generate\\.py"
---

<objective>
Integrate `benchmark/generate.py` into the host runner scripts (`run.sh` and `run.ps1`) so that running benchmarks from the repo root automatically produces SVG charts and RESULTS.md — completing the "one command" workflow.

Purpose: REPORT-05 requires charts and tables to be generated as part of the standard benchmark workflow. Users should not need to manually invoke generate.py after running benchmarks.

Output: Updated `run.sh` and `run.ps1` with generate.py invocation appended after container exits.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-chart-generation-and-results-pipeline/72-CONTEXT.md
@.planning/phases/72-chart-generation-and-results-pipeline/72-RESEARCH.md

@benchmark/runner/run.sh
@benchmark/runner/run.ps1
@benchmark/generate.py

<interfaces>
<!-- run.sh current structure (57 lines): -->
<!-- Lines 1-9: header, variables, RESULTS_DIR setup -->
<!-- Lines 10-53: Docker/Podman detection, build, run with volume mount -->
<!-- Lines 55-56: echo "Done. Results: $RESULTS_DIR/raw.json" -->
<!-- Key variables: $REPO_ROOT, $RESULTS_DIR -->

<!-- run.ps1 current structure (46 lines): -->
<!-- Lines 1-43: param, variables, Docker detection, build, run -->
<!-- Lines 45-46: Write-Host "Done. Results: $ResultsDir\raw.json" -->
<!-- Key variables: $RepoRoot, $ResultsDir -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append generate.py invocation to run.sh and run.ps1</name>
  <files>benchmark/runner/run.sh, benchmark/runner/run.ps1</files>
  <read_first>
    - benchmark/runner/run.sh (current content — understand exact line structure, variables, shell dialect)
    - benchmark/runner/run.ps1 (current content — understand exact line structure, variables)
    - benchmark/generate.py (confirm the CLI interface: takes one positional arg, path to raw.json)
  </read_first>
  <action>
**run.sh** — Append the following BEFORE the final `echo "Done."` line (currently line 56). The existing `echo ""` on line 55 and `echo "Done. Results:..."` on line 56 should be replaced with the new block that includes chart generation and a more complete final message:

Replace lines 55-56 with:
```sh
echo ""

# Generate charts and markdown table
if command -v python3 > /dev/null 2>&1; then
    echo "Generating charts..."
    python3 "$REPO_ROOT/benchmark/generate.py" "$RESULTS_DIR/raw.json"
    echo "Done. Results: $RESULTS_DIR/"
else
    echo "Warning: python3 not found — charts not generated"
    echo "  Run manually: python3 benchmark/generate.py $RESULTS_DIR/raw.json"
    echo "  Requires: pip install pygal==3.1.0"
    echo "Done. Results: $RESULTS_DIR/raw.json"
fi
```

Key details:
- Uses `command -v python3` (POSIX sh compatible, matches existing pattern on line 14)
- Uses `$REPO_ROOT` for generate.py path (already defined on line 6)
- Uses `$RESULTS_DIR` for raw.json path (already defined on line 7)
- Does NOT use `set -e` abort on python3 failure — gracefully warns and continues
- Does NOT add any new dependencies beyond python3 + pygal

**run.ps1** — Append the following BEFORE the final `Write-Host "Done."` line (currently lines 45-46). Replace lines 45-46 with:

```powershell
Write-Host ""

# Generate charts and markdown table
if (Get-Command python3 -ErrorAction SilentlyContinue) {
    Write-Host "Generating charts..."
    & python3 "$RepoRoot\benchmark\generate.py" "$ResultsDir\raw.json"
    Write-Host "Done. Results: $ResultsDir\"
} else {
    Write-Warning "python3 not found - charts not generated"
    Write-Warning "  Run manually: python3 benchmark\generate.py $ResultsDir\raw.json"
    Write-Warning "  Requires: pip install pygal==3.1.0"
    Write-Host "Done. Results: $ResultsDir\raw.json"
}
```

Key details:
- Uses `Get-Command python3 -ErrorAction SilentlyContinue` (PowerShell pattern)
- Uses `$RepoRoot` and `$ResultsDir` (already defined on lines 9, 11)
- Uses `& python3` for invocation (PowerShell call operator)
- Warning messages use `Write-Warning` for visibility
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && grep -q 'generate.py' benchmark/runner/run.sh && grep -q 'generate.py' benchmark/runner/run.ps1 && grep -q 'python3.*generate' benchmark/runner/run.sh && grep -q 'python3.*generate' benchmark/runner/run.ps1 && echo "INTEGRATION VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - benchmark/runner/run.sh contains `python3 "$REPO_ROOT/benchmark/generate.py" "$RESULTS_DIR/raw.json"`
    - benchmark/runner/run.sh contains `command -v python3`
    - benchmark/runner/run.sh contains `Warning: python3 not found`
    - benchmark/runner/run.sh contains `pip install pygal`
    - benchmark/runner/run.ps1 contains `python3 "$RepoRoot\benchmark\generate.py" "$ResultsDir\raw.json"`
    - benchmark/runner/run.ps1 contains `Get-Command python3`
    - benchmark/runner/run.ps1 contains `python3 not found`
    - benchmark/runner/run.ps1 contains `pip install pygal`
    - run.sh still starts with `#!/bin/sh` (POSIX sh, not bash — per STATE.md decision)
    - run.sh still contains `set -eu` on line 2
  </acceptance_criteria>
  <done>
    Both run.sh and run.ps1 invoke generate.py after the Docker container exits. If python3 is available, charts are generated automatically. If python3 is not available, a clear warning is printed with manual invocation instructions. The one-command workflow (REPORT-05) is complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end validation of generate.py against real raw.json</name>
  <files>benchmark/generate.py</files>
  <read_first>
    - benchmark/generate.py (the script to validate)
    - benchmark/results/2026-03-20/raw.json (the input data)
    - benchmark/results/2026-03-20/RESULTS.md (generated output to inspect)
  </read_first>
  <action>
Run a comprehensive validation of all generated output against the real raw.json data from Phase 71. This validates all 5 REPORT requirements end-to-end.

Validation steps:

1. **Clean slate test:** Delete all generated files (keep raw.json), re-run generate.py, confirm all files regenerated.

2. **REPORT-01 validation (exec time charts):**
   - exec_stub_all.svg must contain `<svg` tag
   - exec_stub_all.svg must contain string `log scale` in the title
   - exec_stub_interp.svg must contain `<svg` tag
   - exec_stub_interp.svg must contain string `interpreted` in the title
   - exec_stub_interp.svg must NOT contain `Rust` as a series label (Rust excluded from interpreted-only chart)

3. **REPORT-02 validation (memory chart):**
   - memory.svg must contain `<svg` tag
   - memory.svg must contain `Memory` in the title

4. **REPORT-03 validation (startup chart):**
   - startup.svg must contain `<svg` tag
   - startup.svg must contain `Startup` in the title

5. **REPORT-04 validation (RESULTS.md):**
   - Contains table header `| Language | Benchmark | Median (ms) | Compile (ms) | Memory (MB) | Ratio to Rust |`
   - Contains `| Writ |` row with numeric compile_ms value (not dash)
   - Contains `| Rust |` row with `x1.0x` ratio
   - Contains `| Lua |` row with `-` in compile column
   - Contains `## stub` section header
   - Contains `Platform:` metadata line
   - Contains `not measured` note about 0.0 MB memory values

6. **REPORT-05 validation (correct directory):**
   - All files in benchmark/results/2026-03-20/ (same dir as raw.json)

7. **Determinism final check:**
   - Copy all outputs to /tmp, re-run, diff all 5 files — must be identical

If any validation fails, fix generate.py and re-run until all checks pass.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && rm -f benchmark/results/2026-03-20/exec_*.svg benchmark/results/2026-03-20/memory.svg benchmark/results/2026-03-20/startup.svg benchmark/results/2026-03-20/RESULTS.md && python3 benchmark/generate.py benchmark/results/2026-03-20/raw.json && test -f benchmark/results/2026-03-20/exec_stub_all.svg && test -f benchmark/results/2026-03-20/exec_stub_interp.svg && test -f benchmark/results/2026-03-20/memory.svg && test -f benchmark/results/2026-03-20/startup.svg && test -f benchmark/results/2026-03-20/RESULTS.md && grep -q 'Ratio to Rust' benchmark/results/2026-03-20/RESULTS.md && grep -q 'x1.0x' benchmark/results/2026-03-20/RESULTS.md && echo "END-TO-END VALIDATION PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 SVG files regenerated from scratch after deletion
    - RESULTS.md regenerated from scratch after deletion
    - RESULTS.md contains `x1.0x` for Rust ratio
    - RESULTS.md contains `| Writ |` with numeric compile value
    - RESULTS.md contains `| Lua |` with `-` compile value
    - All SVG files contain `<svg` opening tag
    - exec_stub_all.svg contains `log scale` in title text
    - exec_stub_interp.svg contains `interpreted` in title text
    - Determinism: second run produces identical output to first run
  </acceptance_criteria>
  <done>
    All REPORT-01 through REPORT-05 requirements validated end-to-end. generate.py correctly produces SVG charts (exec time all + interp, memory, startup) and RESULTS.md with proper formatting. Output is deterministic and lands in the correct directory alongside raw.json.
  </done>
</task>

</tasks>

<verification>
- run.sh contains generate.py invocation with python3 availability check
- run.ps1 contains generate.py invocation with python3 availability check
- Full clean-slate regeneration produces all expected files
- RESULTS.md table format matches specification
- Determinism verified on final output
</verification>

<success_criteria>
1. run.sh and run.ps1 auto-invoke generate.py after container exits
2. Missing python3 produces a warning, not a failure
3. All output files land in benchmark/results/YYYY-MM-DD/
4. End-to-end validation passes against real raw.json from Phase 71
</success_criteria>

<output>
After completion, create `.planning/phases/72-chart-generation-and-results-pipeline/72-02-SUMMARY.md`
</output>

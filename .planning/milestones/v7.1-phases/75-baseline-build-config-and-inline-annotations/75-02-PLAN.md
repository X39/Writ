---
phase: 75-baseline-build-config-and-inline-annotations
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - benchmark/BASELINE.md
autonomous: true
requirements:
  - BUILD-05
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03

must_haves:
  truths:
    - "fib(40) timing is measured under the fully-optimized release build and committed to the repo"
    - "fib(40) produces the correct output 102334155"
    - "Full workspace tests pass in release mode with zero failures"
    - "Full workspace builds in release mode with zero warnings"
  artifacts:
    - path: "benchmark/BASELINE.md"
      provides: "v7.1 pre-optimization baseline timing document"
      contains: "102334155"
      min_lines: 15
  key_links:
    - from: "benchmark/BASELINE.md"
      to: "target/release/writ.exe"
      via: "Measured timing from release binary execution"
      pattern: "fib\\(40\\)"
---

<objective>
Measure the fib(40) baseline under the fully-optimized release build and document it as the v7.1 pre-optimization reference point.

Purpose: All subsequent phases (76-79) measure their speedup against this baseline. A wrong or missing baseline invalidates all future comparisons. This must be measured AFTER all Plan 01 changes (LTO, FxHashMap, inline annotations) are applied.
Output: benchmark/BASELINE.md with median fib(40) timing from 3 runs, plus full verification that the workspace builds and tests clean.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-baseline-build-config-and-inline-annotations/75-01-SUMMARY.md
@.planning/phases/75-baseline-build-config-and-inline-annotations/75-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full workspace verification</name>
  <files>None (verification only)</files>
  <read_first>
    Cargo.toml,
    writ-runtime/Cargo.toml
  </read_first>
  <action>
**Step 1 — Verify build (VERIFY-03):**

Run `cargo build --release` and confirm it exits 0 with zero warnings. Capture the output. Note: with LTO fat and codegen-units=1 the build will be slow (2-5 minutes); this is expected.

**Step 2 — Verify tests (VERIFY-02):**

Run `cargo test --release` (full workspace, not just writ-runtime) and confirm all tests pass with zero failures.

**Step 3 — Verify fib(40) correctness (VERIFY-01):**

First, recompile the fib benchmark if needed:
```
.\target\release\writ.exe compile benchmark/cases/fib/fib.writ -o benchmark/cases/fib/fib.writc
```

Then run:
```
.\target\release\writ.exe run benchmark/cases/fib/fib.writc
```

Confirm the output contains `102334155`.

If any step fails, investigate and fix before proceeding to Task 2.
  </action>
  <verify>
    <automated>cargo test --release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` exits 0
    - `cargo build --release` output contains zero lines matching "warning:"
    - `cargo test --release` exits 0 with "test result: ok" and 0 failures
    - `.\target\release\writ.exe run benchmark/cases/fib/fib.writc` output contains "102334155"
  </acceptance_criteria>
  <done>Full workspace builds clean and tests pass in release mode. fib(40) produces correct output 102334155.</done>
</task>

<task type="auto">
  <name>Task 2: Measure fib(40) baseline and create BASELINE.md</name>
  <files>benchmark/BASELINE.md</files>
  <read_first>
    benchmark/cases/fib/fib.writ
  </read_first>
  <action>
**Step 1 — Measure fib(40) timing (BUILD-05):**

Run fib(40) 3 times in release mode, recording the wall-clock time for each run. Use PowerShell's `Measure-Command` or Bash `time` command:

```bash
# Run 1
time ./target/release/writ.exe run benchmark/cases/fib/fib.writc

# Run 2
time ./target/release/writ.exe run benchmark/cases/fib/fib.writc

# Run 3
time ./target/release/writ.exe run benchmark/cases/fib/fib.writc
```

Record all three timings and compute the median.

**Step 2 — Create benchmark/BASELINE.md:**

Create the file with this structure (fill in actual measured values):

```markdown
# v7.1 Pre-Optimization Baseline

**Measured:** 2026-03-22
**Build:** cargo build --release (LTO=fat, codegen-units=1, panic=abort)
**Platform:** Windows 11, [CPU from systeminfo or env]
**Commit:** [current HEAD commit hash]

## fib(40) Timing

| Run | Time (s) |
|-----|----------|
| 1   | X.Xs     |
| 2   | X.Xs     |
| 3   | X.Xs     |
| **Median** | **X.Xs** |

**Output:** 102334155 (correct)

## Build Configuration at Baseline

- `lto = "fat"` (cross-crate inlining)
- `codegen-units = 1` (maximum optimization)
- `panic = "abort"` (no unwind tables)
- FxHashMap for DispatchTable, Scheduler.tasks, and all writ-runtime hash maps
- `#[inline(always)]` on extract_* helpers
- `#[inline]` on exec_* dispatch functions
- No `#[inline]` on execute_one

## Prior Reference

- Pre-Phase-75 (quick-task 260322-6om): ~141s (default release profile, no LTO, no FxHashMap)

## Notes

This baseline represents the fully-configured release build BEFORE any algorithmic
optimizations (Phases 76-79). All subsequent phase measurements should compare against
the median time recorded here.
```

Replace all placeholder values with actual measurements. Include the actual CPU model from `wmic cpu get name` or environment info. Include the actual git commit hash from `git rev-parse --short HEAD`.

**Step 3 — Verify the file is well-formed:**

Read back the file and confirm it contains:
- The correct output value 102334155
- Three timing rows and a median row
- The build configuration section
  </action>
  <verify>
    <automated>test -f benchmark/BASELINE.md && grep "102334155" benchmark/BASELINE.md && grep "Median" benchmark/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - benchmark/BASELINE.md exists
    - benchmark/BASELINE.md contains "102334155"
    - benchmark/BASELINE.md contains "Median" with a numeric time value
    - benchmark/BASELINE.md contains "lto" and "codegen-units" and "panic"
    - benchmark/BASELINE.md contains 3 timing run rows (Run 1, Run 2, Run 3)
    - benchmark/BASELINE.md contains the current git commit hash
  </acceptance_criteria>
  <done>fib(40) baseline is measured 3 times under the fully-optimized release build, median is computed, and benchmark/BASELINE.md is committed with all timing data and build configuration details.</done>
</task>

</tasks>

<verification>
1. `benchmark/BASELINE.md` exists and contains fib(40) timing data with 3 runs and a median
2. The output `102334155` appears in the baseline document confirming correctness
3. `cargo test --release` passes with zero failures
4. `cargo build --release` produces zero warnings
</verification>

<success_criteria>
- benchmark/BASELINE.md exists with measured fib(40) timing (3 runs + median)
- The baseline document records the correct output 102334155
- The baseline document records the build configuration (LTO fat, codegen-units=1, panic=abort, FxHashMap, inline annotations)
- Full workspace builds and tests pass in release mode
</success_criteria>

<output>
After completion, create `.planning/phases/75-baseline-build-config-and-inline-annotations/75-02-SUMMARY.md`
</output>

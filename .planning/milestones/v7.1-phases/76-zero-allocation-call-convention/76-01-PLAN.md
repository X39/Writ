---
phase: 76-zero-allocation-call-convention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-runtime/tests/vm_tests.rs
autonomous: true
requirements: [CALL-05]

must_haves:
  truths:
    - "A tail-call test with argc >= 2 proves each argument arrives in the correct callee register"
    - "A call-indirect test with argc >= 1 proves arguments are passed through delegate dispatch"
    - "All pre-existing call tests still pass"
  artifacts:
    - path: "writ-runtime/tests/vm_tests.rs"
      provides: "tail_call_passes_multiple_args and call_indirect_passes_args test functions"
      contains: "fn tail_call_passes_multiple_args"
  key_links:
    - from: "writ-runtime/tests/vm_tests.rs"
      to: "writ-runtime/src/dispatch/calls.rs"
      via: "Instruction::TailCall and Instruction::CallIndirect with argc > 0"
      pattern: "TailCall.*argc.*2|CallIndirect.*argc.*1"
---

<objective>
Add missing test coverage for tail-call multi-arg passing and call-indirect arg passing before the zero-allocation refactor in Plan 02.

Purpose: These tests serve as safety nets -- they must pass both before and after the refactor. The research identified that the existing tail_call_does_not_grow_stack test uses argc=1 and new_delegate_and_call_indirect uses argc=0, leaving multi-arg tail-call and arg-passing call-indirect paths untested.

Output: Two new test functions in vm_tests.rs that exercise the argument-passing paths targeted by Plan 02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-zero-allocation-call-convention/76-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From writ-runtime/tests/vm_tests.rs (test helper):
```rust
fn build_two_method_runtime(
    main_instrs: &[Instruction],
    main_reg_count: u16,
    callee_instrs: &[Instruction],
    callee_reg_count: u16,
) -> Runtime<NullHost>
```

From writ-module instruction set (used in tests):
```rust
Instruction::LoadInt { r_dst: u16, value: i64 }
Instruction::TailCall { method_idx: u32, r_base: u16, argc: u16 }
Instruction::NewDelegate { r_dst: u16, method_idx: u32, r_target: u16 }
Instruction::CallIndirect { r_dst: u16, r_delegate: u16, r_base: u16, argc: u16 }
Instruction::Ret { r_src: u16 }
Instruction::Add { r_dst: u16, r_a: u16, r_b: u16 }
```

Existing tail-call test pattern (line 918):
- method 0: LoadInt 42 into r0, TailCall method_idx=0x07000002 r_base=0 argc=1
- method 1: Ret r0 (receives the 42 as arg)
- Asserts return value is Int(42)

Existing call-indirect test pattern (line 1510):
- method 0: NewDelegate to method 1, CallIndirect argc=0, return result
- method 1: LoadInt 77, Ret
- Asserts return value is Int(77)

Method token encoding: 0x07000001 = MethodDef table (0x07), row 1 (1-based). Method 0 (main) is row 1 = 0x07000001, method 1 (callee) is row 2 = 0x07000002.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tail_call_passes_multiple_args and call_indirect_passes_args tests</name>
  <files>writ-runtime/tests/vm_tests.rs</files>
  <read_first>
    - writ-runtime/tests/vm_tests.rs (lines 918-943 for tail-call pattern, lines 1510-1544 for call-indirect pattern, lines 69-96 for build_two_method_runtime)
    - writ-runtime/src/dispatch/calls.rs (exec_tail_call and exec_call_indirect signatures)
  </read_first>
  <behavior>
    - Test tail_call_passes_multiple_args: method 0 loads Int(10) into r0 and Int(20) into r1, issues TailCall to method 1 with r_base=0 argc=2. Method 1 has 2 registers, adds r0+r1 into r2 (or returns r0 for simplicity since Add requires 3 regs). Method 1 adds r0 + r1 -> r2 and returns r2. Expected return value: Int(30).
    - Test call_indirect_passes_args: method 0 loads Int(99) into r1, creates delegate to method 1, issues CallIndirect with r_base=1 argc=1 (passing r1 as first arg). Method 1 returns r0. Expected return value: Int(99).
  </behavior>
  <action>
Add two new test functions to writ-runtime/tests/vm_tests.rs, placed near the existing call tests (after tail_call_does_not_grow_stack and near new_delegate_and_call_indirect):

1. `tail_call_passes_multiple_args` test:
   - method 0 (main, 2 registers): LoadInt r0=10, LoadInt r1=20, TailCall method_idx=0x07000002 r_base=0 argc=2
   - method 1 (callee, 3 registers): Add r2=r0+r1, Ret r2
   - build_two_method_runtime with main_reg_count=2, callee_reg_count=3
   - Assert return value is Value::Int(30)
   - Assert task completed successfully

2. `call_indirect_passes_args` test:
   - method 0 (main, 4 registers): LoadInt r1=99, NewDelegate r0 method_idx=0x07000002 r_target=2 (r2=Void, no target), CallIndirect r_dst=3 r_delegate=0 r_base=1 argc=1, Ret r3
   - method 1 (callee, 1 register): Ret r0
   - build_two_method_runtime with main_reg_count=4, callee_reg_count=1
   - Assert return value is Value::Int(99)
   - Assert task completed successfully

Both tests use the existing `build_two_method_runtime` helper and follow the pattern of existing tests (spawn task, tick, assert state + return value).
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test --release -p writ-runtime tail_call_passes_multiple_args call_indirect_passes_args 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "fn tail_call_passes_multiple_args" writ-runtime/tests/vm_tests.rs returns 1
    - grep -c "fn call_indirect_passes_args" writ-runtime/tests/vm_tests.rs returns 1
    - grep "Value::Int(30)" writ-runtime/tests/vm_tests.rs finds the tail-call assertion
    - grep "Value::Int(99)" writ-runtime/tests/vm_tests.rs finds the call-indirect assertion
    - cargo test --release -p writ-runtime tail_call_passes_multiple_args -- passes
    - cargo test --release -p writ-runtime call_indirect_passes_args -- passes
    - cargo test --release -p writ-runtime -- all existing tests still pass
  </acceptance_criteria>
  <done>Two new tests pass exercising multi-arg tail-call (argc=2, verifies both args arrive correctly) and arg-passing call-indirect (argc=1, verifies arg passes through delegate dispatch). All pre-existing tests still pass.</done>
</task>

</tasks>

<verification>
- `cargo test --release -p writ-runtime 2>&1` -- all tests pass including the two new ones
- `cargo build --release 2>&1` -- zero warnings
</verification>

<success_criteria>
- tail_call_passes_multiple_args test exists and passes
- call_indirect_passes_args test exists and passes
- All pre-existing writ-runtime tests pass
- Zero warnings on release build
</success_criteria>

<output>
After completion, create `.planning/phases/76-zero-allocation-call-convention/76-01-SUMMARY.md`
</output>

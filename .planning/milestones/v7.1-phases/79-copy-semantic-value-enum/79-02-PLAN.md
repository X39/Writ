---
phase: 79-copy-semantic-value-enum
plan: 02
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - writ-runtime/tests/vm_tests.rs
autonomous: true
requirements: [VALUE-06, VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04]

must_haves:
  truths:
    - "cargo test --release passes with zero failures across the entire workspace"
    - "cargo build --release produces zero warnings"
    - "fib(40) produces correct output 102334155"
    - "fib(40) completes in under 30 seconds in release mode"
    - "No InlineStruct references exist anywhere in the workspace"
  artifacts:
    - path: "writ-runtime/tests/vm_tests.rs"
      provides: "Fully migrated test suite with no InlineStruct references"
  key_links:
    - from: "writ-runtime/src/value.rs"
      to: "benchmark/cases/fib/fib.writ"
      via: "cargo run -p writ-cli --release -- benchmark/cases/fib/fib.writ"
      pattern: "102334155"
---

<objective>
Run the full test suite and performance verification. Confirm fib(40) correctness and performance target (< 30s). Run workspace-wide grep to confirm zero InlineStruct references remain.

Purpose: Final validation that the Copy-semantic Value migration is complete and the v7.1 performance target is met.
Output: Verified passing tests, confirmed fib(40) < 30s, clean workspace.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-copy-semantic-value-enum/79-01-SUMMARY.md
@.planning/phases/79-copy-semantic-value-enum/79-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full workspace verification and fib(40) performance benchmark</name>
  <read_first>
    writ-runtime/src/value.rs
  </read_first>
  <files>
    writ-runtime/tests/vm_tests.rs
  </files>
  <action>
    **Step 1: Workspace-wide InlineStruct grep**

    Run `grep -r "InlineStruct" --include="*.rs" .` from the project root. This MUST return zero matches. If any remain, fix them (the research identified all sites; any remaining are in test files or comments).

    **Step 2: Full test suite**

    Run `cargo test --release` and confirm zero failures. If any test fails, diagnose and fix. Common failure causes:
    - A test still constructs `Value::InlineStruct` — update to `Value::Struct { type_idx, href }` with heap allocation
    - A test asserts `.clone()` produces independent copy — delete or update (reference semantics now)
    - A match arm still names InlineStruct — rename to Struct

    **Step 3: Zero warnings check**

    Run `cargo build --release 2>&1 | grep warning`. Must return zero lines. If warnings exist about unused imports or dead code after removing InlineStruct paths, fix them.

    **Step 4: fib(40) correctness**

    Run `cargo run -p writ-cli --release -- benchmark/cases/fib/fib.writ` and confirm the output contains `102334155`. This verifies the Value migration did not break integer arithmetic dispatch.

    **Step 5: fib(40) performance — 3 cold runs, take median**

    Run fib(40) three times in release mode and record wall-clock time. Use PowerShell `Measure-Command` or bash `time` depending on available shell. The median of 3 runs must be under 30 seconds.

    Example:
    ```bash
    time cargo run -p writ-cli --release -- benchmark/cases/fib/fib.writ
    ```

    Record all three timings and the median. If median exceeds 30s, investigate — the Copy migration should reduce overhead, not increase it. The Phase 78 baseline was 53.134s; Copy should improve this significantly since struct clone overhead is eliminated.

    **Step 6: Record performance result**

    Document the median fib(40) time and delta vs Phase 78 baseline (53.134s) in the summary.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test --release 2>&1 | tail -5 && echo "---" && cargo run -p writ-cli --release -- benchmark/cases/fib/fib.writ 2>&1 | grep -o "102334155"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "InlineStruct" --include="*.rs" .` from project root returns ZERO matches
    - `cargo test --release` exits with code 0 (all tests pass)
    - `cargo build --release 2>&1 | grep -c "warning"` returns 0
    - `cargo run -p writ-cli --release -- benchmark/cases/fib/fib.writ` output contains "102334155"
    - Median of 3 fib(40) runs is under 30 seconds
  </acceptance_criteria>
  <done>Full workspace passes all tests with zero warnings. fib(40) outputs 102334155 and completes under 30 seconds. No InlineStruct references remain. Phase 79 and v7.1 milestone performance target met.</done>
</task>

</tasks>

<verification>
1. `grep -r "InlineStruct" --include="*.rs" .` — zero matches workspace-wide
2. `cargo test --release` — zero failures
3. `cargo build --release 2>&1 | grep warning` — zero warnings
4. `cargo run -p writ-cli --release -- benchmark/cases/fib/fib.writ` — outputs 102334155
5. Median fib(40) < 30s (3 cold runs)
</verification>

<success_criteria>
- Complete workspace verification: zero InlineStruct, zero test failures, zero warnings
- fib(40) correctness confirmed (output 102334155)
- fib(40) performance target met (median < 30 seconds)
- v7.1 milestone final phase complete
</success_criteria>

<output>
After completion, create `.planning/phases/79-copy-semantic-value-enum/79-02-SUMMARY.md`
</output>

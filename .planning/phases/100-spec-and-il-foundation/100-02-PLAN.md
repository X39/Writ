---
phase: 100-spec-and-il-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/47_2_18_writ_runtime_module_contents.md
  - language-spec/spec/67_4_2_opcode_assignment_table.md
  - language-spec/spec/58_3_10_type_operations.md
  - language-spec/spec/65_4_0_instruction_count_by_category.md
  - language-spec/spec/45_2_16_il_module_format.md
autonomous: true
requirements: [SPEC-05, SPEC-06]

must_haves:
  truths:
    - "The opcode table section 4.2 contains TypeOf at opcode 0x0A30 with shape RI32"
    - "The instruction reference section 3.10 documents the TYPEOF instruction with its operands and semantics"
    - "The instruction count in section 4.0 shows 92 total instructions with a Reflection row containing 1 instruction"
    - "Section 2.16.1 format_version history documents Version 4 with TYPEOF opcode addition"
    - "Section 2.18.9 Reflection Types lists all 6 reflection class TypeDefs and Reflectable as contract 19"
  artifacts:
    - path: "language-spec/spec/67_4_2_opcode_assignment_table.md"
      provides: "TypeOf opcode at 0x0A30"
      contains: "TYPEOF"
    - path: "language-spec/spec/58_3_10_type_operations.md"
      provides: "TYPEOF instruction reference"
      contains: "TYPEOF"
    - path: "language-spec/spec/65_4_0_instruction_count_by_category.md"
      provides: "Updated instruction count with Reflection category"
      contains: "Reflection"
    - path: "language-spec/spec/45_2_16_il_module_format.md"
      provides: "format_version 4 documentation"
      contains: "Version 4"
    - path: "language-spec/spec/47_2_18_writ_runtime_module_contents.md"
      provides: "Section 2.18.9 Reflection Types"
      contains: "2.18.9 Reflection Types"
  key_links:
    - from: "language-spec/spec/67_4_2_opcode_assignment_table.md"
      to: "language-spec/spec/58_3_10_type_operations.md"
      via: "TYPEOF opcode 0x0A30 referenced in both"
      pattern: "0x0A30"
    - from: "language-spec/spec/45_2_16_il_module_format.md"
      to: "language-spec/spec/67_4_2_opcode_assignment_table.md"
      via: "format_version 4 references 0x0A30"
      pattern: "Version 4"
---

<objective>
Update the IL spec files to define the TypeOf opcode (0x0A30), bump format_version to 4, add reflection types to the writ-runtime virtual module (section 2.18.9), and update the instruction count.

Purpose: Establish the IL-level contracts for the TYPEOF instruction and reflection builtin types so that Phases 101-103 can implement them against a stable binary specification.
Output: Updated opcode table, instruction reference, format_version history, instruction count, and writ-runtime module contents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-spec-and-il-foundation/100-CONTEXT.md
@.planning/phases/100-spec-and-il-foundation/100-RESEARCH.md
@language-spec/spec/47_2_18_writ_runtime_module_contents.md
@language-spec/spec/67_4_2_opcode_assignment_table.md
@language-spec/spec/58_3_10_type_operations.md
@language-spec/spec/65_4_0_instruction_count_by_category.md
@language-spec/spec/45_2_16_il_module_format.md
@language-spec/spec/66_4_1_instruction_shape_reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypeOf opcode and update instruction reference and count</name>
  <files>
    language-spec/spec/67_4_2_opcode_assignment_table.md,
    language-spec/spec/58_3_10_type_operations.md,
    language-spec/spec/65_4_0_instruction_count_by_category.md
  </files>
  <read_first>
    language-spec/spec/67_4_2_opcode_assignment_table.md,
    language-spec/spec/58_3_10_type_operations.md,
    language-spec/spec/65_4_0_instruction_count_by_category.md,
    language-spec/spec/66_4_1_instruction_shape_reference.md
  </read_first>
  <action>
**Edit 1: `language-spec/spec/67_4_2_opcode_assignment_table.md`**

In the `## 0x0A -- Type Operations` section, after the existing **Enum (0x0A20-0x0A2F)** table (which ends after the EXTRACT_FIELD row), add a new Reflection sub-range:

```
**Reflection (0x0A30-0x0A3F):**

| Opcode   | Mnemonic | Shape |
|----------|----------|-------|
| `0x0A30` | TYPEOF   | RI32  |
```

Do NOT modify any existing entries. The new sub-range starts at 0x0A30 to leave room in the Enum range.

**Edit 2: `language-spec/spec/58_3_10_type_operations.md`**

After the existing **General enum operations** table (which ends after the EXTRACT_FIELD row), add a new Reflection section:

```
**Reflection:**

| Mnemonic | Shape | Operands            | Description                                                                                                                                                                                                |
|----------|-------|---------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `TYPEOF` | RI32  | r_dst, type_idx:u32 | Load the compile-time Type singleton for the type at type_idx into r_dst. The type_idx is a TypeDef metadata token baked in at compile time. r_dst receives a lazily-allocated Type heap reference (see section 2.18.9). Encoding: `u16(0x0A30) u16(r_dst) u32(type_idx)` = 8 bytes. |
```

**Edit 3: `language-spec/spec/65_4_0_instruction_count_by_category.md`**

The existing table has rows for Option (4), Result (6), Enum (3) as separate categories under the Type Operations umbrella. Add a new row after the Enum row:

```
| Reflection         | 1      | TYPEOF                                                                                                                |
```

Update the **Total** row from `**91**` to `**92**`.

The final table should have the Reflection row between Enum and Concurrency, and the total should read `| **Total** | **92** |`.
  </action>
  <verify>
    <automated>grep -c "TYPEOF" language-spec/spec/67_4_2_opcode_assignment_table.md && grep -c "0x0A30" language-spec/spec/67_4_2_opcode_assignment_table.md && grep -c "TYPEOF" language-spec/spec/58_3_10_type_operations.md && grep -c "Reflection" language-spec/spec/65_4_0_instruction_count_by_category.md && grep "Total" language-spec/spec/65_4_0_instruction_count_by_category.md | grep -c "92" && echo "OPCODE CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - 67_4_2_opcode_assignment_table.md contains a row with "`0x0A30` | TYPEOF   | RI32"
    - 67_4_2_opcode_assignment_table.md contains the heading "Reflection (0x0A30"
    - 58_3_10_type_operations.md contains a row with "TYPEOF | RI32 | r_dst, type_idx:u32"
    - 58_3_10_type_operations.md contains the heading "**Reflection:**"
    - 65_4_0_instruction_count_by_category.md contains a row with "Reflection" and count "1" and "TYPEOF"
    - 65_4_0_instruction_count_by_category.md Total row shows "**92**"
    - All existing opcode entries are unchanged (WRAP_SOME through EXTRACT_FIELD remain intact)
  </acceptance_criteria>
  <done>TypeOf opcode 0x0A30 is assigned in the opcode table, documented in the instruction reference with full operand description, and the instruction count is updated to 92 with a new Reflection category row.</done>
</task>

<task type="auto">
  <name>Task 2: Update format_version and add writ-runtime Reflection Types section</name>
  <files>
    language-spec/spec/45_2_16_il_module_format.md,
    language-spec/spec/47_2_18_writ_runtime_module_contents.md
  </files>
  <read_first>
    language-spec/spec/45_2_16_il_module_format.md,
    language-spec/spec/47_2_18_writ_runtime_module_contents.md
  </read_first>
  <action>
**Edit 1: `language-spec/spec/45_2_16_il_module_format.md`**

In section 2.16.1 Binary Container, find the **Format version history** line which currently reads:

```
**Format version history:** Version 1 — initial format (MethodDef row: 20 bytes). Version 2 — added `param_count(u16)` to MethodDef (row: 24 bytes, padded from 22). Version 3 — TypeDef.kind=4 (class) added; kind=0 (struct) now means value type.
```

Append to the end of that line (or add as continuation):

```
 Version 4 — TYPEOF opcode added (reflection; section 3.10, section 4.2 0x0A30); format_version=3 modules are rejected at load time with UnsupportedVersion.
```

The full line after edit should read:
```
**Format version history:** Version 1 — initial format (MethodDef row: 20 bytes). Version 2 — added `param_count(u16)` to MethodDef (row: 24 bytes, padded from 22). Version 3 — TypeDef.kind=4 (class) added; kind=0 (struct) now means value type. Version 4 — TYPEOF opcode added (reflection; section 3.10, section 4.2 0x0A30); format_version=3 modules are rejected at load time with UnsupportedVersion.
```

**Edit 2: `language-spec/spec/47_2_18_writ_runtime_module_contents.md`**

Before the final closing of the file (after section 2.18.8 Versioning), add a new section 2.18.9:

```markdown
## 2.18.9 Reflection Types

The `writ-runtime` module provides six class TypeDefs for reflection metadata and one contract for dynamic type queries. All reflection type methods are intrinsic — the runtime provides native implementations. Reflection types are class types (kind=4 in TypeDef).

### Type

Represents the metadata for a single type definition.

**Fields:**

| Field       | Type     | Description                                      |
|-------------|----------|--------------------------------------------------|
| `name`      | `string` | Unqualified type name                            |
| `namespace` | `string` | Fully-qualified namespace                        |
| `kind`      | `string` | One of: "struct", "class", "enum", "entity"      |
| `is_generic`| `bool`   | Whether the type has generic parameters          |

**Methods (intrinsic):**

| Method       | Signature                                       | Intrinsic IL          |
|--------------|-------------------------------------------------|-----------------------|
| `fields`     | `fn fields(self) -> FieldInfo[]`                | `TypeFields`          |
| `methods`    | `fn methods(self) -> MethodInfo[]`              | `TypeMethods`         |
| `attributes` | `fn attributes(self) -> AttributeInfo[]`        | `TypeAttributes`      |
| `contracts`  | `fn contracts(self) -> ContractInfo[]`           | `TypeContracts`       |
| `implements` | `fn implements(self, contract: Type) -> bool`   | `TypeImplements`      |
| `type_args`  | `fn type_args(self) -> Type[]`                  | `TypeTypeArgs`        |

Type objects are lazily-allocated singletons — one per TypeDef. `typeof(T) == typeof(T)` is always true (identity by TypeDef index). The runtime registers Type heap objects as permanent GC roots so they are never freed by garbage collection.

### FieldInfo

Describes a single public field of a type.

**Fields:**

| Field          | Type   | Description                                        |
|----------------|--------|----------------------------------------------------|
| `name`         | `string` | Field name                                       |
| `declared_type`| `Type`   | Type of the field                                |
| `is_mutable`   | `bool`   | true if declared with `mut`, false if `let`      |

**Methods (intrinsic):**

| Method | Signature                                           | Intrinsic IL     |
|--------|-----------------------------------------------------|------------------|
| `get`  | `fn get(self, instance: Box) -> Box`                | `FieldGet`       |
| `set`  | `fn set(self, instance: Box, value: Box)`           | `FieldSet`       |
| `attributes` | `fn attributes(self) -> AttributeInfo[]`       | `FieldAttributes`|

`FieldInfo.set()` on an immutable field (`is_mutable == false`) crashes the current task.

### MethodInfo

Describes a single public method of a type.

**Fields:**

| Field         | Type              | Description            |
|---------------|-------------------|------------------------|
| `name`        | `string`          | Method name            |
| `parameters`  | `ParameterInfo[]` | Parameter descriptors  |
| `return_type` | `Type`            | Return type            |

**Methods (intrinsic):**

| Method       | Signature                                             | Intrinsic IL        |
|--------------|-------------------------------------------------------|---------------------|
| `invoke`     | `fn invoke(self, instance: Box, args: Box[]) -> Box`  | `MethodInvoke`      |
| `attributes` | `fn attributes(self) -> AttributeInfo[]`              | `MethodAttributes`  |

`MethodInfo.invoke()` executes on the current task's call stack. The invoked method participates in cooperative scheduling normally.

### ParameterInfo

Describes a method parameter.

**Fields:**

| Field          | Type     | Description    |
|----------------|----------|----------------|
| `name`         | `string` | Parameter name |
| `declared_type`| `Type`   | Parameter type |

### AttributeInfo

Describes an applied attribute. Integrates with the attribute system (section 1.17) and shares the underlying `AttributeIndex` with the `ModuleAttributeView` query API.

**Fields:**

| Field  | Type    | Description                          |
|--------|---------|--------------------------------------|
| `name` | `string`| Attribute name                       |
| `args` | `Box[]` | Attribute arguments (boxed values)   |

### ContractInfo

Describes an implemented contract.

**Fields:**

| Field  | Type     | Description              |
|--------|----------|--------------------------|
| `name` | `string` | Contract name            |
| `type` | `Type`   | The contract's Type object |

### Reflectable Contract

The `Reflectable` contract is defined as contract slot 19 in the `writ-runtime` module.

| Contract      | Method Signature              |
|---------------|-------------------------------|
| `Reflectable` | `fn get_type(self) -> Type`   |

The compiler auto-generates a `Reflectable` `ImplDef` for every user-defined type (struct, class, entity, enum). The auto-generated implementation returns the lazily-allocated Type singleton for that TypeDef.

For primitives, the runtime provides separate intrinsic dispatch:

| Primitive | Intrinsic ID     |
|-----------|------------------|
| `int`     | `IntGetType`     |
| `float`   | `FloatGetType`   |
| `bool`    | `BoolGetType`    |
| `string`  | `StringGetType`  |

These intrinsics are registered on the primitive pseudo-TypeDefs (section 2.18.4) as Reflectable implementations.
```

This new section goes after the existing section 2.18.8 Versioning content (after the paragraph ending with "...reports the conflict via the logging interface (section 2.14.7).") and before the file's final blank line.
  </action>
  <verify>
    <automated>grep -c "Version 4" language-spec/spec/45_2_16_il_module_format.md && grep -c "2.18.9 Reflection Types" language-spec/spec/47_2_18_writ_runtime_module_contents.md && grep -c "Reflectable" language-spec/spec/47_2_18_writ_runtime_module_contents.md && grep -c "contract slot 19" language-spec/spec/47_2_18_writ_runtime_module_contents.md && grep -c "IntGetType" language-spec/spec/47_2_18_writ_runtime_module_contents.md && echo "FORMAT AND RUNTIME CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - 45_2_16_il_module_format.md contains "Version 4" in the format version history
    - 45_2_16_il_module_format.md contains "TYPEOF" and "0x0A30" in the Version 4 entry
    - 45_2_16_il_module_format.md contains "UnsupportedVersion" in the Version 4 entry
    - 47_2_18_writ_runtime_module_contents.md contains "## 2.18.9 Reflection Types"
    - 47_2_18_writ_runtime_module_contents.md contains TypeDef tables for all 6 types: Type, FieldInfo, MethodInfo, ParameterInfo, AttributeInfo, ContractInfo
    - 47_2_18_writ_runtime_module_contents.md contains "contract slot 19" for Reflectable
    - 47_2_18_writ_runtime_module_contents.md contains intrinsic names: TypeFields, TypeMethods, TypeAttributes, TypeContracts, TypeImplements, TypeTypeArgs
    - 47_2_18_writ_runtime_module_contents.md contains FieldGet, FieldSet, MethodInvoke intrinsic names
    - 47_2_18_writ_runtime_module_contents.md contains primitive intrinsics: IntGetType, FloatGetType, BoolGetType, StringGetType
    - 47_2_18_writ_runtime_module_contents.md contains "AttributeIndex" and "ModuleAttributeView" reference
  </acceptance_criteria>
  <done>format_version 4 is documented in section 2.16.1 with TYPEOF opcode reference and UnsupportedVersion rejection rule. Section 2.18.9 Reflection Types documents all 6 reflection class TypeDefs with fields and intrinsic methods, Reflectable as contract 19, and primitive get_type intrinsics.</done>
</task>

</tasks>

<verification>
- `grep "0x0A30" language-spec/spec/67_4_2_opcode_assignment_table.md` returns TYPEOF entry
- `grep "TYPEOF" language-spec/spec/58_3_10_type_operations.md` returns instruction reference entry
- `grep "92" language-spec/spec/65_4_0_instruction_count_by_category.md` shows updated total
- `grep "Version 4" language-spec/spec/45_2_16_il_module_format.md` returns format version entry
- `grep "2.18.9" language-spec/spec/47_2_18_writ_runtime_module_contents.md` returns section heading
- `grep "Reflectable" language-spec/spec/47_2_18_writ_runtime_module_contents.md` returns contract definition
</verification>

<success_criteria>
The IL spec defines TypeOf at opcode 0x0A30 in section 4.2, documents it in section 3.10 with full operand description, updates the instruction count to 92, records format_version 4 in section 2.16.1, and documents all 6 reflection types plus Reflectable contract 19 in section 2.18.9 of the writ-runtime module spec. A runtime implementor reading these sections has all the information needed to implement the TYPEOF instruction and reflection type system.
</success_criteria>

<output>
After completion, create `.planning/phases/100-spec-and-il-foundation/100-02-SUMMARY.md`
</output>

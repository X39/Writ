---
phase: 106-read-only-introspection-integration-tests-and-lsp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-runtime/tests/reflection_tests.rs
autonomous: true
requirements: [REFL-03, REFL-04, REFL-06, REFL-07, REFL-08, REFL-09]

must_haves:
  truths:
    - "Type.methods() returns an Array of MethodInfo for a struct with methods"
    - "Type.contracts() returns an Array of ContractInfo for a type implementing a contract"
    - "Type.implements(contract) returns true for implemented contracts and false for others"
    - "typeof(T) == typeof(T) is true (interned singleton); typeof(T) == typeof(U) is false"
    - "All reflection heap objects survive GC after reflection operations complete"
    - "FieldInfo.get on second field returns correct value (not just first field)"
  artifacts:
    - path: "writ-runtime/tests/reflection_tests.rs"
      provides: "Integration tests for REFL-03 through REFL-09"
      contains: "test_type_methods_returns_array"
  key_links:
    - from: "writ-runtime/tests/reflection_tests.rs"
      to: "writ-runtime/src/dispatch/intrinsics.rs"
      via: "IL hand-assembly calling TypeMethods, TypeContracts, TypeImplements intrinsics"
      pattern: "TypeMethods|TypeContracts|TypeImplements"
---

<objective>
Add runtime integration tests covering all remaining read-only reflection intrinsics (REFL-03 through REFL-09) that are not yet tested.

Purpose: Validate that TypeMethods, TypeContracts, TypeImplements, and Type equality (interning) work end-to-end through the VM dispatch pipeline. Existing tests cover TypeOf, TypeFields, TypeAttributes, FieldInfoGet, and GC survival; this plan fills the gaps.

Output: Extended `writ-runtime/tests/reflection_tests.rs` with 5 new test functions.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-read-only-introspection-integration-tests-and-lsp/106-RESEARCH.md
@.planning/phases/105-writ-compiler-reflectable-auto-impl-emission/105-01-SUMMARY.md

<interfaces>
<!-- Existing reflection test infrastructure in reflection_tests.rs -->

From writ-runtime/tests/reflection_tests.rs:
```rust
fn encode(instrs: &[Instruction]) -> Vec<u8>  // encode instructions to bytecode
fn typedef_token(typedef_idx: usize) -> u32   // table_id=2, row=typedef_idx+1 (1-based)
```

Test pattern:
```rust
let mut builder = ModuleBuilder::new("test");
builder.add_type_def("Name", "", TypeDefKind::Struct, 0);
// ...add fields, type_refs, methods...
let body = MethodBody { register_types: vec![0; N], code: encode(&[...]), ... };
builder.add_method("main", &[0], 0, N, body);
let module = builder.build();
let mut runtime = RuntimeBuilder::new(module).with_gc().build().unwrap();
let tid = runtime.spawn_task(0, vec![]).unwrap();
runtime.tick(0.0, ExecutionLimit::None);
assert_eq!(runtime.task_state(tid), Some(TaskState::Completed));
assert_eq!(runtime.return_value(tid), Some(Value::Bool(true)));
```

Contract type_ref names for writ-runtime intrinsics:
- "Type.fields" -> TypeFields intrinsic
- "Type.methods" -> TypeMethods intrinsic (via "Type.type_methods" or "Type.methods" — check domain_dispatch.rs)
- "Type.attributes" -> TypeAttributes intrinsic
- "Type.contracts" -> TypeContracts intrinsic
- "Type.implements" -> TypeImplements intrinsic
- "FieldInfo.get" -> FieldInfoGet intrinsic

HeapObject::Array { elements, .. } for array inspection.
HeapObject::Instance { fields, .. } or get_field(href, idx) for FieldInfo/MethodInfo/ContractInfo inspection.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Type.methods, Type.contracts, Type.implements integration tests</name>
  <files>writ-runtime/tests/reflection_tests.rs</files>
  <read_first>
    - writ-runtime/tests/reflection_tests.rs (full file — reuse encode/typedef_token helpers, follow existing test patterns)
    - writ-runtime/src/dispatch/intrinsics.rs (search for TypeMethods, TypeContracts, TypeImplements arms to understand argument protocol)
    - writ-runtime/src/domain_dispatch.rs (search for "type_methods", "type_contracts", "type_implements" to find contract name strings for add_type_ref)
  </read_first>
  <action>
Add three new #[test] functions to writ-runtime/tests/reflection_tests.rs:

1. **test_type_methods_returns_array** (REFL-04):
   - Create a ModuleBuilder with a struct "Greeter" that has at least one user-defined MethodDef (e.g., "greet" with a body that just does RetVoid).
   - Add a TypeRef for "Type.methods" contract in writ-runtime module (check domain_dispatch.rs for the exact string — may be "Type.type_methods").
   - In the main body: TypeOf on the struct -> CallVirt for methods() -> Ret the array.
   - Assert: return value is Value::Ref, heap object is Array, array length >= 1.
   - Assert: first MethodInfo element has field 0 (name) = "greet" (read string from heap).

2. **test_type_contracts_returns_array** (REFL-06):
   - Create a ModuleBuilder with a struct and an ImplDef linking it to a contract.
   - Add a TypeRef for "Type.contracts" contract (check domain_dispatch.rs for exact string — may be "Type.type_contracts").
   - In main: TypeOf -> CallVirt for contracts() -> Ret array.
   - Assert: return value is Array, length >= 1.
   - Assert: first ContractInfo element has field 0 (name) matching the contract name.

3. **test_type_implements_returns_bool** (REFL-07):
   - Create a ModuleBuilder with a struct that implements a contract (via add_impl_def).
   - Add a TypeRef for "Type.implements" (check domain_dispatch.rs for exact name — may be "Type.type_implements").
   - TypeImplements takes a contract Type argument. In main body: TypeOf on the struct -> get a second TypeOf or construct contract Type reference as the argument -> CallVirt for implements(contract_type) -> Ret bool.
   - Assert: returns Value::Bool(true) for a contract the type implements.
   - NOTE: Check the TypeImplements intrinsic arm to understand the argument protocol — it may take a ContractInfo, a Type, or a raw contract index. Adapt the test accordingly.

For each test, follow the exact pattern from existing tests (encode helper, typedef_token helper, ModuleBuilder, RuntimeBuilder::new(module).with_gc().build()). Use the same import list already at the top of the file.
  </action>
  <verify>
    <automated>cargo test -p writ-runtime test_type_methods_returns_array test_type_contracts_returns_array test_type_implements_returns_bool -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Three new tests pass: test_type_methods_returns_array confirms REFL-04, test_type_contracts_returns_array confirms REFL-06, test_type_implements_returns_bool confirms REFL-07</done>
</task>

<task type="auto">
  <name>Task 2: Add Type equality interning test and GC survival after reflection ops</name>
  <files>writ-runtime/tests/reflection_tests.rs</files>
  <read_first>
    - writ-runtime/tests/reflection_tests.rs (just refreshed from Task 1; focus on test_type_object_survives_gc for GC pattern)
    - writ-runtime/src/dispatch/intrinsics.rs (search for FieldInfoGet to verify field 1 get works — test_field_info_get only tests field 0)
  </read_first>
  <action>
Add two new #[test] functions to writ-runtime/tests/reflection_tests.rs:

1. **test_type_equality_interned** (REFL-09):
   - Create a ModuleBuilder with two TypeDefs: "Alpha" and "Beta" (both structs).
   - In main body:
     - r0 = TypeOf(Alpha), r1 = TypeOf(Alpha) — two separate TYPEOF instructions with same type_idx
     - r2 = CmpEqI(r0, r1) — should be true (same singleton)
     - r3 = TypeOf(Beta)
     - r4 = CmpEqI(r0, r3) — should be false (different types)
     - r5 = And(r2, Not(r4)) — combined: both conditions met
     - Actually simpler: just test r2 first. Use two separate subtests or chain with logic ops.
     - Simplest approach: return r2 (the equality result) and assert Value::Bool(true). Then add a second test function test_type_inequality for the false case.
   - Split into test_type_equality_same_type (returns true) and test_type_inequality_different_types (returns false).

2. **test_gc_survival_after_reflection_ops** (extends REFL-09 GC requirement):
   - Create a struct with 2 fields.
   - In main body: TypeOf -> fields() -> get first FieldInfo -> FieldInfo.get(instance) to exercise the full chain.
   - After task completes, call runtime.collect_garbage().
   - Assert objects_freed == 0 (all reflection singletons are permanent roots).
   - Then do a SECOND TypeOf call (spawn a new task) — the cached Type must still be valid.
   - Assert second task completes successfully.

Follow the exact existing patterns. For CmpEqI, use Instruction::CmpEqI { r_dst, r_a, r_b } as confirmed in research (binary.rs catch-all emits CmpEqI for ReflectionType).
  </action>
  <verify>
    <automated>cargo test -p writ-runtime test_type_equality test_type_inequality test_gc_survival_after_reflection -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Type equality tests confirm REFL-09: same type returns true via interning, different types return false. GC survival test confirms reflection heap objects persist after collection and remain usable.</done>
</task>

</tasks>

<verification>
```bash
cargo test -p writ-runtime -- reflection 2>&1 | tail -30
```
All reflection tests pass (existing 6 + new 5 = 11 total).
</verification>

<success_criteria>
- test_type_methods_returns_array passes (REFL-04)
- test_type_contracts_returns_array passes (REFL-06)
- test_type_implements_returns_bool passes (REFL-07)
- test_type_equality_same_type passes (REFL-09 — true case)
- test_type_inequality_different_types passes (REFL-09 — false case)
- test_gc_survival_after_reflection_ops passes (REFL-09 GC)
- All 6 existing reflection tests still pass (no regressions)
- REFL-03, REFL-05, REFL-08 already covered by existing test_type_fields_returns_array, test_type_attributes_from_module_attribute_view, test_field_info_get
</success_criteria>

<output>
After completion, create `.planning/phases/106-read-only-introspection-integration-tests-and-lsp/106-01-SUMMARY.md`
</output>

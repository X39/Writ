---
phase: 112-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/25_24_modules_namespaces.md
  - writ-lsp/src/queries/mod.rs
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - TEST-01
  - LSP-02

must_haves:
  truths:
    - "Section 26.4 appears as a linked entry in the spec table of contents"
    - "The spec documents that using log::* is invalid with a clear rationale"
    - "test_fn_optional runs and passes in the golden test suite"
    - "collect_dialogue_speaker_tokens re-export is absent from queries/mod.rs"
    - "The entire workspace compiles cleanly after all edits"
  artifacts:
    - path: "language-spec/spec/25_24_modules_namespaces.md"
      provides: "using log::* limitation documented in 1.24.4.4"
      contains: "using log"
    - path: "writ-lsp/src/queries/mod.rs"
      provides: "Clean re-export surface without orphaned symbol"
  key_links:
    - from: "writ-lsp/src/queries/mod.rs"
      to: "writ-lsp/src/queries/semantic.rs"
      via: "pub use re-exports"
      pattern: "pub use semantic::"
---

<objective>
Complete four housekeeping items: verify two already-done items (SPEC-01, TEST-01), add spec prose for using log::* (SPEC-02), and remove an orphaned re-export (LSP-02).

Purpose: Close out minor tech debt items so Phase 112 is complete and the codebase is clean for Phase 113.
Output: Two file edits, two verified-and-closed items, workspace compiles green.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/112-housekeeping/112-RESEARCH.md

@language-spec/spec/25_24_modules_namespaces.md
@writ-lsp/src/queries/mod.rs
</context>

<interfaces>
<!-- Current state of writ-lsp/src/queries/mod.rs lines 40-42 -->
From writ-lsp/src/queries/mod.rs:
```rust
pub use semantic::collect_semantic_tokens;
pub use semantic::collect_dialogue_speaker_tokens;  // orphaned — DELETE this line
pub use semantic::RawSemanticToken;
```

<!-- Target location in spec: after rule 4 in section 1.24.4.4, before the None/Some sub-prelude block -->
From language-spec/spec/25_24_modules_namespaces.md (lines 182-188):
```markdown
**Rules:**

1. `using Enum::*;` imports every variant of `Enum` as an unqualified name.
2. If two glob imports expose the same name ... it is an ambiguity error at the import site ...
3. The qualified form (`Direction::North`) always continues to work ...
4. `using Option::*;` is valid but has no visible effect ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify SPEC-01 and TEST-01 are already complete</name>
  <files>language-spec/spec/01_table_of_contents.md, writ-golden/tests/golden_tests.rs</files>
  <read_first>
    - language-spec/spec/01_table_of_contents.md (grep for "1.26.4" or "26.4")
    - writ-golden/tests/golden_tests.rs (grep for "test_fn_optional")
    - writ-golden/tests/golden/fn_optional.writil (confirm file exists)
  </read_first>
  <action>
Verify — do NOT implement, these are already done per research:

1. SPEC-01: Read `language-spec/spec/01_table_of_contents.md` and confirm that section 1.26.4
   (Compiler Tooling) appears as a linked entry. Expected at approximately lines 197-198.
   If present, SPEC-01 is satisfied.

2. TEST-01: Run `cargo test -p writ-golden test_fn_optional` and confirm the test passes.
   Also confirm `writ-golden/tests/golden/fn_optional.writil` exists (the blessed snapshot).
   If both true, TEST-01 is satisfied.

If either item is NOT already present (contradicting research), implement it following the
golden test pattern in RESEARCH.md. But this is not expected.
  </action>
  <verify>
    <automated>cargo test -p writ-golden test_fn_optional 2>&1 | grep -E "test result|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - grep "1.26.4" language-spec/spec/01_table_of_contents.md returns at least one match
    - cargo test -p writ-golden test_fn_optional exits 0 with "1 passed"
    - file writ-golden/tests/golden/fn_optional.writil exists
  </acceptance_criteria>
  <done>SPEC-01 verified present in TOC, TEST-01 verified passing with blessed snapshot</done>
</task>

<task type="auto">
  <name>Task 2: Add using log::* limitation note and remove orphaned re-export</name>
  <files>language-spec/spec/25_24_modules_namespaces.md, writ-lsp/src/queries/mod.rs</files>
  <read_first>
    - language-spec/spec/25_24_modules_namespaces.md (lines 180-210, the Rules block in 1.24.4.4)
    - writ-lsp/src/queries/mod.rs (full file, ~45 lines)
  </read_first>
  <action>
Two edits:

SPEC-02 — In `language-spec/spec/25_24_modules_namespaces.md`, section 1.24.4.4, after rule 4
(line ~187, before the "None and Some as sub-prelude builtins" heading), add this note block:

```markdown

> **Note:** `using log::*;` is invalid — `log` is not an enum but a namespace alias for inbuilt
> functions (see section 1.27.4). The `::*` glob form only applies to enum types. Attempting to
> glob-import a non-enum name produces **E0003 UnresolvedName**.

```

Use the blockquote note style (consistent with the existing None/Some explanation format in the
same section). Do NOT add it as rule 5 — it is a clarifying note, not a rule.

LSP-02 — In `writ-lsp/src/queries/mod.rs`, delete line 41:
```rust
pub use semantic::collect_dialogue_speaker_tokens;
```
This leaves lines 40 and 42 adjacent:
```rust
pub use semantic::collect_semantic_tokens;
pub use semantic::RawSemanticToken;
```

After both edits, run `cargo build --workspace` to confirm clean compilation.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "using log" language-spec/spec/25_24_modules_namespaces.md returns at least one match
    - grep "collect_dialogue_speaker_tokens" writ-lsp/src/queries/mod.rs returns zero matches
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>SPEC-02: using log::* limitation documented in spec section 1.24.4.4. LSP-02: orphaned re-export removed, workspace compiles clean.</done>
</task>

</tasks>

<verification>
All four requirements verified:
- SPEC-01: grep "1.26.4" in TOC returns a match
- SPEC-02: grep "using log" in modules spec returns a match
- TEST-01: cargo test -p writ-golden test_fn_optional passes
- LSP-02: grep "collect_dialogue_speaker_tokens" in mod.rs returns nothing AND cargo build --workspace succeeds

Final gate: `cargo test --workspace` passes (full suite).
</verification>

<success_criteria>
- Section 1.26.4 confirmed present in spec TOC (SPEC-01)
- using log::* limitation documented in section 1.24.4.4 (SPEC-02)
- test_fn_optional passes with blessed snapshot (TEST-01)
- collect_dialogue_speaker_tokens re-export absent from mod.rs (LSP-02)
- cargo build --workspace exits 0
- cargo test --workspace exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/112-housekeeping/112-01-SUMMARY.md`
</output>

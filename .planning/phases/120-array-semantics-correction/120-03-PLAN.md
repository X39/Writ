---
phase: 120-array-semantics-correction
plan: 03
type: execute
wave: 3
depends_on: [120-01, 120-02]
files_modified:
  - writ-golden/tests/golden/array_primitives.writ
  - writ-golden/tests/golden/type_array_ops.writ
  - writ-golden/tests/golden_tests.rs
  - language-spec/spec/07_6_primitive_types.md
  - language-spec/spec/57_3_9_arrays.md
  - language-spec/spec/67_4_2_opcode_assignment_table.md
  - language-spec/spec/65_4_0_instruction_count_by_category.md
autonomous: true
requirements: [ARR-01, ARR-02, ARR-03, ARR-04, ARR-05, ARR-06]

must_haves:
  truths:
    - "Golden test array_primitives exercises resize, copy_from, len, slice, indexed access — not add/remove_at/insert/contains"
    - "Golden test type_array_ops exercises indexed access, assignment, len, resize — not add"
    - "Collection golden tests (coll_list_basic etc.) are #[ignore]-d with Phase 121 re-enable comment"
    - "Language spec 07_6_primitive_types.md describes arrays as allocation-explicit, not growable"
    - "IL spec 57_3_9_arrays.md lists exactly 10 opcodes: NewArray through NewArrayFilled"
    - "Opcode assignment table and instruction count reflect the new opcode layout"
  artifacts:
    - path: "writ-golden/tests/golden/array_primitives.writ"
      provides: "Golden test using new array semantics"
      contains: "resize"
    - path: "writ-golden/tests/golden/type_array_ops.writ"
      provides: "Golden test for basic array ops without add"
      contains: "resize"
    - path: "writ-golden/tests/golden_tests.rs"
      provides: "Collection tests marked #[ignore]"
      contains: "#[ignore]"
    - path: "language-spec/spec/07_6_primitive_types.md"
      provides: "Updated array type description"
      contains: "allocation-explicit"
    - path: "language-spec/spec/57_3_9_arrays.md"
      provides: "Updated IL opcode table"
      contains: "ARRAY_RESIZE"
  key_links:
    - from: "writ-golden/tests/golden/array_primitives.writ"
      to: "writ-compiler builtins"
      via: "resize and copy_from dot-calls compile to new opcodes"
      pattern: "arr.resize"
---

<objective>
Update golden test fixtures, ignore collection tests, and rewrite language spec sections to match the new fixed-size array semantics.

Purpose: After Plans 01-02 changed the instruction set and wired it through compiler/runtime, the golden tests must be rewritten to use new methods, collection tests must be temporarily ignored (Phase 121 fixes them), and the spec must document the new semantics.
Output: All golden tests pass (or are intentionally ignored), spec documents updated. `cargo test --workspace` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-array-semantics-correction/120-CONTEXT.md
@.planning/phases/120-array-semantics-correction/120-RESEARCH.md
@.planning/phases/120-array-semantics-correction/120-01-SUMMARY.md
@.planning/phases/120-array-semantics-correction/120-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite golden test fixtures and ignore collection tests</name>
  <files>writ-golden/tests/golden/array_primitives.writ, writ-golden/tests/golden/type_array_ops.writ, writ-golden/tests/golden_tests.rs</files>
  <read_first>
    - writ-golden/tests/golden/array_primitives.writ (current fixture using add/remove_at/insert/contains)
    - writ-golden/tests/golden/type_array_ops.writ (current fixture using add)
    - writ-golden/tests/golden_tests.rs (collection test function locations — search for golden_coll_)
  </read_first>
  <action>
**A) Rewrite `writ-golden/tests/golden/array_primitives.writ`:**

Replace the entire file with a fixture that exercises the NEW array API:

```writ
fn main() {
    let mut arr: int[] = [1, 2, 3];

    // len (retained — ARR-04)
    let _len = arr.len();

    // indexed access (retained — ARR-04)
    let _first = arr[0];
    arr[1] = 42;

    // resize grow — new slots filled with default (0 for int) (ARR-02)
    arr.resize(5);
    let _len_after_grow = arr.len();
    let _new_slot = arr[3];

    // resize shrink — truncation (ARR-02)
    arr.resize(2);
    let _len_after_shrink = arr.len();

    // slice (retained — ARR-04)
    let sub = arr.slice(0, 2);
    let _sub_len = sub.len();

    // copy_from — cross-array copy (ARR-03)
    let mut dst: int[] = [0, 0, 0, 0, 0];
    let src: int[] = [10, 20, 30];
    dst.copy_from(src, 0, 1, 3);
    let _copied = dst[1];

    // copy_from — same array overlap (memmove semantics, D-09)
    let mut overlap: int[] = [1, 2, 3, 4, 5];
    overlap.copy_from(overlap, 0, 1, 3);
    let _shifted = overlap[1];
}
```

**B) Rewrite `writ-golden/tests/golden/type_array_ops.writ`:**

Replace the entire file with:

```writ
fn main() {
    let mut arr: int[] = [1, 2, 3];
    let first: int = arr[0];
    arr[1] = 42;
    let size: int = arr.len();
    arr.resize(5);
}
```

**C) After rewriting .writ files, bless the snapshots:**

```bash
BLESS=1 cargo test -p writ-golden -- golden_array_primitives golden_type_array_ops 2>&1
```

This regenerates the `.writil` snapshot files to match the new compiler output.

**D) In `writ-golden/tests/golden_tests.rs`, add `#[ignore]` to these collection test functions:**

- `golden_coll_list_basic` (around line 1126)
- `golden_coll_map_basic` (around line 1135)
- `golden_coll_set_basic` (around line 1144)
- `golden_coll_hashmap_basic` (around line 1153)
- `golden_coll_list_map` (around line 1162)
- `golden_coll_list_filter` (around line 1171)
- `golden_coll_list_reduce` (around line 1180)

For each, add `#[ignore]` attribute and a comment. Example:

```rust
#[test]
#[ignore] // Phase 121: re-enable after stdlib rewrite
fn golden_coll_list_basic() {
    run_golden_test("coll_list_basic");
}
```

Also check for any other golden tests that reference `collections.writ` or use the old array methods — search the golden directory for `.add(`, `.remove_at(`, `.insert(`, `.contains(` in .writ files and handle accordingly.
  </action>
  <verify>
    <automated>cargo test -p writ-golden 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - array_primitives.writ contains `arr.resize(5)` and `dst.copy_from(`
    - array_primitives.writ does NOT contain `.add(` or `.remove_at(` or `.insert(` or `.contains(`
    - type_array_ops.writ contains `arr.resize(5)`
    - type_array_ops.writ does NOT contain `.add(`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_list_basic`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_map_basic`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_set_basic`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_hashmap_basic`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_list_map`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_list_filter`
    - golden_tests.rs contains `#[ignore] // Phase 121` above `fn golden_coll_list_reduce`
    - `cargo test -p writ-golden` passes (ignoring collection tests)
  </acceptance_criteria>
  <done>Golden array tests exercise new API (resize, copy_from, len, slice, indexing), collection tests are #[ignore]-d, all non-ignored golden tests pass with blessed snapshots</done>
</task>

<task type="auto">
  <name>Task 2: Update language spec and IL spec documents</name>
  <files>language-spec/spec/07_6_primitive_types.md, language-spec/spec/57_3_9_arrays.md, language-spec/spec/67_4_2_opcode_assignment_table.md, language-spec/spec/65_4_0_instruction_count_by_category.md</files>
  <read_first>
    - language-spec/spec/07_6_primitive_types.md (full file — sections 1.6.1 through 1.6.3)
    - language-spec/spec/57_3_9_arrays.md (full file — current opcode table)
    - language-spec/spec/67_4_2_opcode_assignment_table.md (0x09 section)
    - language-spec/spec/65_4_0_instruction_count_by_category.md (Arrays row)
    - .planning/phases/120-array-semantics-correction/120-CONTEXT.md (D-10 through D-13 spec wording decisions)
  </read_first>
  <action>
**A) language-spec/spec/07_6_primitive_types.md — Rewrite section 1.6.1:**

Replace the current "Arrays are ordered, growable, homogeneous collections" paragraph with (per D-10):

```markdown
## 1.6.1 Arrays

Arrays are ordered, homogeneous collections with explicit allocation. They are a compiler-known
semi-primitive — not a single machine word, but with literal syntax and built-in operations. Like
`string`, the compiler understands arrays directly; they are not a standard library type.

Size changes require reallocation via `resize(n)`. Arrays do not grow implicitly — there is no
`add`, `push`, or `append` operation. To build dynamic collections, use `List<T>` from the
standard library.

The array type is written with postfix `[]` notation: `int[]`, `string[]`, `vec2[]`.
```

Update the type table from "Ordered, growable collection of T" to "Ordered, allocation-explicit collection of T":

```markdown
| Type  | Description                                    | Default Value | Literal Syntax |
|-------|------------------------------------------------|---------------|----------------|
| `T[]` | Ordered, allocation-explicit collection of `T` | `[]`          | `[expr, ...]`  |
```

Replace the operations subsection (1.6.2 or equivalent) to list ONLY these operations:
- `arr[i]` — indexed access (read)
- `arr[i] = val` — indexed access (write)
- `arr.len()` — returns length as int
- `arr.slice(start, end)` — returns a new sub-array (start inclusive, end exclusive)
- `arr.resize(n)` — reallocates to new size; new slots filled with type defaults, excess elements truncated
- `arr.copy_from(src, src_idx, dst_idx, len)` — bulk-copies len elements from src[src_idx..] into receiver[dst_idx..]

Remove ALL mention of: `add`, `remove_at`, `insert`, `contains` from the operations list and any examples.

**B) language-spec/spec/57_3_9_arrays.md — Rewrite opcode table:**

Replace the entire table with this (per D-03, D-12, D-13):

```markdown
# 3.9 Arrays

| Mnemonic          | Shape | Operands                                         | Description                                                                                                                                               |
|-------------------|-------|--------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------|
| `NEW_ARRAY`       | RI32  | r_dst, elem_type:u32                             | Create a new empty array (length 0) with the given element type.                                                                                          |
| `ARRAY_INIT`      | var   | r_dst, elem_type:u32, count:u16, r_base          | Create an array pre-filled from consecutive registers r_base..r_base+count-1. Encoding: `u16(op) u16(r_dst) u32(elem_type) u16(count) u16(r_base)` = 12B. |
| `ARRAY_LOAD`      | RRR   | r_dst, r_arr, r_idx                              | Read element at index. Crash on out-of-bounds.                                                                                                            |
| `ARRAY_STORE`     | RRR   | r_arr, r_idx, r_val                              | Write element at index. Crash on out-of-bounds.                                                                                                           |
| `ARRAY_LEN`       | RR    | r_dst, r_arr                                     | Get array length as int.                                                                                                                                  |
| `ARRAY_RESIZE`    | RR    | r_arr, r_new_len                                 | Resize array to r_new_len. If growing, new slots filled with type defaults. If shrinking, excess elements dropped. Negative length crashes.                |
| `ARRAY_COPY`      | var   | r_dst_arr, r_dst_idx, r_src_arr, r_src_idx, r_len | Copy r_len elements from src[src_idx..] to dst[dst_idx..]. Handles same-array overlap (memmove). OOB crashes. Encoding: 5x u16 operands = 12B.           |
| `ARRAY_SLICE`     | var   | r_dst, r_arr, r_start, r_end                     | Create a sub-array copy from index r_start (inclusive) to r_end (exclusive). Encoding: `u16(op) u16(r_dst) u16(r_arr) u16(r_start) u16(r_end)` = 10B.     |
| `NEW_ARRAY_SIZED` | var   | r_dst, elem_type:u32, r_len                      | Create array of r_len elements filled with type defaults. Encoding: `u16(op) u16(r_dst) u32(elem_type) u16(r_len)` = 10B.                                 |
| `NEW_ARRAY_FILLED`| var   | r_dst, elem_type:u32, r_len, r_fill              | Create array of r_len elements filled with value in r_fill. Encoding: `u16(op) u16(r_dst) u32(elem_type) u16(r_len) u16(r_fill)` = 12B.                   |
```

**C) language-spec/spec/67_4_2_opcode_assignment_table.md — Update 0x09 section:**

Find the 0x09 rows and replace with:

```
| 0x0900 | NEW_ARRAY       |
| 0x0901 | ARRAY_INIT      |
| 0x0902 | ARRAY_LOAD      |
| 0x0903 | ARRAY_STORE     |
| 0x0904 | ARRAY_LEN       |
| 0x0905 | ARRAY_RESIZE    |
| 0x0906 | ARRAY_COPY      |
| 0x0907 | ARRAY_SLICE     |
| 0x0908 | NEW_ARRAY_SIZED |
| 0x0909 | NEW_ARRAY_FILLED|
```

**D) language-spec/spec/65_4_0_instruction_count_by_category.md — Update Arrays row:**

The array count stays at 10 (was 10: NewArray through ArrayContains; now 10: NewArray through NewArrayFilled). Verify the existing count and update if different. The category name may also reference "growable" — change to "Arrays" or "Array Operations" if so.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 07_6_primitive_types.md contains "allocation-explicit"
    - 07_6_primitive_types.md contains "resize(n)"
    - 07_6_primitive_types.md contains "copy_from"
    - 07_6_primitive_types.md does NOT contain "growable"
    - 07_6_primitive_types.md does NOT contain `.add(` or `remove_at` or `.insert(` in operations list
    - 57_3_9_arrays.md contains `ARRAY_RESIZE`
    - 57_3_9_arrays.md contains `ARRAY_COPY`
    - 57_3_9_arrays.md contains `NEW_ARRAY_SIZED`
    - 57_3_9_arrays.md contains `NEW_ARRAY_FILLED`
    - 57_3_9_arrays.md does NOT contain `ARRAY_ADD`
    - 57_3_9_arrays.md does NOT contain `ARRAY_REMOVE`
    - 57_3_9_arrays.md does NOT contain `ARRAY_INSERT`
    - 57_3_9_arrays.md does NOT contain `ARRAY_CONTAINS`
    - 67_4_2_opcode_assignment_table.md contains `0x0905 | ARRAY_RESIZE`
    - 67_4_2_opcode_assignment_table.md contains `0x0907 | ARRAY_SLICE`
    - `cargo test --workspace` passes (collection tests ignored)
  </acceptance_criteria>
  <done>Language spec describes arrays as allocation-explicit with resize/copy_from operations, IL spec documents all 10 opcodes with correct assignments, opcode table and instruction count are consistent, full workspace test suite passes</done>
</task>

</tasks>

<verification>
cargo test --workspace 2>&1

Full workspace test suite passes. Collection golden tests are #[ignore]-d (expected — Phase 121 re-enables them). Array golden tests use new API and produce correct IL snapshots.
</verification>

<success_criteria>
- All golden tests pass (array_primitives, type_array_ops exercise new semantics)
- Collection tests are #[ignore]-d with "Phase 121" comments
- Language spec says "allocation-explicit", not "growable"
- IL opcode table has 10 array opcodes: 0x0900-0x0909 contiguous
- Spec mentions resize, copy_from, len, slice as the built-in operations
- Spec does NOT mention add, remove_at, insert, contains
- `cargo test --workspace` is green (minus ignored tests)
</success_criteria>

<output>
After completion, create `.planning/phases/120-array-semantics-correction/120-03-SUMMARY.md`
</output>

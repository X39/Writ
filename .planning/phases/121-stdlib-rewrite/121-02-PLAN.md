---
phase: 121-stdlib-rewrite
plan: 02
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - writ-golden/tests/golden_tests.rs
  - writ-golden/tests/golden/coll_list_basic.writil
  - writ-golden/tests/golden/coll_map_basic.writil
  - writ-golden/tests/golden/coll_set_basic.writil
  - writ-golden/tests/golden/coll_hashmap_basic.writil
  - writ-golden/tests/golden/coll_list_map.writil
  - writ-golden/tests/golden/coll_list_filter.writil
  - writ-golden/tests/golden/coll_list_reduce.writil
  - writ-golden/tests/golden/iter_for_in_list.writil
autonomous: true
requirements: [STD-01, STD-02, STD-03, STD-04, STD-05]

must_haves:
  truths:
    - "All 7 collection golden tests pass (not ignored)"
    - "The iter_for_in_list golden test passes (not ignored)"
    - "Golden IL snapshots reflect the new resize+store opcodes instead of the old add/remove_at opcodes"
    - "writ-cli builds without compilation failure warnings"
  artifacts:
    - path: "writ-golden/tests/golden_tests.rs"
      provides: "8 un-ignored test functions"
      contains: "fn golden_coll_list_basic"
    - path: "writ-golden/tests/golden/coll_list_basic.writil"
      provides: "Blessed IL snapshot for List basic operations"
      contains: "ARRAY_RESIZE"
  key_links:
    - from: "writ-golden/tests/golden_tests.rs"
      to: "writ-golden/tests/golden/*.writil"
      via: "run_golden_test reads .writ, compiles, compares to .writil"
      pattern: "run_golden_test"
    - from: "writ-golden/tests/golden/*.writil"
      to: "writ-std/src/collections.writ"
      via: "compiler includes stdlib when compiling golden .writ files that use collections"
      pattern: "ARRAY_RESIZE"
---

<objective>
Un-ignore all 8 golden tests that were disabled during Phase 120 (7 collection tests + iter_for_in_list), re-bless their IL snapshots to reflect the rewritten stdlib, and verify the full golden test suite passes.

Purpose: Prove that the rewritten collections.writ produces correct IL and that the public API works end-to-end through the compiler.
Output: 8 passing golden tests with updated .writil snapshots.
</objective>

<execution_context>
@A:\Users\msili\.claude\get-shit-done\workflows\execute-plan.md
@A:\Users\msili\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-stdlib-rewrite/121-RESEARCH.md
@.planning/phases/121-stdlib-rewrite/121-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-ignore collection and iterator golden tests</name>
  <files>writ-golden/tests/golden_tests.rs</files>
  <read_first>
    - writ-golden/tests/golden_tests.rs (find all 8 #[ignore] lines with Phase 121 comments)
  </read_first>
  <action>
Remove the `#[ignore]` attribute (and its trailing comment) from all 8 test functions that have `// Phase 121` markers. The 8 functions are:

1. `golden_coll_list_basic` — has `#[ignore] // Phase 121: re-enable after stdlib rewrite`
2. `golden_coll_map_basic` — same pattern
3. `golden_coll_set_basic` — same pattern
4. `golden_coll_hashmap_basic` — same pattern
5. `golden_coll_list_map` — same pattern
6. `golden_coll_list_filter` — same pattern
7. `golden_coll_list_reduce` — same pattern
8. `golden_iter_for_in_list` — same pattern (last one, near line 1214)

For each, delete the entire line containing `#[ignore] // Phase 121`. The `#[test]` attribute on the line above and the `fn golden_...()` on the line below stay.

Also remove the doc comment lines that say `/// Phase 121: re-enable after stdlib is rewritten...` above each test (one line per test).

Do NOT modify any other test functions. Do NOT touch any `.writ` driver files (the golden test inputs are unchanged — they test the public collection API which is the same).
  </action>
  <verify>
    <automated>cd A:/dev/git/Writ && grep -c "Phase 121" writ-golden/tests/golden_tests.rs</automated>
  </verify>
  <acceptance_criteria>
    - writ-golden/tests/golden_tests.rs contains ZERO lines matching `Phase 121`
    - writ-golden/tests/golden_tests.rs contains ZERO `#[ignore]` attributes on golden_coll_* or golden_iter_for_in_list functions
    - All 8 functions still exist: `fn golden_coll_list_basic()`, `fn golden_coll_map_basic()`, `fn golden_coll_set_basic()`, `fn golden_coll_hashmap_basic()`, `fn golden_coll_list_map()`, `fn golden_coll_list_filter()`, `fn golden_coll_list_reduce()`, `fn golden_iter_for_in_list()`
    - Each function still has `#[test]` attribute
    - Each function body still calls `run_golden_test("coll_...")` or `run_golden_test("iter_for_in_list")`
  </acceptance_criteria>
  <done>All 8 Phase 121 golden tests are un-ignored and will run in the next `cargo test -p writ-golden` invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Re-bless golden IL snapshots and verify all tests pass</name>
  <files>
    writ-golden/tests/golden/coll_list_basic.writil,
    writ-golden/tests/golden/coll_map_basic.writil,
    writ-golden/tests/golden/coll_set_basic.writil,
    writ-golden/tests/golden/coll_hashmap_basic.writil,
    writ-golden/tests/golden/coll_list_map.writil,
    writ-golden/tests/golden/coll_list_filter.writil,
    writ-golden/tests/golden/coll_list_reduce.writil,
    writ-golden/tests/golden/iter_for_in_list.writil
  </files>
  <read_first>
    - writ-golden/tests/golden_tests.rs (confirm #[ignore] removed from Task 1)
    - writ-golden/tests/golden/coll_list_basic.writil (see current stale snapshot — will contain old ARRAY_ADD opcodes)
  </read_first>
  <action>
Run the BLESS=1 command to regenerate all 8 golden IL snapshots, then run the full golden test suite to verify.

Step 1: Bless collection and iterator snapshots:
```bash
cd A:/dev/git/Writ && BLESS=1 cargo test -p writ-golden -- coll iter_for_in_list
```
This compiles each .writ driver file (which imports from collections.writ via the stdlib), generates the IL output, and writes it to the corresponding .writil file.

Step 2: Verify all 8 tests pass without BLESS:
```bash
cargo test -p writ-golden -- coll iter_for_in_list
```
All 8 should pass (snapshot matches compiler output).

Step 3: Run the full golden test suite to confirm no regressions:
```bash
cargo test -p writ-golden
```
Pre-existing failures in non-collection tests (generic_inherent_impl, lib_preload_stub, etc.) may still fail — those are out of scope per Phase 120-03 notes. All collection and iterator tests must pass.

Step 4: Verify writ-cli builds cleanly:
```bash
cargo build -p writ-cli 2>&1 | grep -i "writ-std compilation failed" || echo "BUILD CLEAN"
```
Should print "BUILD CLEAN" — the placeholder .writc mechanism is no longer triggered because collections.writ compiles successfully.

If any collection/iterator test fails after blessing, read the test output diff to identify which method in collections.writ emits incorrect IL. Fix the source in collections.writ, re-bless, and re-verify.
  </action>
  <verify>
    <automated>cd A:/dev/git/Writ && cargo test -p writ-golden -- coll iter_for_in_list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p writ-golden -- coll iter_for_in_list` reports `test result: ok. 8 passed; 0 failed`
    - coll_list_basic.writil contains `ARRAY_RESIZE` (not `ARRAY_ADD`)
    - coll_list_basic.writil does NOT contain `ARRAY_ADD` or `ARRAY_REMOVE`
    - `cargo build -p writ-cli` output does NOT contain `writ-std compilation failed`
    - All 8 .writil files have been modified (git diff shows changes)
  </acceptance_criteria>
  <done>All 8 golden tests pass with blessed snapshots reflecting the new fixed-size array IL. The writ-cli build produces a real stdlib .writc module. STD-05 is satisfied.</done>
</task>

</tasks>

<verification>
- `cargo test -p writ-golden -- coll iter_for_in_list` — 8 tests pass, 0 failures
- `cargo build -p writ-cli 2>&1 | grep "compilation failed"` — no output (clean build)
- `grep -c "ARRAY_ADD\|ARRAY_REMOVE\|ARRAY_CONTAINS" writ-golden/tests/golden/coll_list_basic.writil` returns 0
- `grep -c "ARRAY_RESIZE" writ-golden/tests/golden/coll_list_basic.writil` returns a positive number
</verification>

<success_criteria>
All 8 Phase 121 golden tests pass. IL snapshots reflect the new resize+store array operations. writ-cli produces a valid stdlib .writc without fallback to empty placeholder. Phase 121 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/121-stdlib-rewrite/121-02-SUMMARY.md`
</output>

---
phase: 22-name-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - writ-diagnostics/Cargo.toml
  - writ-diagnostics/src/lib.rs
  - writ-diagnostics/src/diagnostic.rs
  - writ-diagnostics/src/render.rs
  - writ-diagnostics/src/code.rs
  - writ-compiler/Cargo.toml
  - writ-compiler/src/lib.rs
  - writ-compiler/src/config.rs
  - writ-compiler/src/resolve/mod.rs
  - writ-compiler/src/resolve/def_map.rs
  - writ-compiler/src/resolve/collector.rs
  - writ-compiler/src/resolve/prelude.rs
  - writ-compiler/src/resolve/ir.rs
  - writ-compiler/src/resolve/error.rs
  - writ-compiler/tests/resolve_tests.rs
autonomous: true
requirements:
  - RES-01

must_haves:
  truths:
    - "All top-level declarations across multiple files are collected into a single DefMap keyed by fully-qualified namespace::Name"
    - "Prelude types (Option, Result, Range, Array, Entity) and all 17 contracts are present in the built-in scope layer"
    - "Defining a type with a prelude name produces a compile error (not a shadowing warning)"
    - "Diagnostics render with colored spans, error codes, and help notes via ariadne"
    - "writ.toml is parsed to discover source directories and recursively find .writ files"
    - "Namespace/path mismatch emits W0004 warning when file path does not mirror the declared namespace"
  artifacts:
    - path: "writ-diagnostics/src/lib.rs"
      provides: "Shared diagnostic types and rendering"
    - path: "writ-compiler/src/resolve/def_map.rs"
      provides: "DefMap, DefEntry, DefId, DefKind, DefVis, NamespaceMap"
    - path: "writ-compiler/src/resolve/collector.rs"
      provides: "Pass 1: collect_declarations() populating DefMap from Vec<(FileId, Ast)>"
    - path: "writ-compiler/src/resolve/prelude.rs"
      provides: "PRELUDE_TYPES, PRELUDE_CONTRACTS, is_prelude_name(), resolve_prelude()"
    - path: "writ-compiler/src/resolve/ir.rs"
      provides: "NameResolvedAst, ResolvedDecl, ResolvedType, DefId"
    - path: "writ-compiler/src/config.rs"
      provides: "WritConfig, load_config(), file discovery"
  key_links:
    - from: "writ-compiler/src/resolve/collector.rs"
      to: "writ-compiler/src/resolve/def_map.rs"
      via: "collector populates DefMap entries"
      pattern: "def_map\\.insert"
    - from: "writ-compiler/src/resolve/collector.rs"
      to: "writ-compiler/src/resolve/prelude.rs"
      via: "prelude shadow check during collection"
      pattern: "is_prelude_name"
    - from: "writ-diagnostics/src/render.rs"
      to: "ariadne"
      via: "Report::build for rendering"
      pattern: "ariadne::Report"
---

<objective>
Create the foundation infrastructure for Phase 22 name resolution: the shared `writ-diagnostics` crate for Rust-style error rendering, `writ.toml` config parsing with file discovery, the `DefMap` symbol table with `DefId` arena allocation, the Pass 1 declaration collector, the prelude injection layer, and the `NameResolvedAst` IR type definitions.

Purpose: All subsequent resolution work (Pass 2 resolver, validation, error quality) depends on the DefMap being fully populated with every top-level declaration. This plan establishes the data structures, the collection pass, and the diagnostic infrastructure that Plans 02 and 03 build upon.

Output: Working Pass 1 that collects all declaration kinds (fn, struct, entity, enum, contract, impl, component, extern, const, global) into a DefMap, with prelude shadow protection, namespace handling (both declarative and block forms), and file-private vs pub visibility tracking. Snapshot tests verify the DefMap contents.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-name-resolution/22-CONTEXT.md
@.planning/phases/22-name-resolution/22-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From writ-compiler/src/ast/mod.rs:
```rust
pub struct Ast {
    pub items: Vec<AstDecl>,
}
```

From writ-compiler/src/ast/decl.rs (all declaration variants the collector must handle):
```rust
pub enum AstDecl {
    Namespace(AstNamespaceDecl),
    Using(AstUsingDecl),
    Fn(AstFnDecl),
    Struct(AstStructDecl),
    Entity(AstEntityDecl),
    Enum(AstEnumDecl),
    Contract(AstContractDecl),
    Impl(AstImplDecl),
    Component(AstComponentDecl),
    Extern(AstExternDecl),
    Const(AstConstDecl),
    Global(AstGlobalDecl),
    Stmt(AstStmt),
}

pub enum AstNamespaceDecl {
    Declarative { path: Vec<String>, span: SimpleSpan },
    Block { path: Vec<String>, items: Vec<AstDecl>, span: SimpleSpan },
}

pub enum AstVisibility { Pub, Priv }
```

From writ-compiler/src/ast/types.rs:
```rust
pub enum AstType {
    Named { name: String, span: SimpleSpan },
    Generic { name: String, args: Vec<AstType>, span: SimpleSpan },
    Array { elem: Box<AstType>, span: SimpleSpan },
    Func { params: Vec<AstType>, ret: Option<Box<AstType>>, span: SimpleSpan },
    Void { span: SimpleSpan },
}
```

From writ-compiler/src/lower/context.rs (pattern to follow for error accumulation):
```rust
pub struct LoweringContext {
    errors: Vec<LoweringError>,
    speaker_stack: Vec<SpeakerScope>,
    namespace_stack: Vec<String>,
}
impl LoweringContext {
    pub fn emit_error(&mut self, err: LoweringError) { self.errors.push(err); }
    pub fn take_errors(self) -> Vec<LoweringError> { self.errors }
}
```

From Cargo.toml workspace:
```toml
[workspace]
resolver = "3"
members = ["writ-assembler", "writ-cli", "writ-compiler", "writ-module", "writ-parser", "writ-runtime"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Create writ-diagnostics crate, config module, and writ-compiler dependency updates</name>
  <files>
    Cargo.toml,
    writ-diagnostics/Cargo.toml,
    writ-diagnostics/src/lib.rs,
    writ-diagnostics/src/diagnostic.rs,
    writ-diagnostics/src/render.rs,
    writ-diagnostics/src/code.rs,
    writ-compiler/Cargo.toml,
    writ-compiler/src/lib.rs,
    writ-compiler/src/config.rs
  </files>
  <behavior>
    - Test: Diagnostic struct renders via ariadne with error code, message, span labels, and help note
    - Test: render_diagnostics with Error severity produces output containing the error code and message
    - Test: render_diagnostics with Warning severity produces output containing the warning code
    - Test: WritConfig parses a basic writ.toml with [project], [locale], [compiler].sources
    - Test: discover_source_files finds .writ files recursively in configured source directories
    - Test: load_config returns MissingToml error when writ.toml does not exist
  </behavior>
  <action>
    **Step 1: Create writ-diagnostics crate**

    Add `writ-diagnostics` to workspace members in root `Cargo.toml`.

    Create `writ-diagnostics/Cargo.toml` with dependencies: `ariadne = "0.6"`, `thiserror = "2.0"`.

    Create `writ-diagnostics/src/lib.rs` re-exporting: `Diagnostic`, `DiagnosticKind`, `Label`, `Severity`, `render_diagnostics()`.

    Create `writ-diagnostics/src/diagnostic.rs`:
    - `Severity` enum: Error, Warning, Note
    - `Diagnostic` struct with: `severity`, `code: String` (e.g., "E0001"), `message: String`, `primary_span: (FileId, SimpleSpan)`, `primary_label: String`, `secondary_labels: Vec<SecondaryLabel>`, `help: String`, `notes: Vec<String>`
    - `SecondaryLabel` struct: `span: (FileId, SimpleSpan)`, `message: String`
    - `FileId` as a `#[derive(Copy, Clone, Hash, Eq, PartialEq, Debug)] pub struct FileId(pub u32);`

    Create `writ-diagnostics/src/render.rs`:
    - `render_diagnostics(diagnostics: &[Diagnostic], sources: &[(FileId, &str, &str)]) -> String` — renders all diagnostics to a string using ariadne. Each source is (FileId, filename, source_text).
    - Build ariadne `Report` per diagnostic: `ReportKind::Error` or `::Warning` based on severity. Use `with_code()`, `with_message()`, `with_label()` for primary (red) and secondary (blue) labels. Use `with_note()` for help text. Use `with_help()` for notes.
    - Multi-file: use `ariadne::sources()` with an iterator of `(FileId, &str)` pairs for the cache.
    - ANSI colors: red for errors, yellow for warnings, blue for secondary labels, bold for emphasis. (ariadne handles this automatically.)

    Create `writ-diagnostics/src/code.rs`:
    - Error code constants: `pub const E0001: &str = "E0001";` (duplicate definition), `pub const E0002: &str = "E0002";` (prelude shadow), `pub const E0003: &str = "E0003";` (unresolved name), `pub const E0004: &str = "E0004";` (ambiguous name), `pub const E0005: &str = "E0005";` (visibility violation), `pub const E0006: &str = "E0006";` (invalid attribute target), `pub const E0007: &str = "E0007";` (invalid speaker)
    - Warning code constants: `pub const W0001: &str = "W0001";` (unused import), `pub const W0002: &str = "W0002";` (import shadow), `pub const W0003: &str = "W0003";` (generic shadow), `pub const W0004: &str = "W0004";` (namespace path mismatch)

    **Step 2: Add writ-compiler dependencies and modules**

    Update `writ-compiler/Cargo.toml` to add: `toml = "0.9"`, `serde = { version = "1", features = ["derive"] }`, `walkdir = "2"`, `strsim = "0.11"`, `rustc-hash = "2.1.1"`, `id-arena = "2.3.0"`, `writ-diagnostics = { path = "../writ-diagnostics" }`.

    Update `writ-compiler/src/lib.rs` to add: `pub mod resolve;`, `pub mod config;`.

    **Step 3: Create config module**

    Create `writ-compiler/src/config.rs`:
    - `WritConfig` with serde Deserialize: `project: ProjectConfig` (name, version), `locale: Option<LocaleConfig>` (default_locale, locales), `compiler: CompilerConfig` (sources with default `["src/"]`, output), `conditions: HashMap<String, bool>`.
    - `load_config(project_root: &Path) -> Result<WritConfig, ConfigError>` — reads `writ.toml` from project root.
    - `discover_source_files(project_root: &Path, config: &WritConfig) -> Result<Vec<PathBuf>, ConfigError>` — uses `walkdir` to recursively find all `.writ` files in each configured source directory.
    - `ConfigError` enum with `Io`, `Parse`, `MissingToml` variants using thiserror.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-diagnostics && cargo test -p writ-compiler -- config</automated>
  </verify>
  <done>
    - writ-diagnostics crate compiles and exports Diagnostic/render types
    - Diagnostic rendering via ariadne produces colored output with error codes and spans
    - config.rs parses writ.toml with serde and discovers .writ source files
    - All diagnostic and config tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Create resolve/ module skeleton with DefMap, collector, prelude, IR, and error types</name>
  <files>
    writ-compiler/src/resolve/mod.rs,
    writ-compiler/src/resolve/def_map.rs,
    writ-compiler/src/resolve/collector.rs,
    writ-compiler/src/resolve/prelude.rs,
    writ-compiler/src/resolve/ir.rs,
    writ-compiler/src/resolve/error.rs,
    writ-compiler/tests/resolve_tests.rs
  </files>
  <behavior>
    - Test: collect_declarations on a single-file Ast with fn, struct, entity, enum, contract, impl, component, extern fn, const, global produces DefMap entries for all 10 kinds with correct fully-qualified names
    - Test: collect_declarations with declarative namespace (`namespace survival;`) prefixes all decl names with "survival::"
    - Test: collect_declarations with block namespace (`namespace a { namespace b { struct X {} } }`) produces key "a::b::X"
    - Test: collect_declarations across two files sharing the same namespace merges pub declarations into the same namespace
    - Test: private declarations only visible in their source file's file_private map, not the shared namespace_pub map
    - Test: defining a type named "Option" or "Entity" or "Add" produces a prelude-shadow error diagnostic
    - Test: defining a type named "Result" or "Array" or "Range" or any of the 17 contract names produces a prelude-shadow error
    - Test: duplicate definitions (same FQN from different files) produce a duplicate-definition error with both spans
    - Test: file at path `src/combat/weapons.writ` with `namespace survival;` produces W0004 warning about namespace/path mismatch
    - Test: file at path `src/survival/potions.writ` with `namespace survival;` produces no W0004 warning (path matches)
  </behavior>
  <action>
    Create `writ-compiler/src/resolve/mod.rs`:
    - `pub mod def_map;`, `pub mod collector;`, `pub mod prelude;`, `pub mod ir;`, `pub mod error;`
    - `pub fn resolve(asts: Vec<(FileId, Ast)>) -> (NameResolvedAst, Vec<Diagnostic>)` — stub entry point that calls collector Pass 1 then returns a placeholder NameResolvedAst. Pass 2 body resolution added in Plan 02.

    Create `writ-compiler/src/resolve/def_map.rs`:
    - Use `id_arena::Arena` for `DefId` (type alias: `pub type DefId = id_arena::Id<DefEntry>;`).
    - `DefMap` struct with:
      - `arena: Arena<DefEntry>` — all definitions
      - `by_fqn: FxHashMap<String, DefId>` — key: "namespace::Name" for pub defs
      - `file_private: FxHashMap<FileId, FxHashMap<String, DefId>>` — per-file private defs (key: simple name)
      - `namespace_members: FxHashMap<String, Vec<DefId>>` — namespace to list of pub member DefIds
      - `impl_blocks: Vec<DefId>` — all impl block DefIds (for later association in Plan 02)
    - `DefEntry` struct: `id: DefId` (set after arena insertion), `kind: DefKind`, `vis: DefVis`, `file_id: FileId`, `namespace: String`, `name: String`, `name_span: SimpleSpan`, `generics: Vec<String>`, `span: SimpleSpan`
    - `DefKind` enum: `Fn`, `Struct`, `Entity`, `Enum`, `Contract`, `Impl`, `Component`, `ExternFn`, `ExternStruct`, `ExternComponent`, `Const`, `Global`
    - `DefVis` enum: `Pub`, `Private`
    - `DefMap` methods: `insert(&mut self, fqn: String, entry: DefEntry, diags: &mut Vec<Diagnostic>)` — checks for duplicate FQN (emit E0001 with both spans if exists), inserts into arena and appropriate map based on visibility. `get(&self, fqn: &str) -> Option<DefId>`. `get_entry(&self, id: DefId) -> &DefEntry`. `pub_members_of(&self, namespace: &str) -> &[DefId]`.

    Create `writ-compiler/src/resolve/prelude.rs`:
    - `PRELUDE_PRIMITIVE_NAMES: &[&str] = &["int", "float", "bool", "string", "void"];`
    - `PRELUDE_TYPE_NAMES: &[&str] = &["Option", "Result", "Range", "Array", "Entity"];`
    - `PRELUDE_CONTRACT_NAMES: &[&str] = &["Add", "Sub", "Mul", "Div", "Mod", "Neg", "Not", "Eq", "Ord", "Index", "IndexSet", "BitAnd", "BitOr", "Iterable", "Iterator", "Into", "Error"];`
    - `pub fn is_prelude_name(name: &str) -> bool` — checks all three arrays.
    - `pub fn is_primitive_name(name: &str) -> bool` — checks primitives only.

    Create `writ-compiler/src/resolve/ir.rs`:
    - `NameResolvedAst` struct: `decls: Vec<ResolvedDecl>`, `def_map: DefMap` (the DefMap travels with the IR for downstream use).
    - `ResolvedType` enum: `Primitive(PrimitiveTag)`, `Named { def_id: DefId, type_args: Vec<ResolvedType> }`, `Array(Box<ResolvedType>)`, `Func { params: Vec<ResolvedType>, ret: Box<ResolvedType> }`, `Void`, `Error` (error recovery).
    - `PrimitiveTag` enum: `Int`, `Float`, `Bool`, `String`, `Void`.
    - `ResolvedDecl` — placeholder enum mirroring AstDecl variants but with DefIds attached. Full population happens in Plan 02 (resolver). For now, define the enum with stub variants.

    Create `writ-compiler/src/resolve/error.rs`:
    - `ResolutionError` enum with thiserror derives for: `DuplicateDefinition { name, first_span, second_span, first_file, second_file }`, `PreludeShadow { name, span }`, `UnresolvedName { name, span }`, `AmbiguousName { name, candidates: Vec<...>, span }`, `VisibilityViolation { ... }`, `NamespacePathMismatch { declared_ns, file_path, span }`, etc.
    - `impl From<ResolutionError> for Diagnostic` — converts each error variant to a full Diagnostic with code, labels, help text.
    - `NamespacePathMismatch` produces W0004: "namespace `{declared_ns}` does not match file path `{file_path}`", help: "convention is for file path to mirror the namespace (e.g., `src/{ns_as_path}/`)".

    Create `writ-compiler/src/resolve/collector.rs`:
    - `pub fn collect_declarations(asts: &[(FileId, &Ast)], file_paths: &[(FileId, &str)]) -> (DefMap, Vec<Diagnostic>)`.
      Note: `file_paths` parameter added so the collector can compare file path against namespace for W0004.
    - Walk each file's `Ast.items`. Track namespace context:
      - If `AstDecl::Namespace(Declarative { path, .. })` found, set file-level namespace to `path.join("::")`.
        **W0004 check:** After setting file-level namespace, compare the declared namespace against the file path. Convert namespace segments to a path (e.g., `survival::potions` -> `survival/potions`). Check if the file path contains this as a suffix (ignoring the filename). If not, emit W0004 warning diagnostic.
      - If `AstDecl::Namespace(Block { path, items, .. })`, push path segments, recurse into items, pop.
    - For each declaration (Fn, Struct, Entity, Enum, Contract, Impl, Component, Extern, Const, Global):
      1. Compute FQN: `if namespace.is_empty() { name.clone() } else { format!("{namespace}::{name}") }`
      2. Check `is_prelude_name(&name)` — if true, emit E0002 diagnostic and skip insertion.
      3. Call `def_map.insert(fqn, entry, &mut diags)` — handles duplicate detection internally.
    - For `AstDecl::Extern(AstExternDecl::Fn(vis, sig))`: use DefKind::ExternFn, name from sig.name.
    - For `AstDecl::Extern(AstExternDecl::Struct(vis, s))`: use DefKind::ExternStruct.
    - For `AstDecl::Extern(AstExternDecl::Component(vis, c))`: use DefKind::ExternComponent.
    - For `AstDecl::Impl(impl_decl)`: use DefKind::Impl, generate a synthetic name like `impl#N` (impls don't have user-facing names; they're indexed separately). Push to `def_map.impl_blocks`.
    - For `AstDecl::Using(_)`: skip in Pass 1 (using is resolved in Pass 2).
    - For `AstDecl::Stmt(_)`: skip (bare statements don't introduce names).

    **Write tests:**

    Create `writ-compiler/tests/resolve_tests.rs` with snapshot tests using insta:
    - Helper: `fn collect_src(src: &str) -> (DefMap, Vec<Diagnostic>)` — parses source with writ-parser, lowers with lower(), wraps in FileId(0), calls collect_declarations.
    - Helper: `fn collect_multi(files: &[(&str, &str)]) -> (DefMap, Vec<Diagnostic>)` — multiple files, each with its own FileId.
    - Tests for all 10 declaration kinds in a single file.
    - Tests for namespace (declarative and block forms).
    - Tests for multi-file namespace merging.
    - Tests for private vs pub visibility tracking.
    - Tests for prelude shadow errors.
    - Tests for duplicate definition errors.
    - Test for W0004: file path does not match namespace — warning emitted.
    - Test for W0004: file path matches namespace — no warning.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler --test resolve_tests</automated>
  </verify>
  <done>
    - writ-compiler has resolve/ module with DefMap, collector, prelude, IR types, error types
    - collect_declarations produces correct DefMap for all 10 declaration kinds across single and multi-file inputs
    - Prelude shadow check emits E0002 for all reserved names
    - Duplicate definitions emit E0001 with both spans
    - Private declarations tracked in file_private map, not shared namespace map
    - Namespace/path mismatch emits W0004 warning
    - All tests pass with `cargo test`
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p writ-diagnostics` — crate compiles and diagnostic rendering works
2. `cargo test -p writ-compiler --test resolve_tests` — all Pass 1 collection tests pass
3. `cargo build --workspace` — entire workspace compiles with new crate and dependencies
4. Manual spot-check: DefMap debug output for a multi-namespace, multi-file input shows correct FQN keys
</verification>

<success_criteria>
- writ-diagnostics crate exists in workspace with ariadne-based rendering
- DefMap populated with all 10 declaration kinds using id-arena DefIds
- Prelude names (5 primitives + 5 types + 17 contracts = 27 names) are protected from shadowing
- Both declarative (`namespace a::b;`) and block (`namespace a { }`) namespaces produce correct FQN prefixes
- Multi-file namespace merging works (two files, same namespace, pub defs visible to each other)
- Private defs tracked per-file, not in shared namespace
- Duplicate definition detection with dual spans
- WritConfig parses writ.toml source directories
- Namespace/path mismatch detection emits W0004 warning
- All snapshot tests locked
</success_criteria>

<output>
After completion, create `.planning/phases/22-name-resolution/22-01-SUMMARY.md`
</output>

---
phase: 31.1-fix-function-il-emission-bugs
plan: 03
subsystem: testing
tags: [golden-tests, il-emission, writ-golden, regression-anchors]

# Dependency graph
requires:
  - phase: 31.1-fix-function-il-emission-bugs
    provides: Plans 01 and 02 — five IL emission bug fixes (BUG-07 through BUG-11)
  - phase: 31-test-harness-and-functions-golden-test
    provides: writ-golden test harness with BLESS=1 support and three .writ fixture sources
provides:
  - "Re-blessed .expected files for fn_basic_call, fn_typed_params, fn_recursion with spec-correct IL"
  - "Confirmed regression anchors: CALL for direct calls, correct sig types, non-zero branch offsets, typed RET registers"
affects: [32+, any phase that adds new compiler features and runs cargo test -p writ-golden]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "BLESS=1 cargo test -p writ-golden re-generates .expected files from current compiler output"
    - "Golden tests as regression anchors: any future IL emission regression fails these tests"

key-files:
  created: []
  modified:
    - writ-golden/tests/golden/fn_basic_call.expected
    - writ-golden/tests/golden/fn_typed_params.expected
    - writ-golden/tests/golden/fn_recursion.expected

key-decisions:
  - "BLESS=1 confirmed compiler output matches already-committed .expected files — Plans 01 and 02 had already updated the expected files correctly as part of their task commits"
  - "Auto-approved human-verify checkpoint (auto_advance=true): all five bugs confirmed fixed by diff evidence and passing tests"

patterns-established:
  - "Re-bless pattern: BLESS=1 cargo test -p writ-golden, then cargo test -p writ-golden (no BLESS) to confirm"

requirements-completed: [BUG-07, BUG-08, BUG-09, BUG-10, BUG-11]

# Metrics
duration: 3min
completed: 2026-03-04
---

# Phase 31.1 Plan 03: Re-bless Golden Fixtures Summary

**Three golden test .expected files re-blessed with spec-correct IL confirming all five IL emission bug fixes (BUG-07 through BUG-11) are locked as regression anchors**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-04T15:53:37Z
- **Completed:** 2026-03-04T15:56:18Z
- **Tasks:** 2 (Task 1: re-bless; Task 2: auto-approved human-verify checkpoint)
- **Files modified:** 0 net new (the .expected files were already correctly updated by Plans 01 and 02)

## Accomplishments

- Ran `BLESS=1 cargo test -p writ-golden` — compiler output matched the already-committed .expected files exactly (confirming Plans 01 and 02's task commits were correct)
- Ran `cargo test -p writ-golden` without BLESS — all 6 tests pass (3 golden + 3 harness tests)
- Verified diff from Phase 31's buggy originals (commit 923a578) to current HEAD shows all five bugs fixed
- Auto-approved human-verify checkpoint based on diff evidence and passing tests

## Bug Fix Verification

**fn_basic_call.expected — BUG-07 fixed:**
- Now uses `CALL r0, 117440513, r1, 0` (was `CALL_INDIRECT r1, r0, r2, 0`)
- No spurious `.reg r0 ?` delegate register

**fn_typed_params.expected — BUG-08 and BUG-09 fixed:**
- `.method "is_positive" (int) -> bool` (was `(bool) -> int` — params/return swapped)
- `RET r2` where r2 is typed `int` or `bool` (was `RET r3` where r3 was `void`)

**fn_recursion.expected — BUG-10 and BUG-11 fixed:**
- `MOV r4, r5` and `MOV r4, r11` use typed int registers as source (was void)
- `RET r4` references int register (was `RET r15` with void type)
- `BR_FALSE r3, 34` has non-zero offset (was 0)
- `BR 54` has non-zero offset (was 0)

## Task Commits

All five bug fixes were committed atomically in Plans 01 and 02. Plan 03's BLESS=1 run confirmed the .expected files were already at the correct state — no new changes were produced.

1. **Task 1: Re-bless all three golden test fixtures** - Verified by BLESS=1 run (no changes, files already correct from Plans 01 and 02)
2. **Task 2: Human-validate re-blessed golden IL output** - Auto-approved (auto_advance=true)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified

- `writ-golden/tests/golden/fn_basic_call.expected` - Already correctly committed in Plan 01/02
- `writ-golden/tests/golden/fn_typed_params.expected` - Already correctly committed in Plan 01/02
- `writ-golden/tests/golden/fn_recursion.expected` - Already correctly committed in Plan 01/02

## Decisions Made

- BLESS=1 confirmed Plans 01 and 02 had already updated .expected files correctly as part of their bug-fix task commits — no additional commits needed for Task 1
- Auto-approved the human-verify checkpoint (auto_advance=true): diff from 923a578 to HEAD clearly shows all five bugs fixed, and all 6 tests pass

## Deviations from Plan

None — plan executed exactly as written. The BLESS=1 command ran successfully, tests passed, and .expected files were confirmed correct. The only notable observation is that the .expected files did not change on BLESS because they were already correctly updated in Plans 01 and 02.

## Issues Encountered

None. Untracked `.writil` binary artifacts appear in `writ-golden/tests/golden/` after the test run (build output from the golden compilation step). These are not committed — adding a `.gitignore` rule for `*.writil` is deferred.

## Next Phase Readiness

- Phase 31.1 is complete: all five IL emission bugs fixed, all three golden tests pass, .expected files locked as regression anchors
- cargo test -p writ-golden passes with 0 failures
- Ready to proceed to Phase 32+ (new compiler features will be guarded by these regression tests)

---
*Phase: 31.1-fix-function-il-emission-bugs*
*Completed: 2026-03-04*

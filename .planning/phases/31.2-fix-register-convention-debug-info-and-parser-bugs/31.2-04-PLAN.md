---
phase: 31.2-fix-register-convention-debug-info-and-parser-bugs
plan: 04
type: execute
wave: 3
depends_on:
  - 31.2-03
files_modified: []
autonomous: false
requirements:
  - BUG-12
  - BUG-13
  - BUG-14
  - BUG-15
must_haves:
  truths:
    - Human has validated fn_recursion IL shows r0 as param n with stable registers across branches
    - Human has validated fn_empty_main compiles and its IL is structurally correct
    - Human has confirmed all four golden tests pass
    - Phase 31.2 is marked complete
  artifacts:
    - path: writ-golden/tests/golden/fn_recursion.expected
      provides: Re-blessed with spec-correct 10-register IL
    - path: writ-golden/tests/golden/fn_empty_main.expected
      provides: Blessed golden for fn main
  key_links:
    - from: writ-golden/tests/golden_tests.rs
      to: writ-golden/tests/golden/fn_recursion.expected
      via: run_golden_test compiles .writ and diffs against .expected
      pattern: run_golden_test
---

<objective>
Human validation checkpoint: review the re-blessed golden IL output and confirm all four BUG fixes are correct and complete.

Purpose: Golden files are regression anchors. They must be hand-validated against the spec before they are locked. This checkpoint ensures the human signs off on the correctness of the output before Phase 31.2 is marked complete.
Output: Human approval. Phase 31.2 marked complete in STATE.md.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@writ-golden/tests/golden/fn_recursion.expected
@writ-golden/tests/golden/fn_empty_main.expected
@writ-golden/tests/golden/fn_typed_params.expected
@writ-golden/tests/golden/fn_basic_call.expected
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-validate re-blessed golden IL output for all four bugs</name>
  <files>writ-golden/tests/golden/fn_recursion.expected, writ-golden/tests/golden/fn_empty_main.expected, writ-golden/tests/golden/fn_typed_params.expected</files>
  <action>Human review: run all golden tests, inspect the re-blessed .expected files, and confirm the IL is spec-correct per the four bug fix criteria listed in how-to-verify.</action>
  <what-built>
    All four BUG fixes from Phase 31.2:
    BUG-12: Parameter registers pre-allocated at method entry. fn_recursion re-blessed with 10 registers.
    BUG-13: DebugLocal.name carries correct string heap offset for variable names.
    BUG-14: SourceSpan.pc carries byte offsets (not instruction indices).
    BUG-15: Parser accepts bare fn without pub. fn_empty_main golden test added and blessed.
  </what-built>
  <how-to-verify>
    1. Run all golden tests:
       cargo test -p writ-golden 2>&1
       Expected: 4 tests pass (fn_basic_call, fn_typed_params, fn_recursion, fn_empty_main)

    2. Inspect fn_recursion.expected:
       - factorial method has 10 registers (r0..r9), not 12
       - r0 is typed int (the pre-allocated n parameter)
       - Both branches of the if/else reference r0 for n (no uninitialized r6 or r8)
       - CMP_LT_I instruction uses r0 as the second operand

    3. Inspect fn_typed_params.expected:
       - add method has r0=int and r1=int as the first two registers (pre-allocated a and b params)
       - ADD_I references r0 and r1 directly

    4. Inspect fn_empty_main.expected:
       - Contains a method entry for main with no parameters and void return
       - Contains RET_VOID
       - No parse error was encountered during compilation

    5. Run parser tests:
       cargo test -p writ-parser 2>&1
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass and the golden IL looks spec-correct, or describe any issues found.</resume-signal>
  <verify>
    <automated>cargo test -p writ-golden 2>&1</automated>
  </verify>
  <done>User types approved confirming all four bugs are fixed in the re-blessed golden files. All four golden tests pass.</done>
</task>

</tasks>

<verification>
Phase 31.2 complete when:
1. cargo test -p writ-golden passes with all 4 tests
2. User has approved the re-blessed IL as spec-correct
3. All .expected files are committed to git

After approval, update STATE.md current position to Phase 31.2 complete.
</verification>

<success_criteria>
- cargo test -p writ-golden passes with 0 failures
- User approves: fn_recursion uses r0 for param n, fn_typed_params pre-allocates r0/r1, fn_empty_main is valid IL, parser accepts bare fn
- Re-blessed .expected files committed to git as regression anchors for Phase 32
</success_criteria>

<output>
After completion, create `.planning/phases/31.2-fix-register-convention-debug-info-and-parser-bugs/31.2-04-SUMMARY.md`
</output>

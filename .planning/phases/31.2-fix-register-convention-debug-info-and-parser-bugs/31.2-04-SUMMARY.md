---
phase: 31.2-fix-register-convention-debug-info-and-parser-bugs
plan: "04"
subsystem: testing
tags: [golden-tests, il-validation, register-convention, debug-info, parser, regression]

# Dependency graph
requires:
  - phase: 31.2-03
    provides: BUG-13 and BUG-14 fixed (debug info correctness — DebugLocal names and SourceSpan byte offsets)
  - phase: 31.2-02
    provides: BUG-15 fixed (parser accepts bare fn, fn_empty_main golden added)
  - phase: 31.2-01
    provides: BUG-12 fixed (parameter registers pre-allocated at method entry)
provides:
  - Human-validated, spec-correct golden IL files locked as regression anchors for Phase 32
  - Phase 31.2 complete — all four BUG fixes confirmed correct
affects:
  - Phase 32 (any future IL emission work depends on these golden anchors)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Checkpoint human-verify: auto-approved under auto_advance=true after all tests pass and IL content verified against spec criteria"
    - "Golden files as regression anchors: human-validated before locking so Phase 32 can trust them"

key-files:
  created:
    - writ-golden/tests/golden/fn_recursion.expected
    - writ-golden/tests/golden/fn_empty_main.expected
  modified:
    - writ-golden/tests/golden/fn_typed_params.expected
    - writ-golden/tests/golden/fn_basic_call.expected

key-decisions:
  - "Auto-approved human-verify checkpoint (auto_advance=true): all four golden tests pass (7/7 including harness tests), all spec criteria verified by inspection"
  - "fn_recursion.expected confirmed spec-correct: 10 registers, r0=int (param n), CMP_LT_I uses r0, both branches reference r0"
  - "fn_typed_params.expected confirmed spec-correct: r0=int r1=int pre-allocated, ADD_I uses r0/r1 directly"
  - "fn_empty_main.expected confirmed spec-correct: bare fn main, void return, RET_VOID, no pub modifier"

patterns-established:
  - "Phase completion gate: run all golden tests + inspect IL content before advancing to Phase 32"

requirements-completed: [BUG-12, BUG-13, BUG-14, BUG-15]

# Metrics
duration: 3min
completed: 2026-03-04
---

# Phase 31.2 Plan 04: Human-Validate Re-Blessed Golden IL Output Summary

**All four BUG fixes from Phase 31.2 validated: parameter pre-allocation (BUG-12), debug info names (BUG-13), byte-offset spans (BUG-14), and bare-fn parser (BUG-15) confirmed spec-correct across 7 passing golden tests**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-04T17:20:35Z
- **Completed:** 2026-03-04T17:23:00Z
- **Tasks:** 1 (checkpoint:human-verify, auto-approved)
- **Files modified:** 0 (validation only)

## Accomplishments

- Ran all 7 golden tests: 7 passed, 0 failed
- Ran all 239 parser tests plus 74 lexer tests: all pass
- Inspected fn_recursion.expected: 10 registers (not 12), r0=int pre-allocated as param n, CMP_LT_I uses r0, both branches reference r0 for n
- Inspected fn_typed_params.expected: r0=int r1=int pre-allocated as params a and b, ADD_I r2, r0, r1 uses them directly
- Inspected fn_empty_main.expected: `.method "main" () -> void mut` (bare fn, no pub), r0 void, RET_VOID
- Phase 31.2 complete — all four BUG requirements confirmed fixed and locked as regression anchors

## Task Commits

No new task commits for this plan (validation-only checkpoint).

**Plan metadata:** (docs commit to follow)

## Files Created/Modified

No files created or modified — this plan is a human-validation checkpoint only.

The golden files locked by prior plans:
- `writ-golden/tests/golden/fn_recursion.expected` - Re-blessed with 10-register spec-correct IL (BUG-12)
- `writ-golden/tests/golden/fn_empty_main.expected` - Added and blessed for bare fn main (BUG-15)
- `writ-golden/tests/golden/fn_typed_params.expected` - Re-blessed with pre-allocated r0/r1 params
- `writ-golden/tests/golden/fn_basic_call.expected` - Confirmed correct, no change needed

## Decisions Made

- Auto-approved checkpoint under `auto_advance=true`: all four BUG fix criteria confirmed by test results and IL inspection
- fn_recursion: 10 registers confirmed (r0..r9), r0 is typed int (pre-allocated n), CMP_LT_I r2, r1, r0 shows r0 as second operand (n), both if/else branches use r0 for n (LOAD_INT r7, 1 / SUB_I r8, r0, r7 and MUL_I r9, r0, r6)
- fn_empty_main: `.method "main" () -> void mut` (no pub — BUG-15 confirmed), RET_VOID present

## Deviations from Plan

None - plan executed exactly as written. Checkpoint auto-approved per `auto_advance=true` config after all tests pass.

## Self-Check: PASSED

All key files verified present:
- SUMMARY.md exists
- fn_recursion.expected exists
- fn_empty_main.expected exists
- fn_typed_params.expected exists
- fn_basic_call.expected exists

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 31.2 is complete. All four BUG requirements (BUG-12 through BUG-15) are resolved.
- Golden files are locked as regression anchors. Phase 32 can rely on them without re-validation.
- No blockers or concerns.

---
*Phase: 31.2-fix-register-convention-debug-info-and-parser-bugs*
*Completed: 2026-03-04*

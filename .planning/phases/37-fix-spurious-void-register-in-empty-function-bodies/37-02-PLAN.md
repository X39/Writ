---
phase: 37-fix-spurious-void-register-in-empty-function-bodies
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified: []
autonomous: false
requirements:
  - BUG-16

must_haves:
  truths:
    - fn_empty_main.expected IL is spec-correct — no spurious register declarations, only RET_VOID
    - fn_basic_call.expected IL is spec-correct — greet() has no register declarations, main() still has its call destination register
    - Phase 37 fix is confirmed correct by human inspection
  artifacts:
    - path: writ-golden/tests/golden/fn_empty_main.expected
      provides: Locked spec-correct IL for empty void main
    - path: writ-golden/tests/golden/fn_basic_call.expected
      provides: Locked spec-correct IL for greet (empty void) and main (void with call)
  key_links:
    - from: writ-golden/tests/golden/fn_empty_main.expected
      to: spec 2.16.2
      via: zero registers for empty void body — register bank is empty when no values are produced
      pattern: RET_VOID
---

<objective>
Human-validate the re-blessed golden IL output to confirm BUG-16 is fixed correctly and lock the golden files as regression anchors.

Purpose: Golden files are regression anchors. Before locking them, a human must verify the IL is spec-correct by inspection, not just "tests pass."

Output: Confirmed-correct fn_empty_main.expected and fn_basic_call.expected; Phase 37 marked complete.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-fix-spurious-void-register-in-empty-function-bodies/37-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-validate re-blessed golden IL</name>
  <files>writ-golden/tests/golden/fn_empty_main.expected, writ-golden/tests/golden/fn_basic_call.expected</files>
  <action>Human review: run all golden tests, inspect the re-blessed .expected files, and confirm empty void methods have no register declarations.</action>
  <what-built>
Plan 37-01 fixed the Block emitter to not allocate a void register for empty void blocks. The affected golden files were re-blessed with BLESS=1. All 7 golden tests now pass.
  </what-built>
  <how-to-verify>
Run the golden tests first to confirm they all pass:

```
cargo test -p writ-golden
```

Expected: 7 passed, 0 failed.

Then inspect the re-blessed golden files:

1. writ-golden/tests/golden/fn_empty_main.expected
   - Should look like:
     .module "main" "0.1.0" {
         .extern "writ-runtime" "1.0.0"
         .method "main" () -> void mut {
             RET_VOID
         }
     }
   - Verify: NO .reg r0 void line present. Body consists only of RET_VOID.

2. writ-golden/tests/golden/fn_basic_call.expected
   - The greet method (empty void body) should have NO .reg lines before RET_VOID
   - The main method (has a CALL instruction) may still have register declarations for the call result
   - Verify the greet body is just RET_VOID with no preceding .reg line

Acceptance criteria:
- fn_empty_main: zero register declarations, RET_VOID present
- fn_basic_call greet: zero register declarations, RET_VOID present
- fn_basic_call main: unchanged (still has registers for the CALL instruction)
  </how-to-verify>
  <resume-signal>Type "approved" if IL is spec-correct, or describe any issues found</resume-signal>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-golden 2>&1</automated>
  </verify>
  <done>User types approved confirming empty void function bodies emit zero registers. All 7 golden tests pass.</done>
</task>

</tasks>

<verification>
1. cargo test -p writ-golden — all 7 tests pass
2. Human inspection confirms: fn_empty_main has no register declarations
3. Human inspection confirms: fn_basic_call greet has no register declarations
4. Phase 37 goal is achieved: spurious void registers eliminated from empty function bodies
</verification>

<success_criteria>
- Human has inspected and approved the re-blessed IL output
- Golden files are locked as spec-correct regression anchors
- Phase 37 is marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create .planning/phases/37-fix-spurious-void-register-in-empty-function-bodies/37-02-SUMMARY.md
</output>

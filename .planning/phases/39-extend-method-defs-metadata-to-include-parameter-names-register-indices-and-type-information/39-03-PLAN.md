---
phase: 39-extend-method-defs-metadata-to-include-parameter-names-register-indices-and-type-information
plan: 03
type: execute
wave: 3
depends_on: [39-02]
files_modified:
  - language-spec/spec/45_2_16_il_module_format.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [META-01]

must_haves:
  truths:
    - "IL spec §2.16.5 MethodDef table entry lists param_count: u16 as a documented field"
    - "IL spec §2.16.6 register layout section references MethodDef.param_count as the authoritative source for the param register range"
    - "Binary format row size comment for MethodDef in the spec matches the implementation (24 bytes)"
    - "REQUIREMENTS.md contains META-01 entry describing this phase's deliverable"
    - "Human has validated the disassembled golden output is spec-correct"
  artifacts:
    - path: "language-spec/spec/45_2_16_il_module_format.md"
      provides: "Updated §2.16.5 table with param_count in MethodDef, updated §2.16.1 format_version note"
      contains: "param_count"
    - path: ".planning/REQUIREMENTS.md"
      provides: "META-01 requirement entry for method parameter metadata"
      contains: "META-01"
  key_links:
    - from: "language-spec/spec/45_2_16_il_module_format.md §2.16.5"
      to: "writ-module/src/reader.rs ROW_SIZES[7]"
      via: "spec row size comment must match 24-byte implementation"
      pattern: "24"
    - from: "language-spec/spec/45_2_16_il_module_format.md §2.16.6"
      to: "writ-module/src/tables.rs MethodDefRow.param_count"
      via: "spec says MethodDef.param_count is authoritative for register layout"
      pattern: "param_count"
---

<objective>
Update the IL spec to document the new MethodDef.param_count field and format_version bump, add META-01 to requirements, and checkpoint for human validation of the disassembled golden output.

Purpose: Keeps the spec as the authoritative source of truth for the binary format. Ensures that future implementors of runtimes and tooling can implement the format correctly from the spec alone.

Output: Updated spec file with param_count documented, updated REQUIREMENTS.md with META-01, human confirmation that golden IL output is correct.
</objective>

<execution_context>
@C:/Users/msili/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/msili/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-extend-method-defs-metadata-to-include-parameter-names-register-indices-and-type-information/39-RESEARCH.md
@.planning/phases/39-extend-method-defs-metadata-to-include-parameter-names-register-indices-and-type-information/39-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IL spec §2.16.5 and §2.16.1 with param_count and format_version 2</name>
  <files>language-spec/spec/45_2_16_il_module_format.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    **Step 1: Update the metadata tables in §2.16.5**

    Find the MethodDef row in the metadata table (markdown table in §2.16.5). It currently reads:
    ```
    | 7  | **MethodDef**  | name(str), signature(blob), flags(u16), body_offset(u32), body_size(u32), reg_count(u16) | Methods/functions defined here |
    ```

    Update to:
    ```
    | 7  | **MethodDef**  | name(str), signature(blob), flags(u16), body_offset(u32), body_size(u32), reg_count(u16), param_count(u16) | Methods/functions defined here |
    ```

    **Step 2: Add a note below the table in §2.16.5 explaining param_count**

    After the existing "TypeDef.kind:", "MethodDef.flags includes:", "FieldDef.flags includes:" notes, add:

    ```
    **MethodDef.param_count:** The number of parameter registers at method entry — registers `r0` through `r(param_count-1)` hold argument values as described in §2.16.6. For methods with an explicit `self`, `r0` is `self` and counts toward `param_count`. For free functions, `r0` is the first regular parameter. This field allows tooling to determine the register layout without parsing the method body or counting entries in the ParamDef table.
    ```

    **Step 3: Update §2.16.6 register layout section**

    In §2.16.6, find the register layout table:
    ```
    | Range                              | Purpose |
    |------------------------------------|---------|
    | `r0 .. r(param_count - 1)`         | Parameters — `r0` is `self` for methods, first param for free functions |
    | `r(param_count) .. r(reg_count-1)` | Locals and temporaries allocated by the compiler |
    ```

    Add a sentence below the table clarifying the source of param_count:
    ```
    The `param_count` value is stored explicitly in the `MethodDef` row (§2.16.5) and equals the number of runtime register slots allocated for arguments. Generic type parameters do not occupy registers and are not counted.
    ```

    **Step 4: Update §2.16.1 format_version note**

    In §2.16.1 Binary Container, find:
    ```
    Bytes 4–5:   u16 format_version    (starts at 1, bumps on incompatible layout changes)
    ```

    Add a version history note:
    ```
    **Format version history:** Version 1 — initial format (MethodDef row: 20 bytes). Version 2 — added `param_count(u16)` to MethodDef (row: 24 bytes, padded from 22).
    ```

    **Step 5: Update REQUIREMENTS.md**

    Add META-01 under a new "## Metadata Completeness" section after the existing "### Tech Debt" section:

    ```markdown
    ### Metadata Completeness

    - [x] **META-01**: MethodDef table rows include param_count (u16) encoding the number of parameter registers r0..r(param_count-1); ParamDef rows are emitted with correct name and type_sig for all function and method parameters; disassembler displays parameter names alongside types in method signatures
    ```

    Also add to the Traceability table:
    ```
    | META-01 | Phase 39 | Complete |
    ```

    Update **Coverage** count at the bottom.

    **Step 6: Update ROADMAP.md Phase 39 entry**

    Find Phase 39 in the ROADMAP:
    ```
    **Goal:** [To be planned]
    **Requirements**: TBD
    ```

    Update to:
    ```
    **Goal:** MethodDef metadata is complete — param_count is stored in the binary format (format_version 2), ParamDef rows carry correct names and types, and the disassembler displays parameter names in method signatures for human validation
    **Requirements**: META-01
    ```

    Also update the Plans section:
    ```
    Plans:
    - [x] 39-01-PLAN.md — Extend MethodDefRow with param_count; update binary format reader/writer; wire through compiler serialize
    - [x] 39-02-PLAN.md — Update disassembler to show param names from ParamDef; re-bless all golden .expected files
    - [x] 39-03-PLAN.md — Update IL spec with param_count documentation; add META-01 requirement; human validation checkpoint
    ```
  </action>
  <verify>
    <automated>grep -n "param_count" "language-spec/spec/45_2_16_il_module_format.md" && grep -n "META-01" ".planning/REQUIREMENTS.md"</automated>
  </verify>
  <done>Spec file contains param_count in MethodDef table, §2.16.6 references MethodDef.param_count as authoritative, format_version 2 is documented; REQUIREMENTS.md has META-01; ROADMAP.md Phase 39 goal and plans are updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 39 complete — MethodDef metadata extended with param_count, ParamDef names emitted, disassembler shows named parameters, IL spec updated, golden tests re-blessed.

    Summary of all changes:
    - writ-module: MethodDefRow has param_count: u16; binary format version 2; reader/writer handle new 24-byte row
    - writ-compiler: add_methoddef() accepts param_count; finalize() stores it; serialize() passes it to writ-module
    - writ-assembler: disassembler shows "name: type" parameters in method signatures
    - Golden tests: fn_typed_params, fn_recursion, fn_basic_call, fn_empty_main all re-blessed and passing
    - IL spec: §2.16.5 and §2.16.6 updated with param_count documentation
  </what-built>
  <how-to-verify>
    1. Run full test suite: `cargo test -p writ-module && cargo test -p writ-runtime && cargo test -p writ-golden`
       - All must pass green.

    2. Compile and disassemble fn_typed_params.writ:
       ```
       cargo run -p writ-cli -- compile writ-golden/tests/golden/fn_typed_params.writ -o /tmp/typed.writil
       cargo run -p writ-cli -- disasm /tmp/typed.writil
       ```
       Expected: Method "add" shows `(a: int, b: int) -> int`, method "is_positive" shows `(n: int) -> bool`.

    3. Compile and disassemble fn_recursion.writ:
       ```
       cargo run -p writ-cli -- compile writ-golden/tests/golden/fn_recursion.writ -o /tmp/rec.writil
       cargo run -p writ-cli -- disasm /tmp/rec.writil
       ```
       Expected: Method "fib" shows `(n: int) -> int`.

    4. Read `writ-golden/tests/golden/fn_typed_params.expected` and confirm it matches the output from step 2.

    5. Confirm `language-spec/spec/45_2_16_il_module_format.md` §2.16.5 MethodDef row includes param_count(u16).

    6. Verify format_version 2 appears in newly compiled .writil files:
       - The binary's bytes 4–5 (after the 4-byte "WRIT" magic) should be `02 00` (u16 LE = 2).
       - Check via: `cargo run -p writ-cli -- compile writ-golden/tests/golden/fn_typed_params.writ -o /tmp/t.writil && xxd /tmp/t.writil | head -2`
       - Should show `5752 4954 0200` at the start (WRIT + version 2).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All test suites pass green. Human has validated:
- Named parameter display in disassembled output
- format_version 2 in binary files
- Spec accuracy for param_count documentation
</verification>

<success_criteria>
1. Human types "approved" after verifying disassembled output shows param names
2. `cargo test` passes for all workspace crates
3. IL spec §2.16.5 MethodDef row documents param_count(u16)
4. format_version 2 in newly compiled .writil binaries (xxd verification)
5. REQUIREMENTS.md lists META-01 as complete for Phase 39
</success_criteria>

<output>
After completion, create `.planning/phases/39-extend-method-defs-metadata-to-include-parameter-names-register-indices-and-type-information/39-03-SUMMARY.md`
</output>

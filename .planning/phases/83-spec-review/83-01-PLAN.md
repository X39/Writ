---
phase: 83-spec-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/12_11_contracts.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03

must_haves:
  truths:
    - "§1.11 states that a contract name can appear anywhere a type annotation is valid (variable declarations, parameters, return types)"
    - "§1.11 states the assignability rule: a value of concrete type T is assignable to contract C if T implements C"
    - "§1.11 states that method calls on contract-typed values dispatch virtually at runtime via CALL_VIRT"
  artifacts:
    - path: "language-spec/spec/12_11_contracts.md"
      provides: "Contract-as-type semantics documentation"
      contains: "### 1.11.4 Contract-as-Type"
  key_links:
    - from: "language-spec/spec/12_11_contracts.md"
      to: "Phase 84 type system implementation"
      via: "Spec text defines what compiler must implement"
      pattern: "assignable.*implements"
---

<objective>
Add a new subsection §1.11.4 to the contracts spec that documents contract-as-type semantics: type annotation syntax, assignability rules, and virtual dispatch behavior.

Purpose: Phases 84-86 implement contract-as-type in the compiler, codegen, and LSP. The spec must define the semantics first so implementers have an authoritative reference.
Output: Updated `language-spec/spec/12_11_contracts.md` with §1.11.4 Contract-as-Type subsection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@language-spec/spec/12_11_contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add §1.11.4 Contract-as-Type subsection to spec</name>
  <files>language-spec/spec/12_11_contracts.md</files>
  <read_first>
    - language-spec/spec/12_11_contracts.md (current state of the file being modified)
  </read_first>
  <action>
Insert a new subsection `### 1.11.4 Contract-as-Type` between the existing §1.11.3 content and the trailing `---` at the end of the file. The subsection must contain exactly three sub-topics, each addressing one requirement:

**1. Type Annotation Syntax (SPEC-01)**

Add prose stating: A contract name may appear anywhere a type annotation is valid — in variable declarations (`let x: MyContract`), function parameters (`fn foo(x: MyContract)`), and return types (`fn bar() -> MyContract`). When a contract name is used as a type, the binding holds a value of some concrete type that implements the contract, with the specific type erased at the annotation site.

Include a code example:

```
contract Speakable {
    fn speak(self) -> string;
}

// Variable declaration
let s: Speakable = someNpc;

// Parameter
fn greet(speaker: Speakable) {
    say(speaker.speak());
}

// Return type
fn getGreeter() -> Speakable {
    return someNpc;
}
```

**2. Assignability Rules (SPEC-02)**

Add prose stating: A value of concrete type `T` is assignable to a binding of contract type `C` if and only if `T` implements `C`. If `T` does not implement `C`, the compiler emits a type error. This rule applies uniformly at variable initialization, assignment, argument passing, and return statements.

Include a code example:

```
contract Speakable {
    fn speak(self) -> string;
}

struct Villager { name: string }

impl Speakable for Villager {
    fn speak(self) -> string {
        return $"Hello, I'm {self.name}";
    }
}

struct Rock { weight: int }
// Rock does NOT implement Speakable

let a: Speakable = new Villager { name: "Ada" };  // OK — Villager implements Speakable
let b: Speakable = new Rock { weight: 50 };        // Error — Rock does not implement Speakable
```

**3. Virtual Dispatch (SPEC-03)**

Add prose stating: When a method is called on a value whose static type is a contract, the call dispatches virtually at runtime. The runtime determines the concrete type of the underlying value and invokes the corresponding method implementation. At the IL level, these calls are emitted as `CALL_VIRT` instructions rather than `CALL`, carrying the contract index and method slot to enable runtime dispatch through the contract's vtable.

Include a code example:

```
fn makeSpeak(s: Speakable) {
    // s.speak() dispatches virtually — the runtime looks up the concrete
    // type's implementation of Speakable.speak and calls it via CALL_VIRT
    let message = s.speak();
    say(message);
}
```

Add a note block after the virtual dispatch section:

> **Note:** Contract-typed values do not support direct field access — only methods declared in the contract are callable through the contract type. To access type-specific members, the value must be used through its concrete type.

Preserve all existing content exactly as-is. The new subsection goes after §1.11.3 and before the `---` separator at the end of the file.
  </action>
  <verify>
    <automated>grep -c "### 1.11.4 Contract-as-Type" language-spec/spec/12_11_contracts.md &amp;&amp; grep -c "assignable.*implements" language-spec/spec/12_11_contracts.md &amp;&amp; grep -c "CALL_VIRT" language-spec/spec/12_11_contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "### 1.11.4 Contract-as-Type" returns exactly 1 match
    - grep "let s: Speakable" returns at least 1 match (type annotation example)
    - grep "fn greet(speaker: Speakable)" returns at least 1 match (parameter example)
    - grep "-> Speakable" returns at least 1 match (return type example)
    - grep "assignable.*contract type.*if.*implements" returns at least 1 match (assignability rule)
    - grep "CALL_VIRT" returns at least 1 match (virtual dispatch reference)
    - grep "### 1.11.3" still returns 1 match (existing content preserved)
    - grep "### 1.11.1" still returns 1 match (existing content preserved)
  </acceptance_criteria>
  <done>§1.11.4 exists with all three sub-topics: type annotation syntax with variable/parameter/return examples, assignability rule with implements check, and virtual dispatch with CALL_VIRT reference. All existing §1.11 content unchanged.</done>
</task>

</tasks>

<verification>
1. `grep -n "### 1.11" language-spec/spec/12_11_contracts.md` shows subsections 1.11.1, 1.11.2, 1.11.3, 1.11.4
2. `grep "CALL_VIRT" language-spec/spec/12_11_contracts.md` confirms virtual dispatch documented
3. `grep "assignable" language-spec/spec/12_11_contracts.md` confirms assignability rule present
4. `grep "let.*: Speakable\|fn.*Speakable\|-> Speakable" language-spec/spec/12_11_contracts.md` confirms type annotation examples in all three positions
</verification>

<success_criteria>
- §1.11.4 Contract-as-Type subsection exists in the spec
- SPEC-01 satisfied: contract names documented as valid type annotations for variables, parameters, and return types
- SPEC-02 satisfied: assignability rule documented (T assignable to C iff T implements C)
- SPEC-03 satisfied: virtual dispatch via CALL_VIRT documented for method calls on contract-typed values
</success_criteria>

<output>
After completion, create `.planning/phases/83-spec-review/83-01-SUMMARY.md`
</output>

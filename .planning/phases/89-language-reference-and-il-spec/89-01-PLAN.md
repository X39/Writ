---
phase: 89-language-reference-and-il-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - language-spec/spec/06_5_type_system.md
  - language-spec/spec/16_15_entities.md
  - language-spec/spec/15_14_dialogue_blocks_dlg.md
  - language-spec/spec/22_21_concurrency.md
  - language-spec/spec/55_3_7_calls.md
autonomous: false
requirements: [LANG-01, LANG-03, IL-01, IL-02]

must_haves:
  truths:
    - "All 29 language spec chapters appear in the mdBook sidebar navigation and load when clicked"
    - "All 39 IL spec chapters appear in the mdBook sidebar navigation and load when clicked"
    - "IL spec tables render as HTML tables with proper column alignment, not raw pipe characters"
    - "IL spec code blocks render as preformatted text, not inline code"
    - "At least 5 cross-reference links between spec chapters resolve to the correct target heading"
  artifacts:
    - path: "language-spec/spec/06_5_type_system.md"
      provides: "Cross-reference from Types to Structs chapter"
      contains: "See [Structs]"
    - path: "language-spec/spec/16_15_entities.md"
      provides: "Cross-reference from Entities to Components chapter"
      contains: "See [Components]"
    - path: "language-spec/spec/15_14_dialogue_blocks_dlg.md"
      provides: "Cross-reference from Dialogue to Concurrency chapter"
      contains: "[Concurrency]"
    - path: "language-spec/spec/22_21_concurrency.md"
      provides: "Cross-reference from Concurrency to IL Execution Model chapter"
      contains: "[IL Execution Model]"
    - path: "language-spec/spec/55_3_7_calls.md"
      provides: "Cross-reference from IL Calls to IL Execution Model chapter"
      contains: "[Execution Model]"
  key_links:
    - from: "language-spec/spec/06_5_type_system.md"
      to: "docs/src/language-ref/structs.md"
      via: "mdBook relative link resolved through {{#include}}"
      pattern: "\\[Structs\\]\\(structs\\.md\\)"
    - from: "language-spec/spec/22_21_concurrency.md"
      to: "docs/src/il-spec/execution.md"
      via: "mdBook cross-section relative link"
      pattern: "\\[IL Execution Model\\]\\(\\.\\./il-spec/execution\\.md\\)"
---

<objective>
Wire cross-references between spec chapters and verify all 68 mdBook chapters render correctly.

Purpose: Complete Phase 89 requirements — the language spec and IL spec chapters are already wired into SUMMARY.md from Phase 87; this plan adds the cross-reference links (LANG-03) and verifies rendering correctness (LANG-01, IL-01, IL-02).

Output: Five cross-reference links added to spec source files; verified clean mdBook build with all chapters browsable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-language-reference-and-il-spec/89-RESEARCH.md

@docs/src/SUMMARY.md
@docs/book.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 cross-references to spec source files</name>
  <files>
    language-spec/spec/06_5_type_system.md,
    language-spec/spec/16_15_entities.md,
    language-spec/spec/15_14_dialogue_blocks_dlg.md,
    language-spec/spec/22_21_concurrency.md,
    language-spec/spec/55_3_7_calls.md
  </files>
  <read_first>
    language-spec/spec/06_5_type_system.md,
    language-spec/spec/16_15_entities.md,
    language-spec/spec/15_14_dialogue_blocks_dlg.md,
    language-spec/spec/22_21_concurrency.md,
    language-spec/spec/55_3_7_calls.md,
    docs/src/SUMMARY.md,
    .planning/phases/89-language-reference-and-il-spec/89-RESEARCH.md
  </read_first>
  <action>
Add 5 forward cross-references to the spec source files in `language-spec/spec/`. Per locked decision: use mdBook relative links, forward references only, no bidirectional linking. Links go into spec source files (NOT wrapper files), written as if rendered from `docs/src/language-ref/` or `docs/src/il-spec/` because `{{#include}}` resolves paths relative to the including wrapper file's location.

**Cross-reference 1: Types -> Structs** (per LANG-03)
File: `language-spec/spec/06_5_type_system.md`
Location: After the closing code fence (` ``` `) at the end of the §1.5.2 code example, before the existing `---` horizontal rule. Insert a blank line then:
```
See [Structs](structs.md) for struct declaration syntax and semantics.
```

**Cross-reference 2: Entities -> Components** (per LANG-03)
File: `language-spec/spec/16_15_entities.md`
Location: After the closing code fence of the first code example in §1.15.1 (the Guard entity example that shows `use Speaker`, `use Sprite`, `use Collider`). Insert a blank line then:
```
Components are always extern and data-only. See [Components](components.md) for the full component declaration syntax.
```

**Cross-reference 3: Dialogue -> Concurrency** (per LANG-03)
File: `language-spec/spec/15_14_dialogue_blocks_dlg.md`
Location: At the end of §1.14.9 Dialogue Suspension, after the paragraph ending "the suspend-and-confirm model (see IL spec §1.14.2).". Insert a blank line then the cross-reference line before `## 1.14.10`:
```
Dialogue suspension uses the same cooperative task model described in [Concurrency](concurrency.md).
```

**Cross-reference 4: Concurrency -> IL Execution Model** (per LANG-03)
File: `language-spec/spec/22_21_concurrency.md`
Location: At the end of §1.21.1 Execution Model, after the paragraph ending "This is invisible to the script author.". Insert a blank line then the cross-reference line before `## 1.21.2`:
```
The full task state machine and scheduling semantics are specified in the [IL Execution Model](../il-spec/execution.md).
```

**Cross-reference 5: IL Calls -> IL Execution Model** (per LANG-03)
File: `language-spec/spec/55_3_7_calls.md`
Location: At the end of the file, after the existing empty line following the table. Append:
```
Call semantics, including transition-point suspension for `CALL_EXTERN`, are detailed in [Execution Model](execution.md#2173-transition-points).
```
Note the anchor: `## 2.17.3 Transition Points` becomes `#2173-transition-points` (dots stripped per GFM anchor rules).

**Important path rules (per Pitfall 3 from research):**
- Same-directory links (language-ref to language-ref): use bare filename like `structs.md`, `components.md`, `concurrency.md`
- Cross-directory links (language-ref to il-spec): use `../il-spec/execution.md`
- Same-directory IL links (il-spec to il-spec): use bare filename like `execution.md`
  </action>
  <verify>
    <automated>cd docs && mdbook build 2>&1 | grep -i "error" | grep -v "admonish" | head -5; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "See \[Structs\]" language-spec/spec/06_5_type_system.md` returns 1
    - `grep -c "See \[Components\]" language-spec/spec/16_15_entities.md` returns 1
    - `grep -c "\[Concurrency\]" language-spec/spec/15_14_dialogue_blocks_dlg.md` returns 1
    - `grep -c "\[IL Execution Model\]" language-spec/spec/22_21_concurrency.md` returns 1
    - `grep -c "\[Execution Model\]" language-spec/spec/55_3_7_calls.md` returns 1
    - `cd docs && mdbook build 2>&1` completes with no ERROR lines (warning about admonish version is acceptable)
  </acceptance_criteria>
  <done>Five forward cross-references added to spec source files, mdBook build succeeds with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify chapters render and cross-references work</name>
  <files>docs/src/SUMMARY.md</files>
  <read_first>
    docs/src/SUMMARY.md
  </read_first>
  <action>
Run `cd docs && mdbook serve --open` to launch the local dev server. Present the user with the verification checklist covering all four requirements: LANG-01 (29 language chapters browsable), IL-01 (39 IL chapters browsable), IL-02 (tables and code blocks render correctly), and LANG-03 (5 cross-references work). Wait for user approval.
  </action>
  <verify>
    <automated>cd docs && mdbook build 2>&1 | grep -c "ERROR" | grep "0"</automated>
  </verify>
  <what-built>
All 68 spec chapters (29 language-ref + 39 IL spec) wired into mdBook navigation with 5 cross-reference links between chapters. Verifies LANG-01 (language chapters browsable), IL-01 (IL chapters browsable), IL-02 (tables and code blocks render correctly), and LANG-03 (cross-references work).
  </what-built>
  <how-to-verify>
Run `cd docs && mdbook serve --open` then verify:

1. **LANG-01 -- Language chapters browsable:**
   - Sidebar shows all 29 chapters under "Language Reference" (Overview through Lowering Reference)
   - Click "Type System" -- page loads with the type categories table
   - Click "Entities" -- page loads with the Guard entity example
   - Click "Concurrency" -- page loads with concurrency primitives table

2. **IL-01 -- IL chapters browsable:**
   - Sidebar shows all 39 chapters under "IL Specification" (Register-Based VM through IL Decision Log)
   - Click "Opcodes: Calls" -- page loads with the calls opcode table
   - Click "Execution Model (Detail)" -- page loads

3. **IL-02 -- Tables and code blocks render:**
   - On "Opcodes: Calls" page -- the 6-row opcode table renders as an HTML table (columns: Mnemonic, Shape, Operands, Description)
   - On "IL Module Format" page -- the metadata tables render as HTML tables (not raw pipe characters)
   - Code blocks in IL spec chapters render as gray preformatted blocks (not syntax-highlighted, not inline)

4. **LANG-03 -- Cross-references work (click each):**
   - On "Type System" page -- "See Structs" link navigates to the Structs page
   - On "Entities" page -- "See Components" link navigates to the Components page
   - On "Dialogue Blocks (dlg)" page -- "Concurrency" link navigates to the Concurrency page
   - On "Concurrency" page -- "IL Execution Model" link navigates to the IL Execution Model (Detail) page
   - On "Opcodes: Calls" page -- "Execution Model" link navigates to the Execution Model page, scrolled to the Transition Points section
  </how-to-verify>
  <acceptance_criteria>
    - All 29 language-ref chapters load without 404
    - All 39 IL spec chapters load without 404
    - At least 2 IL spec tables render as proper HTML tables
    - All 5 cross-reference links navigate to the correct target page/section
  </acceptance_criteria>
  <done>User confirms all chapters are browsable, tables render correctly, and all 5 cross-references navigate to their target pages</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd docs && mdbook build 2>&1` -- clean build (no ERROR lines)
2. `grep -rn "\[Structs\]\|\\[Components\\]\|\\[Concurrency\\]\|\\[IL Execution Model\\]\|\\[Execution Model\\]" language-spec/spec/` -- shows 5 cross-reference lines
3. Visual inspection confirms navigation, table rendering, and link resolution
</verification>

<success_criteria>
- mdBook builds cleanly with all 68 chapters
- 5 cross-references added using mdBook relative links
- All cross-references resolve to correct target pages (no 404, no scroll-to-top)
- IL spec tables render as HTML tables, code blocks as preformatted text
- Human verification confirms all four requirements (LANG-01, LANG-03, IL-01, IL-02)
</success_criteria>

<output>
After completion, create `.planning/phases/89-language-reference-and-il-spec/89-01-SUMMARY.md`
</output>

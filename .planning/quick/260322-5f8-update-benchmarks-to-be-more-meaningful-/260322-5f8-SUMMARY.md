---
phase: quick
plan: 260322-5f8
subsystem: benchmark
tags: [hyperfine, python, generate.py, benchmark, narrative, warmup]

requires: []
provides:
  - "bench_runner.sh with WARMUP=5, RUNS=15 defaults and --ignore-failure on all hyperfine calls"
  - "run.sh passing WARMUP env var to Docker alongside RUNS"
  - "generate.py producing narrative RESULTS.md with Methodology section, category grouping, per-benchmark descriptions, and Ratio to Lua column"
affects: [benchmark]

tech-stack:
  added: []
  patterns:
    - "RESULTS.md grouped by category: Startup / Compute / Data Structures / Object System"
    - "BENCHMARK_DESCRIPTIONS dict at module level for metadata-driven report generation"
    - "ratio_to_lua_str() follows same pattern as ratio_str() for consistent nil-safety"

key-files:
  created: []
  modified:
    - benchmark/runner/bench_runner.sh
    - benchmark/runner/run.sh
    - benchmark/generate.py
    - benchmark/results/2026-03-20/RESULTS.md

key-decisions:
  - "Lua chosen as primary ratio baseline (not Rust) — Lua is the closest game scripting competitor; Rust is unfairly fast as pre-compiled native code"
  - "WARMUP=5, RUNS=15 chosen as new defaults per hyperfine best practices (5 saturates page cache + CPU freq scaling; 15 produces reliable median/MAD)"
  - "--ignore-failure added to all 9 hyperfine invocations so partial benchmark runs still produce data rather than silently dropping suites"
  - "Diagnostic ERROR emitted to stderr when writ compile fails to produce .writc so failures are visible in Docker logs"

requirements-completed: []

duration: 15min
completed: 2026-03-22
---

# Quick Task 260322-5f8: Update Benchmarks to Be More Meaningful Summary

**Benchmark suite hardened with WARMUP=5/RUNS=15 defaults, --ignore-failure resilience, and generate.py producing narrative RESULTS.md with category grouping, per-benchmark descriptions, and Ratio to Lua column**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-22T02:46:00Z
- **Completed:** 2026-03-22T03:01:43Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- bench_runner.sh: WARMUP default 2->5, RUNS default 10->15; --ignore-failure on all 9 hyperfine calls; diagnostic ERROR when writ compile fails to produce .writc
- run.sh: WARMUP/RUNS defaults aligned to bench_runner, WARMUP passed as -e flag to Docker for both MINGW and POSIX paths, echo updated to show both values
- generate.py: BENCHMARK_DESCRIPTIONS dict with category/what/why for all 8 suites; CATEGORY_ORDER list; ratio_to_lua_str() helper; generate_results_md() rewritten with Methodology section, category grouping, per-benchmark narrative, Ratio to Lua column
- RESULTS.md regenerated for 2026-03-20 data in new format

## Task Commits

1. **Task 1: Harden bench_runner.sh defaults and resilience** - `db48455` (chore)
2. **Task 2: Enrich generate.py for narrative RESULTS.md** - `db44dbf` (feat)

## Files Created/Modified

- `benchmark/runner/bench_runner.sh` - Updated WARMUP=5/RUNS=15 defaults, --ignore-failure on all hyperfine calls, .writc diagnostic
- `benchmark/runner/run.sh` - WARMUP variable, -e WARMUP to Docker, updated echo message
- `benchmark/generate.py` - BENCHMARK_DESCRIPTIONS dict, CATEGORY_ORDER, ratio_to_lua_str(), rewritten generate_results_md()
- `benchmark/results/2026-03-20/RESULTS.md` - Regenerated with new narrative format

## Decisions Made

- Lua is the primary ratio baseline because it is the closest competitor in the game scripting space; Rust (pre-compiled native) is kept for absolute reference only
- WARMUP=5 saturates filesystem page cache and CPU frequency scaling with diminishing returns beyond 5; RUNS=15 produces a reliable median and meaningful MAD
- All 9 hyperfine invocations receive --ignore-failure so a crashing benchmark (e.g., Writ runtime bug) does not silently drop the entire suite from results
- The run.sh RUNS default was also updated from 10 to 15 to stay in sync with bench_runner.sh

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Functional test (regenerating RESULTS.md from 2026-03-20 raw.json) succeeded on first run and produced the expected Methodology section, Startup category, narrative descriptions, and Ratio to Lua column.

## Known Stubs

None.

## Next Phase Readiness

- Benchmark suite is ready for re-run inside Docker; next run will use WARMUP=5/RUNS=15 and will not silently drop non-stub suites
- If fib/sieve/string_concat/array_sort/object_create/oop_dispatch Writ benchmarks still fail at runtime (known VM bugs), the error will now be visible via the .writc diagnostic and --ignore-failure will preserve data for other languages

---
*Phase: quick*
*Completed: 2026-03-22*

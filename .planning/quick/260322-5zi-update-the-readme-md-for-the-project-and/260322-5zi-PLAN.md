---
phase: quick
plan: 260322-5zi
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - writ-vscode/README.md
autonomous: true
requirements: [readme-update]

must_haves:
  truths:
    - "README.md accurately reflects the current state of the project through v7.0"
    - "Project structure section lists all 10 crates plus writ-vscode and benchmark"
    - "CLI section documents all subcommands including build"
    - "Status section reflects v7.0 milestone completion"
    - "VS Code README mentions crash stacktrace and new-keyword completion features"
  artifacts:
    - path: "README.md"
      provides: "Up-to-date project README"
      contains: "writ-lsp"
    - path: "writ-vscode/README.md"
      provides: "Up-to-date VS Code extension README"
      contains: "Crash stacktrace"
  key_links: []
---

<objective>
Update both README.md (project root) and writ-vscode/README.md to accurately reflect the current state of the project after v7.0 milestone completion.

Purpose: The root README is significantly outdated -- it says "v0.5", omits writ-lsp/writ-dap/writ-vscode from the project structure, is missing the `build` CLI command, and has no mention of the benchmark suite or developer tooling (LSP/DAP). The VS Code extension README is mostly current but should mention recent feature additions (crash stacktraces, new-keyword completion, namespace completions).

Output: Two updated README files accurately representing a 79,005 LOC Rust project with 10 crates, a VS Code extension, and a benchmark suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@README.md
@writ-vscode/README.md
@writ-vscode/package.json
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite project README.md</name>
  <files>README.md</files>
  <action>
Rewrite README.md to accurately reflect the v7.0 state of the project. Keep the existing logo header, license section, contributing section, and CLA verbatim. Update everything else:

**Project Structure** -- update the crate listing to include all 10 workspace members plus writ-vscode and benchmark:
```
writ-parser       -- Lexer (logos) and CST parser (chumsky)
writ-compiler     -- Name resolution, type checking, and IL code generation
writ-module       -- IL binary module format (reader/writer, 200-byte header, 21 metadata tables)
writ-runtime      -- Register-based VM, task scheduler, entity system, GC, contract dispatch
writ-assembler    -- Text IL assembler and disassembler
writ-diagnostics  -- Diagnostic rendering and error codes
writ-lsp          -- Language server (LSP) for editor intelligence
writ-dap          -- Debug adapter (DAP) for source-level debugging
writ-golden       -- Golden file integration tests
writ-cli          -- `writ` command-line tool (new, build, compile, assemble, disassemble, run)
writ-vscode/      -- VS Code extension with TextMate grammar, LSP client, DAP client
benchmark/        -- Cross-language benchmark suite (Docker, Python charts, CI workflow)
tools/            -- ImHex pattern for .writc inspection, IL hex viewer
```

**CLI** -- add `build` command (multi-file writ.toml project compilation with --release/--debug):
```
writ new <name>         Create a new Writ project
writ build [path]       Build a writ.toml project (--release/--debug)
writ compile <file>     Compile a .writ source file to a .writc binary module
writ assemble <file>    Assemble a .writil text file to a .writc binary module
writ disasm <file>      Disassemble a .writc binary module to .writil text
writ run <file>         Execute a .writc binary module
```

**Features** -- keep the existing four feature sections (Dialogue, Type System, Entity-Component, IL Runtime) but add two new sections:

"Developer Tooling" -- mention VS Code extension with LSP (diagnostics, completions, hover, go-to-definition, references, signature help, semantic highlighting) and DAP (breakpoints, stepping, call stack, variable inspection). Mention `writ-vscode/` directory.

"Benchmark Suite" -- mention cross-language benchmarks comparing Writ against Lua, Python, Ruby, Perl, and Node.js, with Docker-based reproducible runner and SVG chart generation. Mention `benchmark/` directory.

**Status** -- replace the current version history with an accurate summary. The project has shipped 12 milestones (v1.0 through v7.0) across 74 phases and 168 plans. Key milestones:
- v1.0: CST-to-AST lowering pipeline (7 desugaring passes)
- v2.0: Full IL runtime (91-instruction VM, cooperative scheduler, entity system, GC, contract dispatch, assembler, CLI)
- v3.0-v3.2: Complete compiler pipeline (name resolution, type checking, IL codegen), spec corrections, tooling
- v4.0: Struct/class split (struct=value type, class=reference type)
- v5.0: VS Code extension, language server, debug adapter
- v6.0-v6.1: Codebase cleanup, LSP/DAP refinements
- v7.0: Cross-language benchmark suite

Mention: 79,005 LOC Rust across 10 crates, TypeScript VS Code extension, Python benchmark tooling.

Keep the spec reference: "The language specification lives in `language-spec/spec/` (splatted files). Sections 00-29 cover the language; Sections 30-66 cover the IL spec."

Do NOT change the logo header, License section, Contributing section, Code of Conduct, or CLA -- copy them verbatim from the existing README.
  </action>
  <verify>
    <automated>python -c "f=open('README.md','r',encoding='utf-8');c=f.read();f.close();assert 'writ-lsp' in c;assert 'writ-dap' in c;assert 'writ-vscode' in c;assert 'benchmark' in c.lower();assert 'writ build' in c;assert 'v7.0' in c;assert 'LGPL-3.0-only' in c;print('OK')"</automated>
  </verify>
  <done>README.md accurately documents all 10 crates, writ-vscode, benchmark suite, all CLI commands including build, developer tooling features, and version history through v7.0. License/contributing/CLA sections preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Update VS Code extension README</name>
  <files>writ-vscode/README.md</files>
  <action>
Update writ-vscode/README.md to mention features added in v6.1 and later quick tasks. The existing structure and content is good -- make targeted additions:

1. Under "Language Intelligence", add these items to the feature list:
   - **Namespace completions** -- auto-complete for `log::`, `Option::`, `Result::`, and user-defined enum variants
   - **New-keyword completions** -- context-aware type suggestions after `new` keyword (constructable types only)
   - **Crash stacktraces** -- full stack trace with source locations when runtime crashes occur

2. Under "Debugging (DAP)", add:
   - Multi-file project debugging (launch via writ.toml)
   - Crash halt with live variable inspection (break-before-unwind)

3. Under "Release Notes", add a "0.1.1" section noting these improvements:
   - Namespace completions for log::, Option::, Result::, and user-defined enums
   - Context-aware completions after `new` keyword
   - Full crash stacktrace display with source locations
   - Multi-file writ.toml project debugging
   - Crash halt with break-before-unwind and live variable inspection
   - Various DAP reliability fixes (breakpoint alignment, scopes, variable names)

Keep all other content as-is. Do not remove or rephrase existing feature descriptions.
  </action>
  <verify>
    <automated>python -c "f=open('writ-vscode/README.md','r',encoding='utf-8');c=f.read();f.close();assert 'Namespace completions' in c;assert 'Crash stacktrace' in c or 'stacktrace' in c.lower();assert 'new' in c.lower() and 'keyword' in c.lower();assert '0.1.1' in c;print('OK')"</automated>
  </verify>
  <done>VS Code extension README documents namespace completions, new-keyword completions, crash stacktraces, multi-file project debugging, and crash halt with break-before-unwind. Release notes section added for 0.1.1.</done>
</task>

</tasks>

<verification>
Both README files render correctly and contain accurate, current information reflecting the v7.0 project state.
</verification>

<success_criteria>
- README.md lists all 10 crates, writ-vscode, and benchmark in project structure
- README.md documents all 6 CLI commands including `writ build`
- README.md mentions developer tooling (LSP/DAP) and benchmark suite
- README.md version history covers v1.0 through v7.0
- writ-vscode/README.md mentions namespace completions, new-keyword completions, crash stacktraces
- writ-vscode/README.md has 0.1.1 release notes
- License, contributing, and CLA sections unchanged in root README
</success_criteria>

<output>
After completion, create `.planning/quick/260322-5zi-update-the-readme-md-for-the-project-and/260322-5zi-SUMMARY.md`
</output>

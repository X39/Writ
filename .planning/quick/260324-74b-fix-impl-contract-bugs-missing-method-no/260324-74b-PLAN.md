---
phase: quick
plan: 260324-74b
type: execute
wave: 1
depends_on: []
files_modified:
  - writ-compiler/src/check/env.rs
  - writ-compiler/src/emit/collect/contracts.rs
  - writ-compiler/tests/typecheck_tests.rs
  - writ-compiler/tests/emit_tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "E0123 IncompleteContractImpl diagnostic shows the actual target type name (e.g. 'MyClass'), not 'impl#0'"
    - "A complete impl block with self-receiving methods compiles to a module where the method body has reg_count >= 1 (r0 for self)"
    - "Runtime does not crash when dispatching a method call on a class instance that implements a contract with self methods"
  artifacts:
    - path: "writ-compiler/src/check/env.rs"
      provides: "Fixed validate_contract_impls using target_def_id for type name lookup"
      contains: "def_map.get_entry(target_def_id)"
    - path: "writ-compiler/src/emit/collect/contracts.rs"
      provides: "fn_param_map includes self as first entry when has_self is true"
      contains: "self"
    - path: "writ-compiler/tests/typecheck_tests.rs"
      provides: "Test asserting E0123 message contains the target type name"
    - path: "writ-compiler/tests/emit_tests.rs"
      provides: "Test asserting impl method with self has reg_count >= 1"
  key_links:
    - from: "writ-compiler/src/emit/collect/contracts.rs"
      to: "writ-compiler/src/emit/body/mod.rs"
      via: "fn_param_map populated with self entry -> body emitter pre-allocates r0"
      pattern: "fn_param_map.*insert"
---

<objective>
Fix two impl contract bugs: (1) E0123 diagnostic shows "impl#0" instead of the target type name, and (2) impl methods with self parameter crash the runtime because the body emitter never pre-allocates r0 for self.

Purpose: These bugs make contract implementations unusable for any method that takes self.
Output: Corrected compiler code with regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@writ-compiler/src/check/env.rs (validate_contract_impls at line 298)
@writ-compiler/src/emit/collect/contracts.rs (collect_impl, fn_param_map at line 143)
@writ-compiler/src/emit/body/mod.rs (TypedDecl::Impl body emission at line 435)
@writ-compiler/src/emit/body/expr/mod.rs (SelfRef always returns r0 at line 68)
@writ-compiler/src/resolve/collector.rs (impl blocks named "impl#N" at line 192)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix E0123 type name and add self to fn_param_map</name>
  <files>
    writ-compiler/src/check/env.rs
    writ-compiler/src/emit/collect/contracts.rs
    writ-compiler/tests/typecheck_tests.rs
    writ-compiler/tests/emit_tests.rs
  </files>
  <behavior>
    - Test: E0123 diagnostic message for incomplete impl contains "MyClass" (not "impl#0")
    - Test: A complete impl with `fn greet(self) {}` emits a method body with reg_count >= 1
    - Test: Existing test_incomplete_contract_impl_error still passes
    - Test: Existing test_complete_contract_impl_no_error still passes
  </behavior>
  <action>
    **Bug 1 fix** in `writ-compiler/src/check/env.rs`, function `validate_contract_impls()`:
    - Rename `_target_def_id` to `target_def_id` on line 301
    - Replace line 337 (`let ty_name = impl_entry_entry.name.clone();`) with:
      `let ty_name = def_map.get_entry(target_def_id).name.clone();`
    - Update the misleading comment on lines 329-336 to reflect this fix
    - The `impl_entry_entry` (DefEntry for the impl block) has name "impl#N" because
      `resolve/collector.rs:192` assigns synthetic names. The target type's actual name
      lives in the DefEntry for `target_def_id` (the `impl_index` key).

    **Bug 2 fix** in `writ-compiler/src/emit/collect/contracts.rs`, inside the method loop (~line 143-156):
    - After the existing `has_self` check (line 114), when building `fn_params`:
    - Change the fn_params construction to INCLUDE self when `has_self` is true.
    - Prepend `("self".to_string(), self_ty)` to the params list, where `self_ty` is
      `ast_type_to_ty_simple(&impl_decl.target, &impl_entry_generics, def_map)`.
    - This means the body emitter at `emit/body/mod.rs:455` will naturally allocate r0
      for self before regular params, matching the convention that `SelfRef` returns r0.
    - NOTE: `impl_decl.target` is the AstType of the target (e.g., `MyClass`), and
      `ast_type_to_ty_simple` resolves it to Ty(5) for non-primitive types, which is
      acceptable for register allocation (only affects debug info).

    **Tests:**
    - In `typecheck_tests.rs`, add a test `test_incomplete_impl_error_shows_type_name` that
      parses the E0123 diagnostic text and asserts it contains "MyClass" (not "impl#").
      Use the existing `typecheck_src` helper. Extract diagnostic message text and check
      with `contains("MyClass")`.
    - In `emit_tests.rs`, add a test `test_impl_method_with_self_has_registers` that compiles:
      ```
      pub class Greeter {}
      pub contract Greetable { fn greet(self); }
      impl Greetable for Greeter { fn greet(self) {} }
      pub fn main() {}
      ```
      Then calls `emit_all_bodies` and asserts the impl method body has `reg_count >= 1`.
      Use the existing `emit_src` helper plus `emit_all_bodies` (or inspect the builder's
      fn_param_map to verify self is present).
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler -- incomplete_contract_impl --nocapture 2>&1 && cargo test -p writ-compiler -- impl_method_with_self --nocapture 2>&1 && cargo test -p writ-compiler -- contract --nocapture 2>&1</automated>
  </verify>
  <done>
    - E0123 diagnostic says "MyClass does not fully implement MyContract" (not "impl#0")
    - Impl method body with self has reg_count >= 1 (r0 allocated for self)
    - All existing contract tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end runtime validation</name>
  <files>
    writ-compiler/tests/emit_body_tests.rs
  </files>
  <action>
    Add an end-to-end emit body test that compiles a class with a contract impl containing
    a self method that accesses self, and verify the emitted instructions are correct:

    ```writ
    pub class Counter { value: int }
    pub contract Incrementable { fn increment(self) -> int; }
    impl Incrementable for Counter {
        fn increment(self) -> int { 42 }
    }
    pub fn main() -> int {
        let c: Counter = new Counter { value: 0 };
        c.increment()
    }
    ```

    Use the existing `emit_body_src` or equivalent helper from `emit_body_tests.rs`.
    Verify:
    - The module compiles without errors
    - The impl method body has reg_count >= 1
    - The method body contains a Ret instruction (not just RetVoid with 0 regs)

    If `emit_body_tests.rs` doesn't have a suitable helper, add the test to `emit_tests.rs`
    instead (which has `emit_src`).

    Run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd D:/dev/git/Writ && cargo test -p writ-compiler 2>&1 && cargo test -p writ-runtime 2>&1</automated>
  </verify>
  <done>
    - End-to-end test passes: class + contract impl + self method compiles correctly
    - Full writ-compiler and writ-runtime test suites pass with no regressions
  </done>
</task>

</tasks>

<verification>
- `cargo test -p writ-compiler` — all compiler tests pass
- `cargo test -p writ-runtime` — all runtime tests pass
- New tests specifically cover both bugs
</verification>

<success_criteria>
- E0123 shows actual type name, not "impl#0"
- Impl methods with self compile to bodies with reg_count >= 1 (r0 for self)
- No runtime crash when calling impl methods with self on class instances
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260324-74b-fix-impl-contract-bugs-missing-method-no/260324-74b-SUMMARY.md`
</output>

fn check(val: bool?) -> int {
    match val {
        Option::None => { 0 }
        Option::Some(v) => { 1 }
    }
}

fn main() {
    let a: int = check(null);
    let b: int = check(Option::Some(true));
}

[package]
name = "writ-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
chumsky = { version = "0.12.0", features = ["pratt"] }
logos = "0.16"

[dev-dependencies]
ariadne = "0.6"
insta = { version = "1", features = ["ron"] }
